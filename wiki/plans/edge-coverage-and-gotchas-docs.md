# Plan: edge-coverage introspection + gotchas docs page

## Goal

Two deliverables. (1) A queryable `edge_coverage` summary on `HivePlot` reporting what fraction of the underlying edges the current realization actually draws, surfaced as a property (drawn/total counts plus fraction) and as a percent-only clause in `__repr__` (Amendment 1: the repr carries no n/N; the property is the precision channel). Deliberately general: no taxonomy of *why* edges are undrawn (that bookkeeping is fragile once tags, collapsed axes, and repeat axes interact; Gary explicitly chose percent + (n/N)). (2) A short "gotchas" docs page collecting hive plot pitfalls in one place (intra-axis edges, >3-axes adjacency, overplotting, parallel-edge collapse, edge-kwarg-hierarchy overrides, `graph_directed` inference — the latter three per Amendment 4) so other docs link to it instead of re-explaining locally, cross-referencing the coverage property where natural.

Explicitly NOT a warning: `wiki/wiki/plans/graph-metric-conflict-validation.md` rejected a similar warning on the principle that warnings need a real, currently-silent data-fidelity loss with no existing point-of-use signal. This repr line *becomes* that point-of-use signal.

### Out of scope (decided in discussion, recorded here)

- README changes (stays as-is with existing pointers to quick_hive_plots and karate club notebooks). *Superseded for the leading-text addition only:* `docs-cheat-sheet-and-readme.md` WS-B adds "when and why" framing at the top of the README (note written by that plan's orchestrator at its Amendment 7, 2026-07-06); everything else in this bullet stands.
- A `quick_hive_plot()` convenience function with default partition/sorting — REJECTED: forcing the partition/sorting choice is deliberate interpretability pedagogy; `HivePlotMatrix.from_variable_sweep` is the sanctioned "try several" affordance.
- Any warning for undrawn edges (rejected per principle above).
- Changing `repeat_axes=True` default (breaking, partial fix only).
- `HivePlotMatrix` coverage aggregate — deliberate non-goal, not a deferral (Amendment 2). Edges appear in multiple cells by design, so any matrix-level number is semantically muddled, not just redundant ("it doesn't make sense at the matrix level"). Per-cell coverage is reachable via each populated cell's inherited property.
- P2CP: N/A — P2CPs draw all data; no contradicting prior record. `P2CP` composes a `BaseHivePlot` (`src/hiveplotlib/p2cp.py:54`) rather than inheriting, so placing the property on `BaseHivePlot` does not leak it onto `P2CP`'s public surface.

## Alignment (grill)

Status: aligned (2026-06-10). Planning complete; api-critic planning pass recorded below; all open decisions resolved by Gary (Amendments 1-3). Implementation dispatch **held** until branch `46-more-streamlined-networkx-usage-and-support` merges; this work then goes on a fresh branch off master as a separate MR (see Branch / MR sequencing).

Open decisions: none.

- ~~`HivePlotMatrix` propagation~~ — resolved as a deliberate non-goal, see Out of scope and Amendment 2.
- ~~Degenerate-case return when no edges exist~~ — confirmed by api-critic planning pass (`fraction=nan`, repr parenthetical omitted), pinned in Design notes.

Route any resulting plan change to amend-plan (rule 14).

### Execution record (2026-07-06)

- Branch-46 gate lifted (46 merged as MR !33, 2026-06-18). Executing on branch `55-edge-coverage-and-gotchas` (fresh worktree off master), issue #55, draft MR !37.
- Grill for the execution run: **knowingly skipped** per Gary's kickoff directive (2026-07-06). Consequently no `## Failure modes` rubric exists.
- Auto-dispatch: **authorized by Gary** (2026-07-06 kickoff), an explicit maintainer override of the rubric-gated default. Halts: any `must-fix`, any `STATUS: BLOCKED`, any workstream-set change (orchestrator amend-plan, then halt). One commit per workstream after the gate battery (format, test, ty, docs), each pushed. Session never merges.
- Cross-branch constraint (Gary, 2026-07-06): unmerged MR !36 changes `Edges.relevant_edges` from boolean masks to integer position arrays. WS-A implements against master but routes counting through the single private helper already pinned in Design notes ("Representation-agnostic seam"), keeping the !36 reconciliation a one-spot change; the Implementation log records it. (The Prior-ADRs bullet's "WS-1 (not started)" is stale: scaling WS-1 shipped on unmerged branch 53.)

### Adversary's challenge

Status: challenge (6 items)
Plan reviewed: wiki/wiki/plans/edge-coverage-and-gotchas-docs.md (cold). Grill knowingly skipped per the Execution record, so no `## Failure modes` rubric exists and none is coming; these items await the maintainer's async read and the adversary's own post-impl reconcile, not a grill. Rubric absence is the normal pre-grill state, not a defect; the binding Design notes plus WS done-when bullets are the de-facto standard the post-impl attack will use.

Baseline verification (against the `hiveplotlib-55-edge-coverage-and-gotchas` worktree at origin/master): the load-bearing claims hold. Boolean-mask storage per `[a0][a1][tag]` cell confirmed (`hiveplot.py:896` type hint, `:958` write site; `edges.py:121-124`; the `Edges.copy` docstring's "index arrays" at `edges.py:245` is loose wording, not a storage change). `HivePlotMatrix` does not inherit `BaseHivePlot` (`hiveplot_matrix.py:186`), so the property cannot leak onto HPM; P2CP composes as claimed (`p2cp.py:53-54`); the only `__repr__` in `hiveplot.py` is `HivePlot`'s (`:2623`, edges clause now at `:2638`); `__init__.py:9-13` re-exports exactly as Amendment 3 pins; all four repr tests present (`tests/hiveplot_test.py:5622-5676`); datasets, `**hive_plot_kwargs` passthrough (`toy_hive_plots.py:371`), `_warn_if_parallel_edges_collapse` (`hiveplot.py:260`), `graph_multigraph=True` workaround, the three API-example constructor params, WS-B's registration surfaces (`index.rst` "Hive Plots", `conf.py` `nbsphinx_thumbnails`), and all four cross-linked notebooks are all real. Remaining line drift is minor and expected. The seam design also survives the !36 semantics on its own terms: the future index-array branch (`np.union1d` then `len`) never indexes a DataFrame, so the scaling plan's positional-indexing rule cannot bite it.

Angles worked: premise (the pitfall is real and documented; the one soft claim is item 4), approach (alternatives genuinely considered and rejected with named reasons: warning, `quick_hive_plot`, HPM aggregate, FAQ page; no unexamined smaller move found), size-and-maintenance (one property, one NamedTuple, one private helper, one repr clause; the top-level export is the sole addition beyond minimal and Amendment 3 justifies it; the gotchas page consolidates existing re-explanations rather than adding surface). No existential item: this plan should exist.

Items:
  - [must-fix] WS-A's cross-plan edit targets text that no longer exists: the scaling plan corrected its "only known read site" claim itself (In-scope tweak, 2026-07-03, six grep-verified read sites) and scaling WS-1 shipped on branch 53 — at WS-A Files; Prior ADRs bullet 1; Design notes (seam).
    Rubric: no rubric yet — pre-grill
    Push: re-scope the one-line edit before dispatch: append the coverage helper to the scaling plan's WS-1 read-site/consumer list so the !36 lockstep reconcile catches the seventh consumer, or drop the cross-plan edit and rely on this plan's Implementation-log record per the Execution-record constraint. As written, an implementer either halts on the missing target text (rule 9) or improvises an edit into an active plan mid-auto-dispatch; both are avoidable now for one amendment line.
  - [worth-discussing] Stale wiki-state claims: "No `wiki/wiki/adr/` directory exists yet; no prior ADRs" is false as of execution (ADR 0001 networkx-integration and ADR 0002 exist), and `graph-metric-conflict-validation.md` plus `i-want-to-plan-optimized-hoare.md` now live under `plans/archived/` — at Prior ADRs / design docs; Amendment 4.
    Rubric: no rubric yet — pre-grill
    Push: refresh the pointers in one amendment and cite ADR 0001 as the durable source for the gotcha-4/6 behaviors. Also resolve the now-moot gating language: Amendment 4's "gated Workstream E blurb ... (not yet reviewed)" is no longer gated; that plan fully shipped, and the `graph_directed`-inference coverage lives in `examples/computing_graph_metrics.ipynb` (the only example matching `graph_directed` on the baseline), which is gotcha 6's concrete link-out target. Amendment 4's no-deduplication decision stands untouched.
  - [worth-discussing] The llms.txt convention postdates this plan and is unaddressed: `docs/source/_llms/llms.txt` exists on the execution baseline with a CLAUDE.md trip-wire, and WS-B adds exactly the kind of conceptual "why does my plot look wrong" page the curated index exists for, yet no WS-B Files or done-when bullet mentions it — at WS-B Files / done-when.
    Rubric: no rubric yet — pre-grill
    Push: record the llms.txt decision in WS-B before its dispatch: add the entry (a pitfalls index is plausibly a conceptual entry point that "pins high") or knowingly skip; notebook-author owns example-notebook entries per CLAUDE.md. Decide WS-A's `edge_coverage` mention at the same time (more marginal; a new exported class arguably meets the "new capability" bar).
  - [worth-discussing] Premise over-claim in Goal: "This repr line *becomes* that point-of-use signal" reaches only users who echo the object; the silent-data-loss victim calls the viz function directly and never sees a repr. The discovery channel is really the property plus the gotchas page, not the repr — at Goal.
    Rubric: no rubric yet — pre-grill
    Push: accept consciously (one Goal line: the repr serves users who inspect; the gotchas page is the discovery channel) or spend one echoed `hp` in an entry-point notebook so every docs reader sees the clause once. No design change; the property justifies the feature on its own. This does not reopen Amendment 1, which pinned format, not channel reach.
  - [worth-discussing] WS-B's cross-link touch of `hive_plots_more_than_three_groups.ipynb` intersects the live mpl-3.11/cartopy freeze on that notebook (maps break; standing keep-uncommitted guidance) — at WS-B Files.
    Rubric: no rubric yet — pre-grill
    Push: pin the cross-link edit to markdown-cell-only with no re-execution, and pre-check the installed matplotlib before WS-B's docs/test-nb gates; if the freeze is live in the execution env, route the touch through amend-plan rather than letting an agent park on a frozen notebook run.
  - [low-confidence] The seam's write-site docstring misnames the attribute: `__store_edge_ids` says results are stored in `edges.relevant_indices[...]` (`hiveplot.py:906` on the baseline) but the attribute is `relevant_edges` — at Design notes (Representation-agnostic seam).
    Rubric: no rubric yet — pre-grill
    Push: optional one-word docstring fix inside WS-A, which already documents that exact neighborhood as the seam's write site; a stale name in the seam's own doc trail undercuts the one-spot-change story.

### Adversary post-impl

Status: propose
Artifact reviewed: Workstream A, uncommitted diff in the `hiveplotlib-55-edge-coverage-and-gotchas` worktree (`src/hiveplotlib/hiveplot.py`, `src/hiveplotlib/edges.py`, `src/hiveplotlib/__init__.py`, `CHANGELOG.rst`, `tests/hiveplot_test.py`, `tests/edges_test.py`). Blind attack 2026-07-06 against the WS-A done-when bullets and binding Design notes (no `## Failure modes` rubric exists; grill knowingly skipped per the Execution record), reconciled afterward against Amendment 5, the Implementation log, and the api-critic post-impl section.
Dispositions held: yes. No scope ballooning: the diff is exactly the WS-A surface plus Amendment 5 item 6's authorized one-word fix. Specifics:
  - Amendment 5 item 1, both halves verified directly rather than via the api-critic's voucher: the seventh-consumer line sits at `scaling-large-networks.md:1532` (main checkout), verbatim per the amendment and framed as a post-ship addendum, not a retroactive member of the lockstep six; the Implementation log's WS-A entry records the seam and the cross-plan pointer.
  - Amendment 5 item 6 landed (`hiveplot.py:926` now names `relevant_edges`); no CHANGELOG entry, as pinned. Blind, this read as an unauthorized drive-by edit; reconciled, it is the adversary's own planning item 6, disposed and executed.
  - Amendment 5 item 3's WS-A half held: no llms.txt change shipped.
  - Amendments 1-3 held in the artifact: repr clause exactly per the pinned spec (`hiveplot.py:2680-2690`; all four boundary cases plus the thousands separator test-pinned); no coverage surface leaked onto `hiveplot_matrix.py` or `p2cp.py` (HPM repr self-contained, neither class inherits `BaseHivePlot`, no delegation path); export per Amendment 3 (`__init__.py:12`, exercised in-body by the import test).
  - The "Patterns this replaces" `:5675-5676` deviation is sound: `test_repr_collapsed_axis` left unchanged with recorded reasoning; verified blind that its substring assertions are format-agnostic for the edges clause, which the parametrized repr test pins exhaustively.
Blind verification that held under attack: counting semantics exact (denominator `len(self.edges)` sums rows across tags, `edges.py:97`; numerator is per-tag distinct via mask union, `edges.py:271-279`, summed over live tags, `hiveplot.py:1862`); the seam holds (every other `relevant_edges` reader in `src/` is a selection site, no counting path bypasses `_drawn_count`, and the property and repr both route through it); no caching; degenerate `EdgeCoverage(0, 0, nan)` with `total == 0` guidance as pinned; test list complete against every done-when bullet with test-name = test-body throughout; the two-cells-once test cannot pass vacuously (positive mask-sum assertion, `hiveplot_test.py:5823-5825`); CHANGELOG under v0.29.0 Added; CRLF sweep clean; holdout survived (`toy_hive_plots.py:385`).
Gates: pending qa per the Execution record (formal `make test` / `make format` / `make ty` battery not yet run). Targeted evidence so far: 355 tests green scoped (`-n 7`, warnings-as-errors), ruff clean on touched files, ty clean per the code-engineer.
Concerns:
  - [must-fix] `EdgeCoverage` has no docs home, so the `:py:class:` xrefs at `hiveplot.py:1857` and `CHANGELOG.rst:22` dangle and the field docstrings render nowhere — at docs/source/autodoc/hive_plots/high_level_hive_plot_api.rst:8
    Rubric: no entry (no rubric exists; grill skipped)
    Found independently in the blind pass; concurs with and dedupes into the api-critic's must-fix, whose additions close the no-gate loop (`nitpicky` unset in `conf.py`, changelog docs-rendered). Root cause is plan inventory, not implementer deviation: WS-A Files never listed a docs rst. Fix routes through amend-plan: one `.. autoclass:: hiveplotlib.EdgeCoverage` line with `:members:`, placement per the api-critic's worth-discussing item.
  - [worth-discussing] Two example notebooks carry stored outputs pinning the replaced repr clause (`... 86 edges, partition=...`, no percent parenthetical): the cells printing child hive plots in `examples/hpm_from_partition.ipynb` (raw lines 434, 501) and `examples/hpm_generic.ipynb` (raw line 567) — at examples/hpm_generic.ipynb:567
    Rubric: no entry (no rubric exists; grill skipped)
    The "Patterns this replaces" consumer inventory named only the test assertions; execution-based notebook testing stays green on stale stored outputs, and docs built from them would show the old repr beside a changelog announcing the new one. Fix: re-execute the two notebooks (neither is the mpl-3.11-frozen one) or accept the staleness knowingly. No bearing on WS-B, whose notebook executes fresh; plan-end batching would apply were the run not already halting on the must-fix.
  - [worth-discussing] The !36 reconcile record understates the swap surface by two test bodies: the direct `_drawn_count` tests plant boolean masks and must swap to index arrays with the helper body, but the seventh-consumer line (`scaling-large-networks.md:1532`) names only the helper, and the scaling plan's WS-1 test bullet (`:1534`) predates these tests — at tests/edges_test.py:268
    Rubric: no entry (no rubric exists; grill skipped)
    The miss is loud, not silent (`np.union1d` over planted bools collapses to at most two values and both tests fail), so this is record completion, not a correctness risk: append half a line to the seventh-consumer bullet, e.g. "its two direct tests in `tests/edges_test.py` plant boolean masks and swap with it." Cross-plan bearing only (scaling plan); none on WS-B.
  - [low-confidence] Adjacent contradictory representation docstrings: `Edges.copy()` says "numpy index arrays" (`edges.py:245`) one method above `_drawn_count`'s correct "boolean masks" (`edges.py:264`) — at src/hiveplotlib/edges.py:245
    Rubric: no entry (no rubric exists; grill skipped)
    Baseline text (the planning pass called it loose wording), and the scaling plan already tracks `copy()`'s docstring wording as a verify-not-break site (`scaling-large-networks.md:1533`), so it self-resolves at the !36 reconcile; until then master carries the contradiction beside the new seam. Optional one-word alignment if WS-A reopens for the must-fix anyway.
  - [low-confidence] `test_edge_coverage_multi_tag_fixture` is saturated (30 of 30 drawn, fraction 1.0), so alone it cannot distinguish per-tag distinct summing from wrong aggregations — at tests/hiveplot_test.py:5787
    Rubric: no entry (no rubric exists; grill skipped)
    Fully mitigated by the non-saturated siblings (2/3 cross-tag at `:5802`, 86/100 two-cells at `:5812`). Informational; no push.

#### Workstream B (2026-07-06)

Status: clean
Artifact reviewed: Workstream B ("Hive Plot Gotchas" docs page), uncommitted diff in the `hiveplotlib-55-edge-coverage-and-gotchas` worktree (`examples/hive_plot_gotchas.ipynb` new, `docs/source/notebooks/index.rst`, `docs/source/conf.py`, `docs/source/_static/hive_plot_gotchas.png`, `docs/source/_llms/llms.txt`, cross-link touches in `examples/adding_repeat_axes.ipynb` and `examples/hive_plots_more_than_three_groups.ipynb`). Blind attack against the WS-B done-when bullets plus Amendment 5's WS-B pins (no `## Failure modes` rubric exists; grill knowingly skipped per the Execution record), reconciled afterward against the `### Adversary's challenge` planning items and their disposition in Amendments 5-6.
Dispositions held: yes. No scope ballooning: the page is a short pitfalls index (one live demo on gotcha 1, prose+links for gotchas 2-6), exactly as scoped; no new datasets, no demo build-out on 4-6, no page bloat. Nothing to subtract. Specifics against the disposed planning items:
  - Adversary planning item (llms.txt convention unaddressed, disposed as Amendment 5 item 3 → "WS-B adds an entry, WS-A does not"): held in the artifact. `docs/source/_llms/llms.txt:44` carries exactly one WS-B entry under `## Getting started`, after the three existing links, one-line format, absolute `https://hiveplotlib.readthedocs.io/stable/notebooks/hive_plot_gotchas.html` URL — the pinned placement and shape. No WS-A llms.txt change (already confirmed in the WS-A entry above).
  - Adversary planning item (repr-reach over-claim, disposed as Amendment 5 item 4 → one `print(hp)` echo inside the intra-axis bullet at zero added scope): held. Gotcha 1 spends exactly one `print(hp)` echo (`cell-5`) showing the `(76% drawn)` clause in the check-coverage flow, on the page a confused user reaches. Not a manufactured extra cell.
  - Adversary planning item (mpl-3.11/cartopy freeze on the `>3 groups` cross-link touch, disposed as Amendment 5 item 5 → markdown-cell-only, no re-execution): held. The cross-link lands inside an existing markdown cell (`1d724b92`, `cell_type: markdown`), verbatim per the freeze pin. Blind I could not run `git diff HEAD` to byte-confirm zero output/execution-count churn and flagged it worth-discussing; the coordinator's direct check confirms it: `git diff HEAD -- examples/hive_plots_more_than_three_groups.ipynb` is +3/-1 all inside a markdown `source` array, zero `execution_count`/`outputs`/`output_type`/`image/png` hits, same for `adding_repeat_axes.ipynb` (+2, markdown-only). The freeze constraint held; my worth-discussing resolves to clean.
  - Adversary planning item (gotcha-6 deliberate-overlap retention, Amendment 4 no-deduplication decision, gotcha-6 link target pinned to `computing_graph_metrics` by Amendment 5 item 2): held. Gotcha 6 (`cell-15`) is its own distinct entry that links out to `computing_graph_metrics.ipynb#controlling-the-hiveplot-internal-graph-type` rather than being deduplicated away. The binding decision is honored.
Blind verification that held under attack: all six gotchas present and correctly characterized against their done-when descriptions (`cell-2` intra-axis, `cell-11` >3 axes, `cell-12` overplotting, `cell-13` parallel-edge collapse, `cell-14` kwarg hierarchy, `cell-15` `graph_directed` inference); gotcha 1 genuinely demonstrates `edge_coverage` with a faithful repr echo (`cell-3` shows `EdgeCoverage(drawn=76, total=100, fraction=0.76)`, `cell-5` `print(hp)` shows `...100 edges (76% drawn)...`, verified against the real `__repr__` at `hiveplot.py:2667-2699` with no `__str__` override so `print` falls back to repr, and against `example_hive_plot`'s seeded defaults `num_nodes=100/num_edges=100/seed=0` that make 76/100 reproducible); sole dataset is corpus-established `example_hive_plot` (`toy_hive_plots.py:360`), no new dataset; prose voice-compliant (zero em-dashes, zero AI-filler tells by grep); registration complete (`index.rst:22,35` nblinkgallery + toctree under "Hive Plots", `conf.py:76` thumbnail); all five sibling-notebook out-links target real headings following the slugified-heading convention; gotcha-4 `graph_multigraph=True` + collapse-warning and gotcha-6 inference machinery are real in `src/`.
Gates: `make docs` rebuilt clean (exit 0, "build succeeded") per the coordinator; `make test` / `make test-nb` / `make format` / `make ty` are the formal WS-B qa battery, pending-qa, with the evidence that the new notebook passed its scoped nbmake during authoring. viz-critic returned one worth-discussing (recovered intra-group edges not visually distinguished though the caption points at them; house-style fix = color them per `adding_repeat_axes`) with no downstream bearing (WS-B is the last workstream), batching to plan-end per the auto-dispatch rules; not mine to dispose and it does not change my subtractive read (a per-edge color styling is not added surface, it is a fidelity fix on the one existing demo figure).
Concerns:
  - [low-confidence] The intra-axis section's API-reference link uses the Python-domain object-id anchor (`../autodoc/hive_plots/low_level_hive_plot_api.rst#hiveplotlib.BaseHivePlot.edge_coverage`) rather than the slugified-heading anchor every sibling API-reference link uses — at examples/hive_plot_gotchas.ipynb cell-10 (raw line 184)
    Rubric: no entry (no rubric exists; grill skipped)
    Blind I raised this as must-fix, on two worries: convention deviation, and an unverifiable fragment (no built HTML in the worktree, and `linkcheck`/`make docs` do not validate an intra-page `#fragment`). Both dissolve on reconcile. (a) The anchor form is deliberately PINNED by Amendment 6 item 1's "WS-B bearing" clause precisely because the target is an autodoc'd attribute, a different target class from the sibling section-heading links; the slugified-heading convention was never the right comparand for an object-id target. (b) The coordinator rebuilt the docs and grepped the built HTML: `id="hiveplotlib.BaseHivePlot.edge_coverage"` is minted on `low_level_hive_plot_api.html`, the gotchas page's rendered href matches it exactly, and `grep -c "edge_coverage]("` on the built page is 0 (no unrendered-markdown leak); the api-critic's WS-A closure independently confirmed the same anchor resolves from the CHANGELOG xref. The link resolves. Downgraded to low-confidence as a standing watch only: this is the one fragment class `make docs` cannot gate, so future edits to that anchor (or a rename of the property/class) would break silently — worth a targeted built-HTML grep whenever this specific link changes, not a WS-B defect.
No surviving must-fix. My blind finding 1 (must-fix) dissolves to the low-confidence watch above; blind finding 2 (worth-discussing) confirmed clean by the coordinator's direct diff; blind finding 3 was already self-cleared as a Grep display artifact. WS-B is clean against its done-when contract and against every disposed planning item; the only open items are qa's pending gate battery and the viz-critic worth-discussing batched to plan-end, neither of which is mine to dispose.

## Prior ADRs / design docs

No `wiki/wiki/adr/` directory exists yet; no prior ADRs. Relevant plans/concepts (research-liaison pre-task pass):

- `wiki/wiki/plans/scaling-large-networks.md` — WS-1 (not started) changes `relevant_edges` storage from boolean masks to integer index arrays plus a Dask-aware non-materializing variant. Coverage must be representation-agnostic and becomes a **second consumer**; that plan's claim at its line ~65 that `viz/datashader.py:203-204` is "the only known read site" needs updating (it is already stale: `viz/matplotlib.py:460`, `viz/bokeh.py:580`, `viz/holoviews.py:636`, `viz/plotly.py:726`, `hiveplot.py:4555` also read it).
- `wiki/wiki/plans/graph-metric-conflict-validation.md` — warnings-rejection principle cited in Goal; warning conventions if ever needed (plain UserWarning, stacklevel=3, api-critic owns message text).
- `wiki/wiki/plans/hiveplot-unify-axes.md` — HPM mirroring precedent (same name, same shape) if propagation is chosen.
- `wiki/wiki/plans/i-want-to-plan-optimized-hoare.md` — seven-surface kwarg-replication pain; deliberate propagation decisions.
- `wiki/wiki/concepts/edge-rendering.md` — edge tags are separate DataFrames; pipeline reference for numerator/denominator semantics.
- `wiki/wiki/analyses/karate-club-walkthrough.md`, `wiki/wiki/concepts/hive-plot.md` ("Why Three Axes?") — gotchas source material.
- Wiki log 2026-05-31 entry on `relevant_edges` syncing across `Edges.copy` / `reset_edges` — correctness constraint for coverage.

## Patterns this replaces

- `HivePlot.__repr__` edges clause `f"{len(self.edges)} edges, "` at `src/hiveplotlib/hiveplot.py:2543`. Replace with `f"{coverage.total:,} edges ({pct}% drawn), "` (exact spec pinned in Design notes, "Repr display rule"). Existing repr assertions at `tests/hiveplot_test.py:5627`, `:5641`, `:5655`, `:5675-5676` must be updated to the new format.
- Everything else is net new addition.

## Default justifications

No new constructor parameters or defaults. One degenerate-case behavior to pin down (not a default per se): when `self.edges is None` or `len(self.edges) == 0`, the property returns `drawn=0, total=0, fraction=float("nan")` (a 0/0 coverage is undefined, and `0.0` would falsely read as "everything hidden"), and the repr omits the coverage parenthetical. Api-critic planning pass to confirm.

## Naming audit

- New property: **`edge_coverage`** (recommended) on `BaseHivePlot`, inherited by `HivePlot`. Alternatives considered: `coverage` (ambiguous — axis coverage? node coverage?), `drawn_edges` (sounds like it returns the edges themselves), `edge_visibility` (collides with viz-kwarg vocabulary like alpha/zorder). "Coverage" matches user vocabulary from test coverage: fraction of a whole that is exercised. Property, not method: it takes no arguments and is cheap (a dict walk plus mask ORs), matching existing zero-arg properties like `edge_kwarg_hierarchy`.
- New return type: **`EdgeCoverage`** NamedTuple with fields `drawn: int`, `total: int`, `fraction: float`. `fraction` in [0, 1] (not `percent`; matches pandas/numpy vocabulary where user-facing fractions are unit-interval floats). NamedTuple gives named access (`hp.edge_coverage.fraction`), tuple unpacking, and a readable default repr. Export location (Amendment 3, per api-critic recommendation): defined in `src/hiveplotlib/hiveplot.py` next to `BaseHivePlot`, re-exported from the `hiveplotlib` top-level `__init__` (which already re-exports from `hiveplotlib.hiveplot`, `src/hiveplotlib/__init__.py:9-13`), so `from hiveplotlib import EdgeCoverage` works for annotations and `isinstance` checks.
- Docs page title: **"Hive Plot Gotchas"** at `examples/hive_plot_gotchas.ipynb` (recommended). Direct precedent: pandas' long-standing "Gotchas" user-guide page, so the word is established ecosystem vocabulary, not slang. Alternatives: "Common Pitfalls" (fine but no ecosystem anchor), "FAQ" (Gary lukewarm; the page answers "why does my plot look wrong," not arbitrary questions).
- Prose-only terms: "drawn" / "undrawn" edges (used in repr and docs); "realization" only in wiki/plan prose, not user-facing.

## API usage examples

### Proposed (from planner / Orchestrator)

```python
# Example 1: check how many edges the current realization draws
# Example data:
from hiveplotlib.datasets import example_hive_plot

hp = (
    example_hive_plot()
)  # generates edges between all axes incl. repeat; built without repeat axes

# Call site:
coverage = hp.edge_coverage
print(coverage)  # EdgeCoverage(drawn=82, total=100, fraction=0.82)
print(coverage.fraction)  # 0.82
print(hp)  # ... 100 edges (82% drawn), partition='partition_0', ...
```

```python
# Example 2: adding repeat axes recovers intra-axis edges; coverage reflects it
# Example data:
from hiveplotlib.datasets import example_hive_plot

hp_no_repeat = example_hive_plot()
hp_repeat = example_hive_plot(repeat_axes=True)

# Call site:
assert hp_repeat.edge_coverage.fraction > hp_no_repeat.edge_coverage.fraction
```

```python
# Example 3: coverage on a networkx-built hive plot (intra-club edges undrawn with two axes)
# Example data:
import networkx as nx

g = nx.karate_club_graph()

# Call site:
from hiveplotlib import HivePlot

hp = HivePlot(
    graph=g,
    node_graph_metrics="degree",
    partition_variable="club",
    sorting_variables="degree",
)
drawn, total, fraction = (
    hp.edge_coverage
)  # tuple unpacking; fraction < 1 (intra-club edges hidden)
```

(Examples written against the current branch surface: `HivePlot(graph=...)` is the keyword-only parameter shipped in the in-flight v0.28 work at `src/hiveplotlib/hiveplot.py:2084`.)

### API Critic's take (planning mode)

Reviewed 2026-06-10 against the snippets above; verified `example_hive_plot(repeat_axes=True)` is real (flows through `**hive_plot_kwargs`, `toy_hive_plots.py:370`) and the repr clause being replaced is at `hiveplot.py:2543`. Verdicts on the flagged questions:

- **Property vs method: Agreed.** Zero arguments, cheap, read-only introspection; matches the `edge_kwarg_hierarchy` precedent (`hiveplot.py:2550`), and no-caching means there is no staleness argument for a method.
- **`EdgeCoverage(drawn, total, fraction)`: Agreed**, including field order (reads as n, N, n/N) and `fraction` over `percent` (unit-interval float matches pandas/numpy vocabulary). The NamedTuple earns its place: Example 3's tuple unpacking and Example 1's `.fraction` access are both real usage shapes a bare float or string can't serve, and the default repr is the headline print in Example 1.
- **Missing decision — `EdgeCoverage` export location.** The plan never says where the class lives or whether users can import it. A user annotating `coverage: EdgeCoverage` or catching it in `isinstance` needs an import path; define it in `hiveplot.py` next to `BaseHivePlot` and export from the `hiveplotlib` top level (the `__init__` already re-exports from `hiveplotlib.hiveplot`). Pin before WS-A dispatch.
- **Degenerate no-edges case (`fraction=nan`, repr clause omitted): Agreed.** `nan` is honest where `0.0` lies ("everything hidden") and `1.0` is vacuous; it also does the right thing in the natural user check, since `hp.edge_coverage.fraction < 1` ("are edges hidden?") is `False` on an empty plot. Two docstring requirements: (1) document the NaN explicitly and point users at `total == 0` as the emptiness check (NaN fails `==` against itself, so `fraction != fraction` traps the unwary); (2) keep `drawn=0, total=0` as stated so the counts stay arithmetic-safe.
- **Repr clause: prefer the planner's short form, but pin the discrepancy first.** The Goal sentence ("percent plus drawn/total counts, surfaced both as a property and as a clause in `__repr__`") can be read as the repr also carrying n/N (e.g. `"82% of edges drawn (82/100)"`), while the proposed clause is `f"{total:,} edges ({pct}% drawn), "`. Preferred: the proposed short form. Reason: the clause already carries N, an n/N parenthetical duplicates it ("100 edges (82/100 drawn)"), and the property is the precision channel one access away; the repr's job is the point-of-use nudge, not the full report. If Gary intended n/N literally in the repr, decide now since the repr tests bake the format. The rounding-inward boundary rule (no false 100%/0%) is exactly right either way.
- **Multi-tag counting legibility: Agreed**, because it changes nothing the user already sees: the existing repr's "N edges" is already `len(self.edges)`, i.e. tag-rows, so the coverage clause inherits established semantics rather than coining new ones. Requirement: the property docstring states it in one plain sentence ("an edge appearing under two tags counts once per tag, in both counts"), not only the design notes.

Recurring pattern to carry into WS-A: two pin-before-dispatch items (export location; repr n/N vs percent-only), both cheap now and format-breaking after the repr tests land.

### API Critic — post-implementation review

Reviewed 2026-07-06 in post-impl mode against the `hiveplotlib-55-edge-coverage-and-gotchas` worktree (line numbers below are that worktree's): `EdgeCoverage` NamedTuple (`src/hiveplotlib/hiveplot.py:391`), `edge_coverage` property (`:1842`), repr clause (`:2679-2690`), `Edges._drawn_count` seam (`src/hiveplotlib/edges.py:259`), top-level re-export (`src/hiveplotlib/__init__.py:12`), CHANGELOG entry, new tests. Walked as a user against the plan's three API usage examples.

Planning-pass requirements, all five met:

- **NaN documented, `total == 0` as the emptiness check: met.** Property docstring states the degenerate return and "Since ``nan`` fails every comparison, check for this case with ``total == 0`` rather than via ``fraction``" (`hiveplot.py:1852-1853`).
- **Multi-tag counting in one plain sentence: met.** "An edge appearing under two tags is two rows and counts once per tag in both counts" (`hiveplot.py:1850`).
- **Field order `(drawn, total, fraction)`: met** (`hiveplot.py:400-407`), locked by `test_edge_coverage_named_tuple_import_and_unpacking`.
- **Repr percent-only short form with inward rounding: met**, exactly per the Design-notes spec (`hiveplot.py:2680-2690`): integer percent, 100 clamps to 99 unless `drawn == total`, 0 clamps to 1 when `drawn > 0`, thousands separator, degenerate `"0 edges, "`. All four boundary cases plus the separator are pinned in the parametrized repr test.
- **Top-level import path: met** (`__init__.py:12`), exercised in-body by the import/unpacking test.

Walkthrough notes: Example 1's shape reproduces (`EdgeCoverage(drawn=76, total=100, fraction=0.76)`; the repr clause reads naturally at real sizes, with `"1,000 edges (99% drawn), "` test-pinned). Example 2's assertion holds (0.76 vs 1.0). Example 3's tuple unpacking is the tested shape, and its `fraction < 1` claim is sound since `repeat_axes=False` is the `HivePlot` default. The property docstring leads with its one-sentence job, then why edges go undrawn, counting semantics, degenerate case, recompute-on-access; reads cleanly top-down. Verified in passing: Amendment 5 item 6's `relevant_edges` docstring fix landed (`hiveplot.py:926`); item 1's seventh-consumer line is in `scaling-large-networks.md` (main checkout, :1532) verbatim; no coverage surface leaked onto `hiveplot_matrix.py` or `p2cp.py` (Amendment 2 honored).

Status: propose
API surface reviewed: [BaseHivePlot.edge_coverage, EdgeCoverage (+ top-level re-export), HivePlot.__repr__ coverage clause, Edges._drawn_count (private seam), CHANGELOG entry]
Concerns:
  - [must-fix] `EdgeCoverage` is missing from the API reference: no `autoclass`/`automodule` under `docs/source/autodoc/` documents it, so the `:py:class:` xrefs at the property's `:return:` (`hiveplot.py:1857`) and in the docs-rendered changelog (`CHANGELOG.rst:22`, pulled in by `docs/source/changelog.rst`) both dangle as unlinked text, and the class's field docstrings (the drawn/total/fraction contracts) render nowhere. No gate catches this: `nitpicky` is unset in `conf.py` (the `nitpick_ignore` list at `conf.py:143` is inert without it), so `make docs` builds silently — at docs/source/autodoc/hive_plots/high_level_hive_plot_api.rst:8
    Suggested change: add `.. autoclass:: hiveplotlib.EdgeCoverage` with `:members:` to one API page, which resolves both dangles; placement per the next item.
  - [worth-discussing] `edge_coverage` renders only on the Low-Level API page, since `autoclass:: hiveplotlib.HivePlot` has `:members:` but not `:inherited-members:`; a HivePlot-page reader never encounters the property. This is the pre-existing convention for all inherited `BaseHivePlot` surface, not a WS-A regression, and it is mitigated (the changelog's `:py:attr:` deep-link resolves straight to the property; WS-B's gotchas page demos it) — at docs/source/autodoc/hive_plots/high_level_hive_plot_api.rst:8
    Suggested change: leave `:inherited-members:` alone; when placing the `EdgeCoverage` autoclass, choose deliberately between low-level adjacency (next to where the property renders, under `BaseHivePlot`) and high-level visibility (next to `HivePlot`, where its users look).
  - [low-confidence] Manual-wiring honesty (the code-engineer's flagged caveat, examined): `_drawn_count` only counts cells whose tag key appears in `edges.tags` (`edges.py:271-276`), so wiring drawn via `connect_axes` under tags absent from the edge data (auto-generated integer tags against a hand-assigned multi-tag `Edges`, or ad-hoc subset tags) is silently invisible to `drawn`, and deliberately mismatched mask lengths under one tag across axis pairs would surface as an opaque numpy broadcast error from the property. Both require hand-wiring off the automated path; the fully-manual `BaseHivePlot` case (`edges` attribute never assigned) lands in the documented `total == 0` degenerate branch, which the docstring names accurately ("``edges`` unset or empty"). The docstring stays honest for every documented path without dragging manual wiring in — at src/hiveplotlib/edges.py:271
    Suggested change: accept as-is per the plan's automated-path-semantics pin; if a hedge is ever wanted, one clause suffices ("``drawn`` only counts wiring recorded under tags present in the edge data").
Test-method-coverage audit: clean (sampled: `test_edge_coverage_no_repeat_vs_repeat_axes`, `test_edge_coverage_edges_unset`, `test_edge_coverage_drops_after_reset_edges`, `test_edge_coverage_named_tuple_import_and_unpacking` all call `edge_coverage` in body; `test_repr_no_edges` and `test_repr_edge_coverage_percent_rounds_inward_at_boundaries` call `repr(...)`; both `test_drawn_count_*` tests call `_drawn_count`)

Closure (2026-07-06, scoped re-check of the `must-fix` only): CLOSED. `low_level_hive_plot_api.rst` now carries `.. autoclass:: hiveplotlib.EdgeCoverage` / `:members:` as its final entry, matching the three sibling directives' shape; it mints the `hiveplotlib.EdgeCoverage` anchor that both formerly-dangling `:py:class:` xrefs (property `:return:` at `hiveplot.py:1857`, CHANGELOG.rst:22-23) target, and renders the class with its three field docstrings on the page where `edge_coverage` itself renders. A user annotating `coverage: EdgeCoverage` or catching it in `isinstance` now finds it in the API reference, co-located with the `BaseHivePlot` property that returns it. The `worth-discussing` (HivePlot-page `:inherited-members:`) was a pre-existing convention, deliberately left in place per the disposition, not part of the must-fix.

## Notebook review

**Editorial-critic (post-impl, 2026-07-06): clean.** The notebook succeeds as the intended short "index of pitfalls with links" tutorial. Framing coherent (one reader question, six answers, no overpromise-vs-body gap); the single live demo (gotcha 1) is the right one to show since it exercises the new `edge_coverage` property; the five prose-only sections each earn their place and correctly sidestep warnings-as-errors; gotcha 6's deliberate overlap retention is the right call; all cross-links resolve; dataset coherence clean (only `example_hive_plot`, no new data); voice compliant (no em-dashes, no filler, length-disciplined). No must-fix / worth-discussing / low-confidence findings. (Editorial-critic has no plan-write access; outcome recorded by the dispatching session.)

**Viz-critic (post-impl, 2026-07-06): propose.** One figure (the gotcha-1 two-panel before/after) plus its thumbnail; polish budget correctly set at instructional tier (no over-polish ask). Verified good: title `y=1.05` nudge cleared the top axis label; the 76%→100% story reads truthfully from silhouette; honest comparable panels; grayscale-safe accessibility; clean thumbnail.
- **[worth-discussing]** The recovered intra-group edges are not color-distinguished, yet the cell-8 caption points directly at them ("the recovered intra-group edges are the ones curving between each axis and its repeat"). House-style fix (used on every repeat-axis figure in the linked `adding_repeat_axes.ipynb`): `for ax in ("A","B","C"): hp_repeat.update_edges(partition_id_1=ax, partition_id_2=ax, color=..., linewidth=3, alpha=1)`, plus a caption clause naming the highlight color. Cheap, house-style, not an over-polish ask; the headline reads from silhouette alone, so it is not blocking. **Disposition: batched to plan-end (no downstream workstream), surfaced to the maintainer for an apply/accept call per the auto-dispatch rules; not auto-applied.**
- **[low-confidence]** Right panel shows duplicate axis labels (two A/B/C), inherent to `example_hive_plot(repeat_axes=True)` and consistent with the corpus; pre-explained by the cell-6 markdown. No action. (Viz-critic has no plan-write access; outcome recorded by the dispatching session.)

## Design notes (Workstream A semantics — binding)

- **Counting semantics.** Denominator: `len(self.edges)` — total rows across all tag DataFrames. Per `wiki/wiki/concepts/edge-rendering.md`, tags are separate DataFrames; an edge appearing under two tags is two rows and counts once per tag in both numerator and denominator (tags are independent edge sets; collapsing duplicates across tags would require cross-tag row identity that doesn't exist). Document this in the property docstring. Numerator: for each tag, the number of **distinct** edge rows referenced by any `relevant_edges[a0][a1][tag]` cell, summed over tags. Distinct, not sum-of-cell-lengths: the same row can be referenced by multiple axis-pair cells (repeat-axes and bidirectional wiring), and a sum would double-count.
- **Representation-agnostic seam.** Today cells store boolean masks (`edges.py:121-124`, written at `hiveplot.py:898`); scaling WS-1 will move to integer index arrays plus a Dask-aware variant. Implement the per-tag distinct count behind one small private helper (e.g. `Edges._drawn_count(tag)` or a module-level function) that normalizes a cell's stored value: boolean masks → `np.logical_or.reduce(masks).sum()`; index arrays later → `len(reduce(np.union1d, arrays))`. The public property never touches the stored representation directly, so scaling WS-1 swaps one helper body. Update `wiki/wiki/plans/scaling-large-networks.md` WS-1's read-site claim in this workstream (one-line edit listing the helper as a consumer).
- **No caching.** Compute lazily on each property access from `relevant_edges`. This makes correctness across `Edges.copy` (deep-copies `relevant_edges`, `edges.py:256`), `reset_edges` (pops cells, `hiveplot.py:718-779`), and re-`connect_axes` automatic — no state to go stale (per the wiki log 2026-05-31 syncing entry).
- **Repr display rule (exact spec, pinned by Amendment 1 — percent-only, no n/N).** The edges clause at `hiveplot.py:2543` becomes `f"{coverage.total:,} edges ({pct}% drawn), "`, e.g. `"5,100 edges (82% drawn), "`. `pct` is an integer percent: `round(coverage.fraction * 100)`, then rounded inward at the boundaries — never display `100` unless `drawn == total` (clamp to `99`), never display `0` unless `drawn == 0` (clamp to `1`). No n/N in the repr: the clause already carries the total, and the `edge_coverage` property is the precision channel. Note `{total:,}` adds a thousands separator the current clause lacks; repr tests must expect it. Degenerate case (`total == 0`): the parenthetical is omitted, clause is `"0 edges, "`. The repr tests bake this format. `HivePlotMatrix.__repr__` (`hiveplot_matrix.py:2175`) untouched — matrix-level coverage is a deliberate non-goal (Amendment 2).

## Workstreams

### Workstream A: `edge_coverage` property and repr clause

**Status:** not started
**Files:** `src/hiveplotlib/hiveplot.py` (`EdgeCoverage` NamedTuple next to `BaseHivePlot`; property on `BaseHivePlot`; `HivePlot.__repr__` clause at :2528-2548), `src/hiveplotlib/__init__.py` (re-export `EdgeCoverage`), `src/hiveplotlib/edges.py` (private distinct-count helper, if placed there), `tests/hiveplot_test.py` (new coverage tests + repr assertion updates at :5620-5680), `tests/edges_test.py` (helper tests if helper lands on `Edges`), `CHANGELOG.rst`, `wiki/wiki/plans/scaling-large-networks.md` (read-site claim, one line).
**Done when:**

- Api-critic planning-mode pass on the API usage examples is recorded above **before** implementation dispatch.
- `edge_coverage` property exists on `BaseHivePlot` per the binding semantics above; `EdgeCoverage` defined in `hiveplot.py` and importable as `from hiveplotlib import EdgeCoverage`; `HivePlot` repr includes the percent-only coverage clause per the pinned spec; `HivePlotMatrix` and `P2CP` surfaces unchanged.
- Tests cover: multi-tag counting (e.g. `example_multi_tag_hive_plot()`), no-repeat vs repeat-axes coverage delta, distinctness (a row referenced by two cells counts once), degenerate no-edges case, coverage drop after `reset_edges`, preservation through `Edges.copy`, update after re-`connect_axes`, and the repr boundary rule (no false 100%/0%).
- `make test` green (100% coverage, warnings-as-errors), `make format`, `make ty` clean.
- CHANGELOG entry under the in-flight unreleased version (v0.28.0 if still WIP at landing; otherwise the next unreleased section) — new user-visible behavior, so the entry is required regardless of release cycle.
- Api-critic post-implementation review recorded above.

### Workstream B: "Hive Plot Gotchas" docs page

**Status:** not started
**Files:** `examples/hive_plot_gotchas.ipynb` (new), `docs/source/notebooks/index.rst` (add under "Hive Plots"), `docs/source/conf.py` (thumbnail entry, plus `_static/` image), cross-link touches in `examples/adding_repeat_axes.ipynb` and `examples/hive_plots_more_than_three_groups.ipynb` (link only; no prose rewrites).
**Done when:**

- New short tutorial-genre notebook covering at least: (1) intra-axis edges invisible without repeat axes (source: `wiki/wiki/analyses/karate-club-walkthrough.md`), (2) >3 axes means not all axis pairs adjacent, so not all edges visible at once (source: `wiki/wiki/concepts/hive-plot.md` "Why Three Axes?"; link out to `hive_plots_more_than_three_groups`), (3) overplotting — same-color edges drawn on top of each other hide density (link out to `hive_plots_for_large_networks` / datashader material), (4) parallel-edge collapse on networkx multigraph ingestion — edges merge last-write-wins and a warning fires by default (`_warn_if_parallel_edges_collapse`, `src/hiveplotlib/hiveplot.py:258`); entry states what happens, why, what the warning means, and the `graph_multigraph=True` workaround, linking to existing coverage rather than re-teaching (Amendment 4), (5) "I set an edge kwarg and it didn't take effect" — a higher-priority kwarg in the hierarchy (`all_edge_kwargs` → ... → `non_repeat_edge_kwargs`) overrode it and a conflict warning fired; entry names the hierarchy and links to `examples/edge_kwarg_hierarchy.ipynb` as the canonical reference (Amendment 4), (6) `graph_directed` inference — building from nodes/edges with `graph_directed` unset auto-infers directedness from the requested metrics (e.g. `in_degree` implies directed, `triangles` implies undirected), which can surprise users; deliberate overlap with the gated Workstream E blurb in `wiki/wiki/plans/graph-metric-conflict-validation.md`, see Amendment 4. Each of gotchas 4-6 is a few sentences plus links, no demo build-out. Planner note: notebook-author may add gotchas from the viz-quality-bar skill and examples corpus, but keep the page short — it is an index of pitfalls with links, not a re-teaching of each topic.
- Cross-references the WS-A `edge_coverage` property in the intra-axis section ("check coverage, notice undrawn edges, add a repeat axis"), so WS-A must land first or in the same MR.
- Uses built-in datasets already established in the corpus (`example_hive_plot` and/or `nx.karate_club_graph()`); no new datasets.
- Prose follows the writing-voice rules (no em-dashes, no AI filler, length discipline).
- Listed in `index.rst` and `conf.py` thumbnails; `make test-nb` passes for the new notebook; `make docs` builds with no new warnings.
- Editorial-critic notebook review and viz-critic figure pass recorded.

**Notebook-coherence audit:** net-new notebook; tutorial genre; documents `HivePlot` (plus the new property); datasets `example_hive_plot` / karate club, both established in the corpus. No genre drift or dataset additions to sign off.

## Branch / MR sequencing

Current branch `46-more-streamlined-networkx-usage-and-support` carries in-flight v0.28 work. This plan's work belongs on a **fresh branch off master after that MR merges, as a separate MR** (recommended in the originating discussion; nothing here depends on unmerged branch-46 internals except Example 3's `graph=` idiom, which will be on master by then). Both workstreams can ship in one MR, WS-A first.

## Plan amendments

### Amendment 1 (2026-06-10) — In-scope tweak: repr clause format pinned, percent-only

Gary chose the api-critic's recommended short form: `f"{coverage.total:,} edges ({pct}% drawn), "` (e.g. `"5,100 edges (82% drawn), "`), no n/N in the repr — the clause already carries the total and the `edge_coverage` property is the precision channel. Exact spec (integer percent, inward-rounded boundaries, thousands separator, degenerate `"0 edges, "` form) pinned in Design notes "Repr display rule"; Goal and "Patterns this replaces" updated to match. Resolves the api-critic planning pass's repr-discrepancy pin-before-dispatch item.

### Amendment 2 (2026-06-10) — In-scope tweak: `HivePlotMatrix` coverage recorded as deliberate non-goal

Not a deferral awaiting demand. Gary's rationale is conceptual, not merely "per-cell access already works": a matrix-level coverage aggregate doesn't make sense ("it doesn't make sense at the matrix level") — edges appear in multiple cells by design, so any matrix-level number is semantically muddled, not just redundant. Recorded under Out of scope; Alignment open decision closed; Design notes HPM sentence updated. `HivePlotMatrix.__repr__` stays untouched and the WS-A repr test surface is final.

### Amendment 3 (2026-06-10) — In-scope tweak: `EdgeCoverage` export location pinned

Per the api-critic recommendation, accepted by Gary: define `EdgeCoverage` in `src/hiveplotlib/hiveplot.py` next to `BaseHivePlot`; re-export from the `hiveplotlib` top-level `__init__` (which already re-exports from `hiveplotlib.hiveplot`). `from hiveplotlib import EdgeCoverage` is the supported import path for annotations and `isinstance` checks. Naming audit, WS-A Files, and WS-A done-when updated. Resolves the api-critic planning pass's export-location pin-before-dispatch item.

### Amendment 4 (2026-06-10) — In-scope tweak: WS-B gotchas page expanded from three to six entries

Gary approved (2026-06-10 docs-gaps discussion) adding three gotchas to WS-B's coverage list, keeping the page's character (an index of pitfalls with links, not a re-teaching of each topic; each new entry is a few sentences plus links):

4. **Parallel-edge collapse on graph ingestion.** Networkx multigraph ingestion collapses parallel edges last-write-wins; a warning fires by default (behavior from `wiki/wiki/plans/graph-metric-conflict-validation.md`, implemented at `src/hiveplotlib/hiveplot.py:258` `_warn_if_parallel_edges_collapse`). Entry covers what happens, why, what the warning means, and the `graph_multigraph=True` workaround.
5. **Edge-kwarg-hierarchy overrides.** A higher-priority kwarg in the hierarchy overrode the one the user set, with a conflict warning. Entry names the hierarchy and links to `examples/edge_kwarg_hierarchy.ipynb` (canonical reference per CLAUDE.md).
6. **`graph_directed` inference.** With `graph_directed` unset, directedness is auto-inferred from requested metrics (`in_degree` implies directed, `triangles` implies undirected). **Overlap-is-deliberate decision:** Gary explicitly decided this earns its own gotcha entry EVEN IF the gated Workstream E notebook blurb in `graph-metric-conflict-validation.md` (not yet reviewed) also covers it. A later session must not "deduplicate" this entry away against that blurb.

Provenance: these emerged from comparing the gotchas page scope against a proposed conceptual-FAQ docs page; the FAQ page was **rejected** and its surviving entries fold into this gotchas page instead. Gotchas 4 and 6 document v0.28 in-flight branch-46 behavior, consistent with the existing Branch / MR sequencing (work starts after branch 46 merges); both behaviors verified present on the branch (feasibility: prose-plus-links only, no new entry points, no new datasets). Goal sentence and WS-B done-when updated to match; notebook-coherence audit unchanged (same genre, same datasets, no demo build-out for the new entries).

### Amendment 5 (2026-07-06) — In-scope tweaks: adversary's six-item challenge disposed (grill skipped; this amendment is the disposition channel)

The grill was knowingly skipped for this run (Execution record), so the `### Adversary's challenge` items are disposed here by the orchestrator per the execution session's routing under the autonomous-run directive; the maintainer reads async. Workstream set unchanged: no additions, no removals, no deferrals. Body sections are deliberately not edited in this pass; where their text conflicts with this amendment, the amendment governs, and dispatch briefs quote from here.

1. **Cross-plan edit re-scoped (must-fix; WS-A).** The instruction in Design notes ("Update `wiki/wiki/plans/scaling-large-networks.md` WS-1's read-site claim in this workstream (one-line edit listing the helper as a consumer)") and the matching WS-A Files sub-item are superseded: the target text no longer exists. The scaling plan corrected its "only known read site" claim itself (in-scope tweak, 2026-07-03) and scaling WS-1 shipped on unmerged branch 53 / MR !36; verified in the main checkout, the corrected consumer map is the six-site lockstep enumeration at scaling WS-1 Files (the "All six read sites, moved in lockstep to positional takes" bullet, ~:1531; restated at its done-when ~:1538 and its 2026-07-03 in-scope-tweak entry). Re-scoped instruction, both halves required: (a) WS-A appends one line to that six-site enumeration (locate by grepping "All six read sites" at edit time, not by line number; wiki edits happen in the main checkout per the Execution record), verbatim modulo the helper's final name/location: "Seventh consumer, added on master after WS-1 shipped (added by `edge-coverage-and-gotchas-docs.md` WS-A, 2026-07-06): `<helper>` (`src/hiveplotlib/<file>.py`) normalizes `relevant_edges` cell values for `BaseHivePlot.edge_coverage`; at the !36 lockstep reconcile, swap its boolean-mask branch to the index-array branch (`len(reduce(np.union1d, arrays))`). It counts without indexing a DataFrame, so WS-1's positional-take rule does not apply to it." The appended line is a post-ship addendum and must not read as a retroactive member of the shipped, grep-verified lockstep six. (b) WS-A's Implementation-log entry in this plan records the seam and this cross-plan pointer (maintainer kickoff requirement, Execution record). Both halves, not either-or.

2. **Stale wiki-state pointers annotated; gotcha 6 link target pinned (worth-discussing; WS-B).** History sections stay as written; corrections live here. As of execution, `wiki/wiki/adr/` exists: ADR 0001 (networkx-integration) is the durable record for the gotcha-4/6 behaviors (both shipped in v0.28), and ADR 0002 also exists. `graph-metric-conflict-validation.md` and `i-want-to-plan-optimized-hoare.md` now resolve under `wiki/wiki/plans/archived/`; read this plan's citations to them there. Amendment 4's "gated Workstream E blurb ... (not yet reviewed)" clause is moot: that plan fully shipped, and the `graph_directed`-inference coverage lives in `examples/computing_graph_metrics.ipynb` (verified: the only example matching `graph_directed` on the baseline). Pinned for WS-B: gotcha 6's link-out target is `computing_graph_metrics`. Amendment 4's no-deduplication decision stands unchanged, now reading against that shipped notebook coverage instead of a "gated blurb": gotcha 6 keeps its own entry even though the notebook also covers the behavior.

3. **llms.txt decision pinned (worth-discussing; WS-B adds an entry, WS-A deliberately does not).** The curated-index convention postdates this plan; pinning now so the notebook-author doesn't improvise. WS-B Files and done-when gain `docs/source/_llms/llms.txt`: the gotchas page is a conceptual "why does my plot look wrong" entry point, not a feature demo, so its entry pins high, landing under `## Getting started` after the three existing links, matching their one-line format with the absolute URL `https://hiveplotlib.readthedocs.io/stable/notebooks/hive_plot_gotchas.html` (notebook-author owns the entry per the CLAUDE.md trip-wire; the summary line is the author's to draft, editorial-critic reviews it). WS-A makes no llms.txt change: the index links pages and WS-A adds none; `edge_coverage` / `EdgeCoverage` ride the existing blanket API Reference entry, and WS-B's entry (whose intra-axis section demonstrates the property) is the index-level discovery pointer. `llms-full.txt` is out of scope for both workstreams (owned by the standing weekly refresh, not per-plan edits).

4. **Repr-reach over-claim consciously accepted (worth-discussing; no scope change).** The Goal sentence stands, read as: the repr clause is the point-of-use signal for users who inspect the object; the `edge_coverage` property and the gotchas page are the discovery channels for everyone else. No design change (the property justifies the feature alone; Amendment 1 pinned format, not channel reach). Within WS-B's existing intra-axis done-when bullet, the demo includes one `print(hp)` echo showing the coverage clause in the check-coverage flow: the adversary's "one echoed `hp` where docs readers see it," satisfied at zero added scope on exactly the page a confused user reaches.

5. **mpl-3.11/cartopy freeze constraints pinned on WS-B's `hive_plots_more_than_three_groups.ipynb` touch (worth-discussing).** The cross-link edit is markdown-cell-only with no re-execution of that notebook; the committed diff on that file must show only the added/edited markdown cell (any output, `execution_count`, or metadata churn elsewhere in the file halts to the maintainer, honoring the standing keep-uncommitted guidance on that notebook's outputs). The WS-B notebook gate stays scoped to the new notebook as written; before interpreting any notebook-run result that includes `hive_plots_more_than_three_groups`, qa records the installed matplotlib version. Under mpl 3.11 a failure there is the known freeze, not a WS-B regression, and a green run is equally non-evidence (a prior 3.11.0 pass was a false signal from a since-reverted change). Anything beyond the markdown cross-link routes back through amend-plan.

6. **Seam write-site docstring fix folded into WS-A (low-confidence; orchestrator's call as routed).** `__store_edge_ids`'s docstring names a nonexistent attribute, `edges.relevant_indices[...]`, at `src/hiveplotlib/hiveplot.py:906` on the baseline; the attribute is `relevant_edges`. WS-A fixes the one word in passing: it already touches `hiveplot.py` and documents this exact neighborhood as the seam's write site, and a stale name in the seam's own doc trail undercuts the one-spot-change story the !36 reconcile leans on. No CHANGELOG entry (private-method docstring fix). If the !36 reconcile later conflicts on this docstring (branch 53 also rewrites `__store_edge_ids`), keep whichever side names `relevant_edges` correctly.

**Touched specs, summarized:** WS-A Files (scaling-plan sub-item re-scoped per item 1; the `hiveplot.py` touch gains the item-6 word fix); Design notes seam bullet, final sentence (superseded per item 1); WS-B Files + done-when (llms.txt entry per item 3; gotcha-6 link target per item 2; `print(hp)` echo inside the existing cross-reference bullet per item 4; `hive_plots_more_than_three_groups` edit constraints per item 5). Nothing removed, nothing deferred, workstream set unchanged.

### Amendment 6 (2026-07-06) — In-scope tweaks + record-only dispositions: WS-A post-impl findings disposed (run halted on the must-fix; this is the maintainer's async review package)

Consolidated disposition of the api-critic post-impl review and the adversary post-impl attack (both recorded above; they converge on one must-fix). Workstream set unchanged: every item folds into WS-A's existing scope, pins a WS-B constraint, or is record-only. Nothing below executes until the maintainer green-lights the fix dispatch, which quotes from here. No new CHANGELOG entries anywhere in this amendment: items 1 and 2 complete the same unreleased v0.29.0 feature whose entry already exists.

1. **`EdgeCoverage` docs home added to WS-A (must-fix, both critics — In-scope tweak).** Root cause was plan inventory (WS-A Files never listed a docs rst), not implementer deviation. WS-A Files gains `docs/source/autodoc/hive_plots/low_level_hive_plot_api.rst`; WS-A done-when gains the render check below. Pinned placement, verified against the worktree page (exactly three directives — `Node`, `Axis`, `BaseHivePlot` — each bare `:members:`, four-space indent): append as the page's new final entry, directly after the `BaseHivePlot` block:

   ```rst
   .. autoclass:: hiveplotlib.EdgeCoverage
       :members:
   ```

   Placement rationale (the api-critic's placement fork, decided here as implementation topology): low-level adjacency over high-level visibility. `edge_coverage` renders only under `BaseHivePlot` on the low-level page (`autoclass:: hiveplotlib.HivePlot` lacks `:inherited-members:`, a pre-existing convention this plan leaves alone per the critic), and both shipped xrefs resolve to names this one directive homes there: the property `:return:`'s `~hiveplotlib.EdgeCoverage` (`hiveplot.py:1857`) and the changelog's `hiveplotlib.BaseHivePlot.edge_coverage` + `hiveplotlib.EdgeCoverage` (`CHANGELOG.rst:21-23`). One line resolves every dangle onto the one page where the property renders; high-level placement would split class from property and interleave a return-record between the construction entry points (`NodeCollection`, `Edges`, `HivePlot`). The `hiveplotlib.EdgeCoverage` path matches the page's re-export convention and Amendment 3's supported import path. `:members:` without `:inherited-members:` keeps inherited tuple methods (`count`, `index`) off the page.

   Render check (`nitpicky` stays unset — repo-wide xref triage is out of WS-A scope, so the check is targeted, not a build gate): after `make docs`, the built low-level page contains the `EdgeCoverage` class entry with the three field docstrings, and the built changelog's `edge_coverage` / `EdgeCoverage` mentions are hrefs into `autodoc/hive_plots/low_level_hive_plot_api.html` (grep the built HTML), not plain literals.

   **WS-B bearing, pinned now:** the gotchas page's cross-reference to the property must resolve under the same convention — corpus-precedent relative form (`computing_graph_metrics.ipynb` links via `../autodoc/hive_plots/graph_features.rst#<anchor>`), targeting `../autodoc/hive_plots/low_level_hive_plot_api.rst#hiveplotlib.BaseHivePlot.edge_coverage`. Never a `HivePlot.edge_coverage` anchor and never the high-level page (that anchor does not exist under the convention). An `EdgeCoverage` class link, if used, targets `#hiveplotlib.EdgeCoverage` on the same page.

2. **Stale stored repr outputs swept: two-notebook re-execution folded into WS-A (worth-discussing, adversary — In-scope tweak).** Decision: sweep, not knowing acceptance. The repr clause is WS-A's replaced pattern and these stored outputs are call sites; replace-and-sweep moves them in the same MR, and docs build from stored outputs, so the site would otherwise show the old clause beside a changelog announcing the new one. WS-A Files gains `examples/hpm_from_partition.ipynb` and `examples/hpm_generic.ipynb` (re-execution only; zero source-cell edits). Sweep capped at exactly these two — no repo-wide re-execution; orchestrator re-grepped the worktree's `examples/` for the old-form clause (`\d+ edges, partition=`) and confirmed these are the only carriers, at exactly the raw lines the adversary cited (434, 501, 567); neither is the mpl-3.11-frozen notebook. Check: both execute cleanly in place in the worktree venv; post-sweep, raw grep of the two files shows the child-print outputs carrying the `(N% drawn)` parenthetical and zero old-form hits; the diff on both files is outputs/execution-count churn only (expected notebook churn in review).

3. **Seventh-consumer bullet completed — cross-plan edit applied by the orchestrator in this pass (worth-discussing, adversary — In-scope tweak, already executed).** The fork between doing it now and pinning it for the fix dispatch is the orchestrator's; decided: done now. It is a plan-file edit in the main checkout (orchestrator surface per the Execution record), and leaving it open invites a fix agent improvising into an active plan — the failure mode Amendment 5 item 1 existed to prevent. The scaling plan's seventh-consumer bullet (`wiki/wiki/plans/scaling-large-networks.md`, scaling WS-1 Files) now ends: "Its two direct `test_drawn_count_*` tests (`tests/edges_test.py`) plant boolean masks into cells and swap to planted index arrays with the helper body; a miss there is loud, not silent (`np.union1d` over planted bools fails both)." Record completion only (the adversary confirmed the miss would be loud); no WS-A artifact change, nothing left for the fix dispatch.

4. **Record-only dispositions (each remaining finding met a decision; no action).**
   - Api-critic [low-confidence], manual-wiring docstring honesty: verdict accepted — the property docstring is honest for every documented path per the plan's automated-path semantics; the optional hedge clause is not added.
   - Adversary [low-confidence], `Edges.copy()` "index arrays" wording (`edges.py:245`) contradicting `_drawn_count`'s "boolean masks" one method below: deliberately no WS-A action even though WS-A reopens for item 1. The wording becomes true at the !36 reconcile (cells then store index arrays), and the scaling plan already tracks that exact docstring as a verify-not-break site (scaling WS-1 Files, `edges.py:244-256`); flipping it on this branch would manufacture a fresh reconcile conflict. Master knowingly carries the baseline contradiction until !36 lands.
   - Adversary [low-confidence], saturated `test_edge_coverage_multi_tag_fixture` (30/30, fraction 1.0): fully mitigated by the non-saturated siblings (2/3 cross-tag at `tests/hiveplot_test.py:5802`, 86/100 two-cells at `:5812`); no test change.

**Touched specs, summarized:** WS-A Files (+ `docs/source/autodoc/hive_plots/low_level_hive_plot_api.rst` per item 1; + `examples/hpm_from_partition.ipynb` and `examples/hpm_generic.ipynb`, re-execution only, per item 2); WS-A done-when (+ item 1's render check; + item 2's sweep check; both ride the Execution record's existing per-workstream docs gate); WS-B constraints (+ item 1's link-anchor pin, joining Amendment 5's WS-B pins). Workstream set unchanged; nothing removed, nothing deferred.

## Holdouts

- `src/hiveplotlib/datasets/toy_hive_plots.py:381-385` (`example_hive_plot` docstring note about fewer edges visualized without repeat axes): kept as-is — it documents the dataset generator's behavior, not a re-explanation the gotchas page replaces. Optionally add a cross-link in WS-B, not required.

## Implementation log

- 2026-07-06: Workstream A complete (code; tests are the follow-on dispatch). Added `EdgeCoverage` NamedTuple in `hiveplot.py` (re-exported from the `hiveplotlib` top level), the `BaseHivePlot.edge_coverage` property per the binding Design notes (per-tag distinct counts summed over tags; `len(self.edges)` denominator; no caching; degenerate `EdgeCoverage(0, 0, nan)`), and the pinned percent-only `HivePlot.__repr__` clause (`{total:,} edges ({pct}% drawn), `, inward-clamped boundaries, degenerate `"0 edges, "`); `HivePlotMatrix` / `P2CP` untouched. Seam per the Execution-record constraint: implemented against master's boolean masks, with all counting of `relevant_edges` cell values routed through the single private helper `Edges._drawn_count()` (`src/hiveplotlib/edges.py`), so the !36 reconciliation is a one-spot change (swap that helper's mask-union body to the index-array branch, `len(reduce(np.union1d, arrays))`); the seventh-consumer line was appended to `scaling-large-networks.md` WS-1's read-site enumeration per Amendment 5 item 1. Amendment 5 item 6's `relevant_indices` → `relevant_edges` docstring fix landed in `__store_edge_ids` (no CHANGELOG). CHANGELOG entry under v0.29.0 Added. Smoke-verified (property, repr incl. both clamps and thousands separator, multi-tag, degenerate); the three format-baked repr assertions in `tests/hiveplot_test.py` fail as expected until test-engineer updates them.
- 2026-07-06: Workstream A remediation (Amendment 6 docs fixes). Item 1: appended `.. autoclass:: hiveplotlib.EdgeCoverage` / `:members:` as the new final entry in `docs/source/autodoc/hive_plots/low_level_hive_plot_api.rst`, directly after `BaseHivePlot`, mirroring the page's bare-`:members:` / four-space style. After `make docs` (build succeeded, no new warnings): the built low-level page renders the `EdgeCoverage` class entry with all three field anchors and docstrings (`hiveplotlib.EdgeCoverage.{drawn,total,fraction}`), and the built changelog's `EdgeCoverage` and `BaseHivePlot.edge_coverage` mentions are `reference internal` hrefs into `autodoc/hive_plots/low_level_hive_plot_api.html` (zero dangling literals). Item 2: re-executed `examples/hpm_from_partition.ipynb` and `examples/hpm_generic.ipynb` in place with the worktree venv (`kernel_name=python3`, which resolves to the worktree `.venv`, not the user-level `hiveplotlib` kernelspec pointing elsewhere); both ran clean end-to-end. nbformat cell-source diff confirms zero source-cell changes and unchanged cell counts (71/63); the diff is outputs + execution-count churn only. Raw grep: the `(N% drawn)` clause now present in the child-repr outputs (`86 edges (6% drawn)` / `(63% drawn)` / `(66% drawn)` at raw lines 434/501/567) and zero old-form `\d+ edges, partition=` hits across `examples/`. Item 3 (seventh-consumer bullet on `scaling-large-networks.md`) was applied by the orchestrator in the Amendment 6 pass; no WS-A artifact change. rst stayed LF (no CRLF flip); worktree diff is exactly the three intended files plus the pre-existing WS-A source/test/CHANGELOG set. Full-suite gate battery remains with qa per the Execution record.
- 2026-07-06: Workstream A tests complete. `tests/hiveplot_test.py`: the three format-baked repr assertions updated to the pinned clause (`{len(hp.edges):,} edges (<pct>% drawn), ` — uniform-sort 76%, mixed-sort 76%, repeat-`["A"]` 86%; every baked value empirically probed in the worktree venv before asserting); new module helper `six_node_three_axis_hive_plot` (nodes 0-1/2-3/4-5 on axes A/B/C so intra- vs inter-axis rows are exact); new tests `test_repr_no_edges` (degenerate `"0 edges, "`, no parenthetical), `test_repr_edge_coverage_percent_rounds_inward_at_boundaries` (4 parametrized clamp cases: 999/1,000→99, 10/10→100, 1/1,000→1, 0/10→0; also pins the thousands separator), `test_edge_coverage_no_repeat_vs_repeat_axes` (76/100/0.76 vs 100/100/1.0), `test_edge_coverage_multi_tag_fixture` (30/30 across two tags), `test_edge_coverage_edge_under_two_tags_counts_once_per_tag` (surgical cross-tag duplicate → 2/3), `test_edge_coverage_counts_rows_referenced_by_two_cells_once` (manually wiring the reverse repeat-pair cell re-references the same 10 intra-A rows; count stays 86, with a positive mask-sum assertion so a silently-dropped cell can't pass vacuously), `test_edge_coverage_drops_after_reset_edges` (76→61 partial→0 full; total fixed at 100), `test_edge_coverage_updates_after_reconnect_axes` (0→15 via `connect_axes`), `test_edge_coverage_preserved_through_edges_copy`, `test_edge_coverage_no_edges` (zero-row `Edges` → 0/0/nan), `test_edge_coverage_named_tuple_import_and_unpacking` (in-body `from hiveplotlib import EdgeCoverage`, isinstance, field-order pin), plus `TestBaseHivePlot.test_edge_coverage_edges_unset` for the `edges is None` branch. `tests/edges_test.py`: `test_drawn_count_counts_rows_referenced_by_two_cells_once` (mask union → 3, not the per-cell sum 4) and `test_drawn_count_zero_when_no_cell_references_tag` (empty `relevant_edges`, and cells existing only for another tag). Disposition on the "Patterns this replaces" `:5675-5676` item: `test_repr_collapsed_axis` left unchanged by design — its substring assertions pin partition-name rendering and are format-agnostic for the edges clause, which the new parametrized repr tests pin exhaustively; extending it would couple an unrelated contract to the clause format. Scoped verification: 355 passed (`hiveplot_test.py` + `edges_test.py`, `-n 7`, warnings-as-errors); coverage on the scoped run: `edges.py` and `__init__.py` at 100% and no new `hiveplot.py` line (NamedTuple, property, repr clause) in the missing set; ruff format/check clean; no CRLF flips. Full-suite gate battery (`make test`, `make ty`, docs) remains with qa per the Execution record.
- 2026-07-06: Workstream B complete. New `examples/hive_plot_gotchas.ipynb` (H3 title "Hive Plot Gotchas", short pitfalls-index genre): gotcha 1 (intra-axis edges without repeat axes) is the one demo — builds `example_hive_plot()`, shows `hp.edge_coverage` = `EdgeCoverage(drawn=76, total=100, fraction=0.76)`, echoes `print(hp)` for the `(76% drawn)` repr clause (Amendment 5 item 4), then `repeat_axes=True` → 100% with a two-panel before/after figure; links to `adding_repeat_axes` and the property via the corpus-precedent relative anchor `../autodoc/hive_plots/low_level_hive_plot_api.rst#hiveplotlib.BaseHivePlot.edge_coverage` (Amendment 6 item 1). Gotchas 2-6 are prose+links only (no demo): 2 → `hive_plots_more_than_three_groups#the-problem-with-a-6-axis-hive-plot`, 3 → `hive_plots_for_large_networks#visualization-challenges-in-pure-matplotlib`, 4 (parallel-edge collapse, `graph_multigraph=True` workaround) → `computing_graph_metrics#parallel-edges-and-the-collapse-warning`, 5 (edge-kwarg hierarchy) → `edge_kwarg_hierarchy#overlapping-edge-kwargs-and-the-edge-kwarg-hierarchy`, 6 (`graph_directed` inference, deliberate overlap kept) → `computing_graph_metrics#controlling-the-hiveplot-internal-graph-type` (Amendment 5 item 2). Real seeded values used throughout (drawn=76, not the planning-era 82). Registered in `docs/source/notebooks/index.rst` (Hive Plots section, both `nblinkgallery` + `toctree`) and `docs/source/conf.py` thumbnails (`_static/hive_plot_gotchas.png`, generated from a text-stripped before/after render). One llms.txt entry added under `## Getting started` after the three existing links (Amendment 5 item 3). Cross-link pointers added markdown-only to `adding_repeat_axes.ipynb` (+2 lines) and `hive_plots_more_than_three_groups.ipynb` (+3/-1, zero execution/output churn — the mpl-3.11/cartopy freeze notebook not re-executed, Amendment 5 item 5). Executed with the worktree `python3` kernel; official notebook test green (`make test-nb` scoped, warnings-as-errors); `make docs` builds with no new warnings (only the pre-existing benign nbsphinx CSS copy-overwrite), all cross-links including the autodoc anchor resolve in the built HTML. Caught and fixed one real bug in the property link (a backticked markdown link label breaks nbsphinx link parsing, leaving a literal code span + bare URL; plain-text label resolves). No CHANGELOG entry (docs page, per the file's convention). No CRLF flips; worktree diff is exactly the new notebook + `_static` thumbnail + index.rst + conf.py + llms.txt + the two markdown-only cross-linked notebooks.
