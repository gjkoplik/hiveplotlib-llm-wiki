# Plan: performance regression + numeric-equivalence gate

<!--
Working plan, tracked in the wiki submodule. Concise per mental-model rule 17.
This is the keystone gate for scaling-large-networks.md; it must land first.
-->

## Goal

Give hiveplotlib an in-repo gate that makes every downstream scaling/perf change assertable as "measurably faster (or no worse) **and** output-equivalent to the proven path, with no regression on small graphs." Concretely: a parametric synthetic-graph generator, a deterministic numeric-equivalence check (curve arrays + rasterized output match the proven single-shot datashader path within tolerance), and a hybrid regression layer (2026-06-11 amendment): **pytest gates assert relative, same-run ratios** (e.g. streamed peak RSS as a fraction of single-shot peak RSS, sampled from a subprocess), while **ASV (airspeed velocity) owns timing/memory history**, the rolling baseline, and the capture-at-merge artifact store. Absolute wall-time numbers are never CI pass/fail criteria. This is the wall that protects against silent reward-hacking when `scaling-large-networks.md` Workstream 1 swaps single-shot curve concatenation for per-chunk streaming + raster aggregation (including the `ds.var`/`ds.std` streaming case). Nothing downstream can claim "faster, no regression" until this ships, so it is sequenced first.

## Prior ADRs / design docs

None — net-new design space. No `wiki/wiki/adr/` directory exists; ADR promotion has never been run. No existing perf/benchmark/regression-gating wiki page; this is net-new. Relevant concrete artifacts:

- `wiki/wiki/concepts/bezier-curves.md` — Bézier kernel, numba mode switch, and float32 curve storage; the equivalence gate compares this kernel's output across paths.
- `runners/performance/profile_hiveplot_ds.py` + `runners/performance/profiling_utils.py` — the existing in-repo profiling runner this harness extends. Establishes the "tooling in-repo, artifacts in `data/profiling/`" precedent.
- `wiki/wiki/sources/perez-2021-hype.md` — prior-tool scale comparator (weakly relevant; useful baseline framing only).
- `wiki/wiki/plans/scaling-large-networks.md` — the downstream super-plan this gate guards. Its Workstream 1 (streaming curve aggregation at `src/hiveplotlib/viz/datashader.py:181-234, 386-388`) is the first consumer of the equivalence check.

## Patterns this replaces

This **extends** `runners/performance/`; it does not supersede the existing runner. The existing single-shot datashader path stays as the regression **baseline**.

- `runners/performance/profiling_utils.py:95-100` (`time_call`) returns a single elapsed time. `time_call` stays for the existing runner's per-call use. Timing **history** is owned by ASV `time_` benchmarks (2026-06-11 amendment); the previously planned hand-rolled `time_median_of_n` helper is dropped, not replaced in-repo.
- No existing memory measurement exists in `runners/performance/`. Net-new addition (RSS-from-subprocess sampler).
- No existing equivalence check exists anywhere in `tests/` or `runners/`. Net-new addition.
- No `tests/`-level perf or equivalence assertions exist today. Net-new test module(s).

Nothing is deleted. QA Engineer's post-execution grep should find the existing `time_call` and the existing runner intact.

## Default justifications

- **Peak RSS: the spawned child measures and reports its OWN stdlib `resource.getrusage(RUSAGE_SELF).ru_maxrss`, launched via a two-level subprocess (primary; psutil / `/usr/bin/time -v` are cross-platform fallback only), NOT `tracemalloc`**: `tracemalloc` only tracks Python-level allocations and under-counts numba- and numpy-level C allocations, which dominate at scale. A streaming refactor could "optimize" against a false tracemalloc number while real RSS holds flat or grows. Measuring peak RSS of a child process that runs the workload end-to-end is the only number that can't be gamed by moving allocations into C. `ru_maxrss` is the exact kernel high-water mark (no polling/sampling miss, no new dependency), and Linux/WSL is both the canonical host and CI, so this is the standing primary path. The 2026-06-26 grill confirmed the **intent** (stdlib `getrusage`, exact high-water mark, no psutil on the single-process path); implementation corrected the **mechanism** to child-reported `RUSAGE_SELF` (2026-06-27 amendment): parent-side `getrusage(RUSAGE_CHILDREN)` is a cumulative max across ALL reaped children of the parent's lifetime, so sequential measurements would be poisoned by the largest prior one, and the two-level subprocess (a near-empty launcher spawns the measured interpreter) resets the Linux `ru_maxrss` floor a heavy parent (~200 MB under pytest) would otherwise impose on directly spawned children. psutil polling and `time -v` are the documented fallback for non-Linux hosts. This is a deliberate design decision, recorded here so it is not relitigated.
- **Repeated-sample timing with fixed seeds, via ASV's built-in repeat/statistics machinery** (supersedes the hand-rolled median-of-N helper, 2026-06-11): the original rationale stands (a single sample is dominated by JIT warmup, GC, and scheduling jitter; a robust statistic over N runs is needed), but ASV `time_` benchmarks already implement it, so no `time_median_of_n` ships.
- **CI gates are relative, same-run ratios in pytest, NOT absolute timings vs. a committed baseline** (2026-06-11): absolute wall-time baselines are unstable on shared GitLab CI runners; ratios measured within one run on one machine (e.g. streamed peak RSS / single-shot peak RSS, streamed vs. single-shot timing at small scale) are self-normalizing across machines and more reward-hack-resistant. Absolute numbers are reported by ASV for history, never asserted in CI.
- **ASV-as-CI-gate considered and rejected** (2026-06-11): `asv continuous` must build and benchmark two commits per MR — too heavy for per-MR gating. ASV owns history/reporting only; pytest owns the gate.
- **Fully-hand-rolled rolling-baseline store considered and rejected** (2026-06-11): reinvents ASV's commit-indexed history tracking. ASV's committed results JSON is the durable per-scenario store (plain JSON, plottable without ASV's HTML dashboards) and the static data source for the scaling plan's Workstream 6 blog figures. The store is ASV's **native results format committed as-is**; no bespoke schema (2026-06-11 follow-up).
- **Retroactive re-benchmarking as the foundation considered and rejected** (2026-06-11): ASV old-commit re-runs cannot replace capture-at-merge because of benchmark-code API drift across the chain and environment non-reproducibility (old commit rebuilt under re-run-time deps). Recovery-only; details in the 2026-06-11 follow-up amendment.
- **Equivalence tolerance: per-array `rtol`/`atol` chosen per the compared dtype, defaulting to float32-appropriate tolerances** (e.g. `rtol=1e-5, atol=1e-6` for float32 curve arrays; exact or near-exact integer-count match for rasters). The proven path stores curves as float32 (`src/hiveplotlib/hiveplot.py:1176-1207`), so a float64-tight tolerance would false-fail; a too-loose tolerance would let a real numeric bug through. Tolerances are scale-independent (a few thousand edges proves the algebra), so fixtures stay small.
- **Equivalence fixtures stay small (low thousands of edges), in-repo; sweep-scale artifacts stay out of git**: equivalence is scale-independent, so the gate proves correctness on small in-repo fixtures that run in CI in seconds. Large sweep CSVs/profiles/plots are gitignored under `data/` (already covered by `.gitignore:17`).

## Naming audit

This work adds developer/CI-facing test and harness entry points, not user-facing library API. Names are checked against pytest/benchmark-tooling vocabulary, not the hive-plot user vocabulary.

- New test functions: `test_equivalence_*`, `test_no_regression_*` (pytest convention; `test_<thing>_<scenario>` per CLAUDE.md test-name=body contract). OK.
- New harness helpers (in `runners/performance/`): `measure_peak_rss` (matches the "peak RSS" / "maximum resident set size" vocabulary shared by `getrusage`'s `ru_maxrss`, psutil, and `time -v`; primary impl is stdlib `getrusage`, 2026-06-26 grill), `assert_curves_equivalent` / `assert_raster_equivalent` (pytest `assert_*` convention). OK — these mirror `pytest-benchmark` and `numpy.testing.assert_allclose` naming users of perf tooling already know. (`time_median_of_n` dropped per the 2026-06-11 ASV amendment.) ASV benchmark functions follow ASV's own `time_*` / `peakmem_*` naming convention.
- New synthetic-generator wrapper: parametrized call into the existing `example_hive_plot`; no new public dataset name. The generator is `example_hive_plot` already; this plan only parametrizes its existing `num_nodes`/`num_edges`/`partition_data_column`/`sorting_variables`/`seed`/`repeat_axes` kwargs. No new name.
- Pytest marker for the slower regression assertions: reuse-or-add. If a perf marker doesn't exist, add `@pytest.mark.performance` (registered in `pyproject.toml` / `conftest.py`). Vocabulary: "performance" matches the directory name and CLAUDE.md's optional-marker convention. OK.
- Pytest marker for harness-validation tests: `@pytest.mark.perf_harness` (2026-07-03 amendment). Bare `harness` rejected as ambiguous (test harness / agent harness / performance harness); `perf_harness` matches this plan's and `runners/performance/README.md`'s vocabulary. OK.

Internal module names are out of scope.

## API usage examples

No user-facing API change. This plan adds CI/dev tooling and test fixtures only; no library class, method, or function signature changes. Skipping the three API-usage subsections per the template.

## Notebook review

No notebook change. This plan touches `runners/performance/`, `tests/`, and config only.

## Alignment (grill)

### Maintainer shared-understanding pass (grill), Wave 1 — scenario sizing, peak-RSS measurement, Workstream A self-proof (2026-06-26)

Confirmed positions from the maintainer shared-understanding pass. These pin previously open calls; the corresponding workstream edits land in the 2026-06-26 amendments below.

- **Benchmark scenario sizes confirmed** (concrete pin for the shrunk capture-at-merge checkpoint): tiny ≈ 34 nodes / 78 edges (karate-club scale), small ≈ 2,000 nodes / 5,000 edges, medium ≈ 25,000 nodes / 250,000 edges, large ≈ 100,000 nodes / 2,000,000 edges. 10M+ is an occasional manual stress run, **not** a standing capture-at-merge scenario (too slow to re-run every merge for marginal signal).
- **Tiny floor scenario added** (maintainer concern): small=2,000 won't catch a fixed-overhead regression at karate-club scale, which is exactly where an accidental "always stream" or new per-call setup cost bites hardest. The tiny scenario is a **time-only guard** (`time_` only); peak RSS at that size is dominated by interpreter/imports, so a `peakmem_` ratio there is noise. The streaming-vs-single-shot ratio guard in Workstream C sits at this tiny floor (or includes it).
- **Peak-RSS measurement approach confirmed** (maintainer delegated the call): stdlib `resource.getrusage(RUSAGE_CHILDREN).ru_maxrss` measured around a spawned child is the **primary** source (exact kernel high-water mark, no polling/sampling miss, no new dependency). psutil and `/usr/bin/time -v` become the documented cross-platform **fallback**. The `tracemalloc`-rejection rationale is unchanged. *[Mechanism corrected in implementation: the child measures and reports its OWN `getrusage(RUSAGE_SELF).ru_maxrss` via a two-level subprocess; the grill-confirmed intent (stdlib getrusage, exact kernel high-water mark, no psutil on the single-process path) stands. See the 2026-06-27 amendment.]*
- **Workstream A self-proof confirmed:** A ships now proven solely by the identity-passes / perturbation-fails soundness test (no real alternate path exists until scaling WS2). The curve and raster comparison helpers expose a dtype-aware tolerance plus a raster-mismatch knob (not hardcoded exact) so WS2 can dial them later.

## Workstreams

Sequenced so the equivalence wall (A) and the measurement primitives (B) land before the CI assertions (C) that depend on them. A and B are independent and may run concurrently; C depends on both. The whole gate must complete before `scaling-large-networks.md` Workstream 1 begins.

### Workstream A: deterministic numeric-equivalence checker

**Status:** complete (2026-06-26; see Implementation log)
**Files:** `runners/performance/equivalence.py` (new), `tests/performance_equivalence_test.py` (new), small in-repo fixtures (generated at test time from `example_hive_plot`, no committed data files).
**Done when:**
- A helper compares two curve-construction outputs (the proven single-shot `HivePlot.construct_curves` path vs. an alternate path passed as a callable) and asserts elementwise closeness with dtype-aware tolerance. It compares **like-for-like dtypes**: `bezier_xy_with_nans` defaults to float64 (`src/hiveplotlib/utils.py:256`) but every internal call site passes float32 explicitly, so the checker coerces both operands to the proven path's stored dtype (float32, `src/hiveplotlib/hiveplot.py:1176-1207`) before comparing, and treats NaN-separator rows as positionally-matched NaNs (not as inequality).
- A helper rasterizes both paths through the datashader backend (`datashade_hive_plot_mpl`, `src/hiveplotlib/viz/datashader.py:445`) at a fixed canvas size and asserts the aggregated rasters match (exact or near-exact integer-count agreement for the line/point aggregations).
- Both helpers run against a small fixture (low thousands of edges) built from `example_hive_plot(num_nodes=..., num_edges=..., partition_data_column="low", labels=["A","B","C"], cutoffs=3, sorting_variables="low", seed=<fixed>, repeat_axes=True)`.
- A passing baseline test proves the checker is sound by comparing the single-shot path to itself (identity case must pass) and a deliberately-perturbed path (must fail), so the gate can't silently always-pass.
- `equivalence.py` exercised by tests (the 100% coverage gate applies to `src/hiveplotlib` only, per the 2026-06-11 coverage-scope ruling); warnings-as-errors clean; `make ty` clean.

### Workstream B: measurement primitive (subprocess peak RSS)

Shrunk by the 2026-06-11 ASV amendment: timing and memory **history** helpers are superseded by ASV's built-in `time_` / `peakmem_` benchmarks. This workstream now delivers only the peak-RSS helper the pytest ratio gates need, measured via stdlib `getrusage` as the primary path (2026-06-26 grill; psutil optional fallback only).

**Status:** complete, including tier 2 (tier 1 2026-06-26, tier 2 2026-06-27; see Implementation log)
**Files:** `runners/performance/profiling_utils.py` (extend), `tests/performance_measurement_test.py` (new).
**Done when:**
- A `measure_peak_rss(fn) -> int` helper (takes a top-level importable zero-arg function) runs the workload in a **spawned child** that measures and reports **its OWN stdlib `getrusage(RUSAGE_SELF).ru_maxrss` as the primary path** (2026-06-26 grill confirmed the intent; mechanism corrected in implementation, 2026-06-27 amendment): an exact kernel high-water mark, no polling/sampling miss, no new dependency. Launched via a **two-level subprocess** (near-empty launcher spawns the measured interpreter) so sequential measurements stay independent (parent-side `RUSAGE_CHILDREN` is a cumulative max across all reaped children) and the measured child dodges the Linux `ru_maxrss` floor a heavy parent (~200 MB under pytest) would impose. Linux/WSL is both the canonical host and CI, so this is the standing measurement. psutil (`Process.memory_info().rss` polled) and `/usr/bin/time -v` "Maximum resident set size" are the documented **cross-platform fallback only**. Docstring states explicitly why `tracemalloc` is rejected (under-counts numba/numpy C allocations) — this rationale stands verbatim regardless of the ASV restructure.
- No `time_median_of_n` (or any other timing-history helper) is added; ASV owns timing/memory history (2026-06-11 amendment).
- Tests assert: peak RSS of a child that allocates a known large array is meaningfully above a baseline child (sanity bound, not an exact byte assertion).
- psutil is **not** a required core dependency. The primary path (`getrusage`) is stdlib; the fallback is handled so the suite never errors on psutil's / `time -v`'s absence (skip or degrade with a clear marker, per CLAUDE.md optional-backend convention). *[Test-guard half superseded 2026-07-03: the measurement tests' psutil skipifs are removed by the skipif-removal ruling — in the `perf_harness`/`performance` lane a missing dep is a broken `.[perf,testing]` install and must ImportError loudly. The helper-side contract stands: tier-1 stays stdlib-only, and `measure_peak_rss_tree`'s RuntimeError naming the `perf` extra is a runtime contract, not a test guard.]*
- New helper exercised by tests (100% coverage gate applies to `src/hiveplotlib` only, per the 2026-06-11 coverage-scope ruling); warnings-as-errors clean; `make ty` clean.

**Tier-2 process-tree peak RSS (added 2026-06-26; in-scope for THIS MR; demoted to provisional 2026-07-03 — see the status bullet below).** Buildable and testable now without Dask (validated against a synthetic stdlib `multiprocessing` workload), so it moves into this harness MR rather than deferring to scaling WS5.
- A process-tree peak-RSS helper (`measure_peak_rss_tree`, or a `tree=True` mode on `measure_peak_rss`) runs the workload in a child and **samples** the summed RSS of that child plus all descendant processes at a fixed interval (~10–50 ms), tracking the max, returning bytes. Uses **psutil** for descendant discovery (`children(recursive=True)`) + per-process RSS sampling.
- **psutil returns as a dependency for the tree path only.** The single-process exact path (tier 1, shipped above) stays psutil-free (stdlib `resource.getrusage(RUSAGE_SELF)`); only the tree path needs psutil. This does **not** contradict the earlier "psutil not required" decision — that decision was about the single-process path, which remains stdlib-only. The split is: tier-1 single-process = stdlib exact; tier-2 tree = psutil sampled.
- **Two honest caveats, documented in the helper docstring and here:** (a) the tree path is **sampled, not exact** (unlike the single-process `ru_maxrss` high-water mark), so a sub-interval transient peak can be missed; acceptable because Dask workloads run on second-to-minute timescales with allocation plateaus. (b) summing RSS across processes **overcounts shared library pages**, so the result is a **conservative upper bound** on real footprint; PSS would be more accurate (Linux-only, pricier) and is a possible future refinement.
- **Tested now** against a synthetic multiprocessing workload (a top-level importable function spawning N children that each allocate a known array concurrently): assert the tree peak is meaningfully above a single-process baseline and roughly tracks the concurrent allocation. Exercised-by-tests (coverage gate is `src/hiveplotlib` only).
- **Status: provisional, not presumed-permanent (2026-07-03 maintainer decision, keep-but-demote).** Tier-2 is a *candidate* instrument for scaling WS5's Dask-path memory measurement, not the chosen one. WS5's measurement-methodology done-when is an explicit instrument selection among: tier-1 (sufficient if WS5 lands on Dask's **default threaded local scheduler** for dataframe work — one process, so single-process exact RSS captures everything); Dask-native diagnostics (MemorySampler / per-worker metrics; preferred where applicable, i.e. on the distributed scheduler); and tier-2 only where an external whole-tree observer is needed (the multiprocessing scheduler, which has no Dask diagnostics; or external verification of framework self-reporting, which matters most for the future autonomous-loop use where the metric must resist gaming — tier-2's one retained unique justification). **Hard pruning trigger:** if, as the scaling workstreams execute, the profiling practice in use does not employ tier-2 and its presence can no longer be justified, it MUST be removed (`measure_peak_rss_tree`, `_sample_tree_rss`, its two tests, the tree workload, and psutil from the `testing` extra if nothing else needs it) BEFORE the scaling super-plan closes — an explicit recorded decision at that plan's chain-close checkpoint, not a silent lingering. Full record in the 2026-07-03 amendment.

**Measurement-scope limitation — single-process exact (tier 1) AND multi-process sampled (tier 2) are covered; only GPU VRAM remains uncovered (2026-06-26; tier-2 demoted to provisional candidate 2026-07-03).** With the tier-2 process-tree helper added above, the harness covers **single-process exact (tier 1, shipped)** for the in-RAM numba-threaded work (scaling-plan WS1–3) **and offers multi-process sampled (tier 2, this MR) as a provisional candidate** for the Dask path (scaling-plan WS5; instrument selection at WS5, pruning checkpoint at chain close — 2026-07-03 amendment). The **only remaining uncovered regime is GPU VRAM**, which lands at WS5. Recorded here as the known boundary; the primary/canonical record lives in this plan, with a pointer added to scaling-plan Workstream 5 in parallel.

- **Tier 1 — single-process exact (`getrusage(RUSAGE_SELF)`), fine for scaling WS1–3:** numba `parallel=True` / `prange` use a threading backend (TBB / OpenMP / workqueue). Threads share one address space and therefore one RSS, so every byte all the parallel threads allocate lives in the measured child process and is fully captured by its `ru_maxrss`. Concurrent thread allocations push the high-water mark up together, so peak RSS captures the worst-case parallel footprint by construction. Multithreaded memory needs no special handling.
- **Tier 2 — multi-process sampled (psutil process-tree), provisional candidate for scaling WS5's Dask path (selection at WS5, pruning checkpoint at chain close; 2026-07-03):** a Dask multiprocessing / distributed scheduler spreads allocation across worker *processes*, which a parent's `ru_maxrss` does not see (`RUSAGE_CHILDREN` is the cumulative-max trap WS-B deliberately avoided). The tier-2 helper samples summed RSS across the child + all descendants and tracks the max. Sampled-not-exact and shared-page-overcounting caveats apply (see the tier-2 done-when above); it is a conservative upper bound, fine for the second-to-minute-timescale Dask workloads. Note Dask's default local scheduler for dataframe work is **threaded** (one process), in which case tier-1 suffices; on the distributed scheduler, Dask-native diagnostics are preferred where applicable, leaving tier-2 the external-observer/ungameability role only (2026-07-03 amendment).
- **Still NOT covered (GPU VRAM, scaling WS5):** cuDF / datashader-CUDA work allocates VRAM, not RSS, so neither tier sees it. Needs a distinct GPU-memory metric (per-process cupy mempool peak; see the 2026-06-26 GPU-VRAM amendment for why this lands at WS5, not here).
- **Disposition:** the only memory-measurement extension left for WS5 is the **GPU-VRAM metric**; process-tree RSS for multi-process Dask is **available** here (tier 2, provisional) but not presumed chosen — WS5 selects its instrument among tier-1 / Dask-native diagnostics / tier-2 (2026-07-03 amendment). The GPU metric is WS5's responsibility, flagged in the scaling plan in parallel.

### Workstream C: CI-runnable regression assertions

Restructured by the 2026-06-11 ASV amendment: pytest owns the per-MR **gate** (relative, same-run ratios); ASV owns **history and reporting** (rolling baseline, per-scenario history, marginal-vs-cumulative deltas, capture-at-merge).

**Status:** complete (2026-07-02, both halves; see Implementation log)
**Files:** `tests/performance_regression_test.py` (new), `pytest.ini` (register `@pytest.mark.performance` and `@pytest.mark.perf_harness`; deselect both from the default run, 2026-07-03 amendment), `Makefile` (serial performance target), CI config (serial performance stage), ASV config + benchmarks (home is an implementation detail, e.g. `benchmarks/` + `asv.conf.json`; outside the coverage surface), `runners/performance/README.md` (document the gate and how to run it locally vs. CI).
**Done when:**
- **Pytest ratio gates (relative, same-run, no absolute baselines).** Regression tests build fixtures via the parametrized `example_hive_plot` and assert **same-run ratios**, e.g.: streamed-path peak RSS ≤ a stated fraction of single-shot peak RSS at the large scenario; **tiny-floor** streamed-vs-single-shot timing within a stated band (time-only at that scale, 2026-06-26 grill). Document each chosen ratio/band and its rationale. Absolute timings and absolute RSS are NOT CI pass/fail criteria (they're unstable on shared GitLab CI runners; ratios are self-normalizing and reward-hack-resistant).
- The ratio gates use Workstream B's `measure_peak_rss`; the equivalence assertions from Workstream A gate any alternate path.
- **Serial performance stage (2026-06-11; marker split 2026-07-03).** Regression-gate tests are marked `@pytest.mark.performance`; harness-validation tests (equivalence soundness, measurement-instrument validation, constants consistency, WS2 arming canary) are marked `@pytest.mark.perf_harness` (equivalence + canary modules drop the `datashader` marker — the equivalence modules gain a skipif-on-missing-datashader guard *[skipif guards removed by the 2026-07-03 skipif-removal ruling: no dependency skips anywhere in the perf lane; missing datashader/psutil there is a broken `.[perf,testing]` install and errors loudly]* — and the measurement + consistency modules drop `unmarked`). **Both markers are deselected from the normal `-n 7` parallel run**; the serial pytest invocation (Makefile target + the per-MR `performance_gates` CI job; pytest.ini marker/deselection config) selects `-m "performance or perf_harness"`. Rationale for serial: 7 xdist workers competing for cores scrubs timing measurements. Rationale for the split (driving invariant, 2026-07-03 maintainer decision): a Python-matrix / backend-addon job failure means shipped library code is broken and implies a bugfix release; harness-validation failures are harness-side (fix the tooling, no release) and diluted that signal under `datashader`/`unmarked`. Canary enforcement rides the per-MR `performance_gates` job (not `make test`), which still blocks every MR, including scaling WS2's. Small-graph no-regression is asserted explicitly in this stage.
- **Streaming-vs-single-shot guard anchored at the tiny floor.** The scaling plan's WS2 (chunked rasterization) and WS3 (fused build) share one streaming-vs-single-shot threshold policy; below the threshold, chunking is slower than single-shot. So the ratio gates must cover a **fixed-overhead-scale scenario explicitly** where the single-shot path is the one expected to be selected, catching an accidental "always stream" policy change. The guard sits at the **tiny floor (≈ 34 nodes / 78 edges, karate-club scale), or explicitly includes it** (2026-06-26 grill): a fixed-overhead "always stream" regression first shows there, where the small=2,000 scenario would miss it. This is a **time-only** guard (no peak-RSS ratio at tiny scale). Make the tiny scenario a first-class, named entry in the benchmark scenarios (not just an incidental fixture size) so it can't be dropped silently.
- **ASV history and reporting.** ASV benchmarks cover the representative scenario set at **tiny / small / medium / large** scales (2026-06-26 grill; concrete sizes: tiny ≈ 34 nodes / 78 edges, small ≈ 2,000 / 5,000, medium ≈ 25,000 / 250,000, large ≈ 100,000 / 2,000,000; 10M+ is a manual stress run, not a standing capture-at-merge scenario). The **tiny scenario carries `time_` benchmarks only** (no `peakmem_`): peak RSS at karate-club scale is dominated by interpreter/imports, so a `peakmem_` ratio there is noise. **Small / medium / large carry both `time_` and `peakmem_`.** ASV owns: the rolling baseline updated as each scaling workstream merges; retained per-scenario history; **per-workstream marginal delta and cumulative joint effect** versus the original pre-chain baseline; and the capture-at-merge artifact store from the 2026-06-05 amendment. ASV's committed results JSON is the durable per-scenario store and the static data source for the scaling plan's Workstream 6 blog figures (plain JSON, plottable without ASV's HTML dashboards). Results are committed **as-is in ASV's native format** (no bespoke schema), produced by capture-at-merge runs on the **maintainer's local machine** (canonical host, ASV machine-tagged); CI never writes to the ASV history (2026-06-11 follow-up). Captures also run at **dependency-bump merges** (datashader/numba/numpy pins), not just workstream merges, so history steps attribute to the bump rather than the next unrelated code merge — the pytest gates are structurally drift-immune (all assertions are same-run, same-environment comparisons), so version bumps manifest **only** as ASV history steps, and capturing at the bump keeps those steps attributable (2026-07-03 amendment). Done-when: benchmarks parametrized over the four scales (tiny is `time_`-only); committed results capture each merge; marginal and cumulative deltas surfaceable from the stored history. Planned suite addition: scaling WS3 adds a matplotlib-backend render canary `time_` benchmark (all four vector backends consume the shared persisted-curves contract, so one core-dep benchmark canaries the family; see `wiki/wiki/plans/scaling-large-networks.md`, 2026-07-03 render-canary amendment).
- **Aggregation-combination language stays consistent with the scaling plan (2026-06-11).** Wherever this harness describes combining per-chunk raster aggregates, it must not assume addition: `ds.any` combines via elementwise OR/max; combination happens on raw per-chunk aggregates, with `tf.spread` and the density-correction divide applied exactly once at the end. Full correction lives in `wiki/wiki/plans/scaling-large-networks.md`.
- `runners/performance/README.md` documents: the gate's purpose, the pytest-gate / ASV-history split (including why ASV-as-CI-gate and a hand-rolled baseline store were rejected), the RSS-subprocess-over-tracemalloc decision, the ratios-over-absolutes decision, the serial performance stage, the `performance` / `perf_harness` marker split and its CI-signal rationale (matrix/addon jobs = shipped-code-only signal; perf_harness lane = tooling, 2026-07-03), the dependency-bump capture practice, the equivalence-tolerance rationale, and the in-repo-vs-deferred-sweep boundary.
- Full suite green under `make test` (`-n 7`, 100% coverage on `src/hiveplotlib`, warnings-as-errors) with `performance` and `perf_harness` tests deselected (2026-07-03 marker split); the serial performance stage (`-m "performance or perf_harness"`) green; `make ty` clean; CHANGELOG entry added. ASV benchmark code and `runners/performance/` are exempt from the coverage gate (2026-06-11 ruling; `pytest.ini` already sets `--cov=src/hiveplotlib`, so no config change needed).

## Cross-workstream interaction support

Surfaced in maintainer discussion (2026-05-29). The per-MR gate is per-workstream by construction; two interaction patterns need explicit harness support, both folded into Workstream C's benchmark scenarios:

1. **Cumulative / rolling-baseline benchmark across the chain** (implemented via ASV history per the 2026-06-11 amendment). Re-run a representative benchmark set at tiny / small / medium / large scales (2026-06-26 grill; tiny is `time_`-only, the other three carry both `time_` and `peakmem_`) against a rolling baseline updated as each scaling workstream merges, reporting both the per-workstream marginal delta and the cumulative joint effect versus the original pre-chain baseline. Catches benefits masked in isolation (WS2 memory win only fully realized after WS3) and two neutral changes interacting into a regression. Benchmark-scenario / reporting requirement, not a new equivalence concern.
2. **Tiny-floor no-regression for the shared streaming-vs-single-shot threshold.** WS2 (chunked rasterization) and WS3 (fused build) share one threshold policy; below it, chunking is slower. The regression band must assert no-regression at the **tiny floor specifically (≈ 34 nodes / 78 edges, karate-club scale), or a band that includes it** (2026-06-26 grill), where single-shot should stay selected and a fixed-overhead "always stream" regression first bites — the small=2,000 scenario would miss it. Time-only guard at that scale.

The full cross-workstream interaction catalog (all interaction pairs, not just the harness-side ones) lives in `wiki/wiki/plans/scaling-large-networks.md` under "Cross-workstream performance interactions" (added in parallel). This plan records only the two harness-side requirements above.

## Out of scope (stated explicitly)

- **Exploratory overnight sweep rig / autonomous-research while-loop.** Deferred; homed later (possibly a sibling repo or a third submodule), not here. The in-repo/separate line is "does CI need it / does it track the API": the gate tracks the API lockstep and runs in CI, so it is in-repo; the sweep rig does neither.
- **Large artifacts** (sweep CSVs, profiles, result plots). Stay out of git under `data/profiling/` (already gitignored via `.gitignore:17`).
- **Heavy GPU/cuDF/Dask benchmarking deps and the GPU-VRAM metric.** The gate runs on the standard test deps (plus psutil for the tier-2 tree path). The single-process exact peak-RSS path (tier 1) and the process-tree sampled path (tier 2, multi-process Dask RSS) both ship in this MR; only the **GPU-VRAM metric** is deferred to scaling-plan Workstream 5, because there is no cuDF/datashader-CUDA path to measure until WS5 and the cupy stack arrives with WS5/RAPIDS (see the Workstream B measurement-scope limitation and the 2026-06-26 GPU-VRAM amendment).
- **The Bézier "Bernstein hoist" kernel tweak and numba autotuning.** Handled outside both plans (hoist = small standalone PR; autotune = deferred, already mined by the maintainer).
- **float32 storage itself.** Already shipped (`src/hiveplotlib/hiveplot.py:1176-1207`); the harness treats it as the given baseline, not a change to gate.

## Holdouts

- `runners/performance/profiling_utils.py` (`time_call`): kept as-is; the existing runner uses it per-call. As shipped, the module's additions are `measure_peak_rss` (tier 1), `measure_peak_rss_tree` + `_sample_tree_rss` + `HAS_PSUTIL` (tier 2), and an incidental import-block fix (a `pyproject.toml` file-handle leak plus a dead `runners.get_version` import removal; see the 2026-06-26 WS-B Implementation log entry). The earlier "only addition is `measure_peak_rss`" wording predated tier 2 (corrected 2026-06-27); the holdout guarantee is that `time_call` itself remains intact.

## Implementation log

Append-only. One line per completed workstream.

2026-06-26: Workstream B complete. Added `measure_peak_rss(fn) -> int` to `runners/performance/profiling_utils.py` (primary path: child reports its OWN `getrusage(RUSAGE_SELF).ru_maxrss`, kB->bytes on Linux / bytes on macOS; docstring rejects `tracemalloc` and `RUSAGE_CHILDREN`-in-parent, names psutil/`time -v` as optional fallback) plus `tests/performance_measurement_test.py` (3 tests: large-vs-baseline delta, large-then-small independence guard, psutil order-of-magnitude cross-check gated on availability) and a lightweight `tests/_performance_measurement_workloads.py`. Avoided the heavy-parent `ru_maxrss`-inheritance floor (every child saturating at pytest's ~200 MB) via a two-level subprocess: a near-empty launcher spawns the measured interpreter so its parent is ~10 MB. Incidentally fixed a `pyproject.toml` file-handle leak (and a dead `runners.get_version` import) in this module that tripped warnings-as-errors on import. `time_call` left intact (Holdout). `make ty`/`make format` clean; green serially and under `-n 7`.
2026-06-27: Workstream B tier-2 complete. Added `measure_peak_rss_tree(fn, sample_interval_s=0.02) -> int` to `runners/performance/profiling_utils.py`: runs the workload in a freshly spawned clean child (`subprocess.Popen`), then the PARENT samples summed RSS across child + all descendants (`psutil.Process.children(recursive=True)` + `memory_info().rss`, via helper `_sample_tree_rss`) at the interval, tracking the max, returning bytes; mid-run children are rediscovered each pass and a final post-exit pass catches a last-interval peak. Docstring states both honest caveats (sampled-not-exact; cross-process RSS overcounts shared pages = conservative upper bound, PSS a future Linux-only refinement) and why the sampler runs from the parent (not in the measured tree). Added module-level `HAS_PSUTIL` flag and a `RuntimeError` (naming the `perf` extra) when psutil is absent; added `psutil` to the `perf` extra in `pyproject.toml`. Added a `multiprocess_tree_workload` (3 spawn workers each holding ~50 MB concurrently via a shared `Barrier`) plus constants to `tests/_performance_measurement_workloads.py`, and 2 psutil-gated tests to `tests/performance_measurement_test.py` (tree peak exceeds single-process baseline by ~the worker sum; tree peak tracks concurrent allocation within a generous ceiling). Tier-1 `measure_peak_rss`/`time_call` left intact; `make ty`/`make format`/ruff clean; 5 measurement tests green serially and under `-n 7`.
2026-06-26: Workstream A complete. Added `runners/performance/equivalence.py` (`collect_curves`, `assert_curves_equivalent` with dtype-aware tolerance + positional-NaN matching, `rasterize_hive_plot` + `assert_raster_equivalent` with an `allowed_mismatch_fraction`/`atol` knob through `datashade_hive_plot_mpl`) and `tests/performance_equivalence_test.py` (11 `@pytest.mark.datashader` soundness tests: identity-passes / perturbation-fails for curves and rasters, dtype-aware-tolerance pass, matching-vs-mismatched-NaN, shape/key-set mismatch fails, allowed-mismatch knob). Curves built at-construction by `example_hive_plot(num_nodes=300, num_edges=2000, seed=7, repeat_axes=True)`; no committed fixture files. `make ty`/`make format` clean, warnings-as-errors clean.
2026-07-02: Workstream A touch-up complete. Extended the raster gate for per-side render kwargs (the WS2/WS3 same-instance-rendered-two-ways pattern): `rasterize_hive_plot(**datashade_kwargs)` now passes through to `datashade_hive_plot_mpl` (caller wins over helper render defaults; reserved canvas kwargs `dpi`/`figsize`/`instance`/`tag` raise `ValueError`), and `assert_raster_equivalent` gained `proven_kwargs`/`alternate_kwargs` forwarded per side. 4 new `@pytest.mark.datashader` tests (same instance + cosmetic per-side `cmap_*` kwargs pass; per-side `pixel_spread_edges` reaches the render and fails; reserved-kwarg `ValueError` direct and via `proven_kwargs`); all 15 green serially and under `-n 7`; `make ty`/ruff clean.
2026-07-02: Workstream C partial (pytest-gate half; ASV half landing separately). Added `tests/performance_regression_test.py` (marker `performance`: 3 running sanity gates — medium-vs-tiny same-run peak-RSS ratio ≥ 1.3× and small-vs-empty-interpreter ≥ 2× via `measure_peak_rss` on build+rasterize children, plus an `assert_curves_equivalent` identity smoke at small — and 3 dormant skip-marked streamed-vs-single-shot gates armed for scaling WS2, with documented constants `STREAMED_PEAK_RSS_MAX_FRACTION_LARGE=0.5` and `TINY_FLOOR_TIME_MAX_RATIO=1.25`) and `tests/_performance_regression_workloads.py` (plan-pinned tiny/small/medium/large scenario sizes as first-class named constants; large is never built by the always-run gates). Registered the `performance` marker + default-run deselection (`-m "not performance"` in `pytest.ini` addopts; verified CLI `-m` overrides keep `-m datashader`/`-m bokeh`/`-m unmarked` workflows unchanged), added serial `make test-performance` (`-n 0`, `--no-cov`, separate report paths) and the `performance_gates` CI job mirroring `unit_tests`' setup. `make test` green (1379 passed, 100% cov, 6 perf tests deselected); `make test-performance` green (3 passed, 3 dormant skips, ~24 s); ruff/`ty` clean.
2026-07-02: Workstream C partial (ASV half; completes C together with the pytest-gate half above). Added `asv.conf.json` (virtualenv env type, pyproject-only `build_command`, wheel installed with the `[datashader]` extra, results committed at `benchmarks/results/`, envs/HTML gitignored under `.asv/`) and `benchmarks/benchmarks.py` (4 classes: `time_construct`/`time_rasterize` at tiny/small/medium/large, `peakmem_construct`/`peakmem_rasterize` at small/medium/large only per the tiny-is-time-only grill call; fixed seed 7 mirroring the equivalence fixture recipe; JIT warmup + instance builds in `setup` so build cost stays out of render timings; `number=1`/`repeat=3`/`rounds=1` with generous timeouts). Machine tag `ringtail` created (`asv machine --yes --machine ringtail`); all four scales proven in dev mode (`asv run --python=same --dry-run`): construct 55ms/91ms/0.32s/2.4s, rasterize 52ms/74ms/1.3s/20s, peakmem up to 5.5GB (large rasterize); no dev-mode measurement results committed (only ASV's regenerable `benchmarks.json`/`machine.json` metadata landed in `benchmarks/results/`). Rewrote `runners/performance/README.md` around the gate (split + rejected alternatives, RSS/tolerance/ratio/serial-stage rationale, scenario table, capture-at-merge commands, recovery-only retroactive rule, boundaries; old profiling-runner docs kept as a trailing section). Added `asv` to the `perf` extra and the WS-C CHANGELOG entry. `asv check`, ruff, `ty check` clean.
2026-07-02: Adversary fix pass complete. De-biased dormant tiny-floor gate (dual untimed warmup + best-of-3 via time_call), added always-running WS2 arming canary (performance_regression_canary_test.py) and benchmarks/workloads/fixture seed+scale consistency test, closed vacuous-pass holes in the equivalence wall (+3 tests), psutil into testing extra + unmarked marker on measurement tests, WS2-alone vs WS2+WS3 caveat on the 0.5 RSS constant.
2026-07-03: Process-provenance scrub complete (maintainer rule: shipped repo code never references wiki plans, grill sessions, or workstream labels). Removed all plan/grill/WS2-WS3/workstream references from benchmarks/, tests/performance*, runners/performance/, and asv.conf.json; rationale preserved with repo-internal pointers (runners/performance/README.md); skip/canary messages made self-contained and actionable; full suite 1386 passed, perf stage 3 passed / 3 skipped, ruff/ty clean.
2026-07-03: Two measurement-test flake fixes (found by looped reruns before first CI). (1) psutil cross-check delta assertion tolerates kernel page reclaim between samples (>= -16 MiB instead of >= 0; a real sign inversion still fails). (2) Tree-workload concurrency race fixed at the root: barrier released the instant all workers arrived, leaving a sub-sampling-interval full-concurrency window; workers now dwell 0.5 s (TREE_HOLD_SECONDS) holding their arrays post-barrier so the sampler always observes the concurrent peak. Assertions untouched for (2). 18/18 looped runs clean (15 serial + 3 under -n 7).
2026-07-03: Perf-lane install contract complete. `perf` extra gains the self-extra `hiveplotlib[datashader]` (pulls datashader/dask/numba/seaborn, so the two extras cannot drift); `performance_gates` CI job switched from `.[dev,testing]` to `.[perf,testing]`, sufficiency proven empirically in a fresh `.[perf,testing]` venv (30 passed + 3 dormant skips, zero dependency skips); dependency skipif guards REMOVED from the four perf-lane test modules per maintainer ruling (the lane has one documented sufficient install, so missing datashader/psutil there is a broken install that must fail loudly; graceful skips remain a default-suite-only convention), with canary/measurement docstrings and `runners/performance/README.md` updated to state the install contract; CHANGELOG harness bullet extended. `make test` unaffected (1359 passed, 100% src coverage); ruff/`ty` clean.
2026-07-03: Makefile capture targets complete (maintainer request, final pre-commit addition). Added `make benchmark-capture` (guards: must be on master, working tree must be clean, each exiting nonzero with a one-line reason; then `asv run HEAD^!`; on success echoes the `git add benchmarks/results` + commit follow-up instead of doing it) and `make benchmark-dev` (throwaway working-tree sanity run wrapping `asv run --python=same --dry-run --quick --show-stderr`, optional `BENCH=<regex>` narrowing). Dirty-tree guard message's asv claim verified against installed asv 0.6.6 source (runner spawns benchmarks from the working tree's `benchmark_dir` abspath while the package builds from the commit). Branch guard verified live on the feature branch (make exits 2 with the message); clean-tree guard logic verified against dirty and simulated-clean porcelain output; `benchmark-dev BENCH="time_construct.*(tiny|small)"` executed end-to-end (tiny 51 ms / small 112 ms, nothing recorded). README "Capture at merge" now names `make benchmark-capture` canonical with raw commands kept visible; dev-mode section names `make benchmark-dev`.
2026-07-03: Maintainer-review fix pass complete (cold final MR review; four findings fixed, maintainer-approved). (1) Closed the residual vacuous-pass hole in `assert_curves_equivalent`: a non-empty mapping whose curve arrays hold no finite values (all zero-size, e.g. every collected edge set has zero edges) now raises, completing the 2026-07-02 "comparing-nothing raises" guarantee; +1 soundness test. (2) Raster-gate knob validation: `allowed_mismatch_fraction` outside `[0.0, 1.0)` and negative/non-finite `atol`/`rtol` raise `ValueError` (catches the percent-style `2.0`-meaning-2% typo that would have made the gate silently always-pass); the knob soundness test now proves genuinely small headroom (0.25 vs a measured ~15% mismatch) instead of 1.0; +2 tests. (3) `measure_peak_rss` child failures are now diagnosable: the launcher relays the inner interpreter's stderr and exit code instead of check-and-swallowing, and the parent raises `RuntimeError` carrying the child's stderr (previously a bare `CalledProcessError` with exit code only); +1 failing-workload test. (4) `benchmark-capture` dirty-tree guard scoped with `--ignore-submodules` (submodule content feeds neither the package build nor the benchmark code, and permanently-dirty submodule state on the canonical host would have refused the first scheduled capture). Also corrected the constants-consistency module docstring's stale "Default-suite guard" first line (perf-lane guard) and `rasterize_hive_plot`'s `plot_width`/`plot_height` docs (canvas targets; actual raster dims derive from the axes bbox, deterministically). README tolerance/capture wording updated to match. Accepted without change: the name-pinned canary residual (the scaling plan's WS2 done-when is the process backstop) and the `performance_gates` job's inline pytest command (CI invokes no make targets anywhere, so inline is the house style).

## Adversary post-impl review (2026-07-02)

Cold review against the full shipped WS-A/B/C set. Three must-fix findings, all confirmed empirically, all fixed in the same MR before maintainer review:

1. **Dormant tiny-floor timing gate had systematic cold-vs-warm bias** (~66x measured: the first in-process rasterize pays import+JIT cost), which would have tolerated an ~80x regression at WS2-unskip. Fix: unconditional warmup of both paths + best-of-N timing in the dormant body.
2. **No mechanism forced the dormant gates to arm at WS2** (skip reasons were prose, not enforcement). Fix: a canary test in the always-running default suite fails the moment `stream_chunk_threshold` appears in the datashader entry point's signature, with instructions to unskip the dormant gates and delete the canary. Scaling-plan WS2's done-when amended in parallel to require the arming + canary deletion. *[Canary home re-marked 2026-07-03: it carries `perf_harness` and enforces via the per-MR `performance_gates` job, not the default `make test` run; still blocks every MR, including WS2's. See the 2026-07-03 marker-split amendment.]*
3. **Vacuous-pass hole in the equivalence wall:** empty-vs-empty curve mappings (e.g. a wrong/stale `tag` on both sides) and (0,0) raster pairs passed silently. Fix: comparing-nothing now raises.

Worth-discussing items and dispositions: seed/size triplication across fixture modules now enforced by a default-suite consistency test *[consistency + measurement tests re-marked `perf_harness` 2026-07-03; they run in the per-MR `performance_gates` job, not the default suite]*; psutil added to the `testing` extra + `unmarked` pytestmark so measurement tests run in CI (previously silently skipped) *[the `unmarked` pytestmark superseded by `perf_harness` 2026-07-03; the run-in-CI intent stands via `performance_gates`]*; the 0.5 dormant RSS-fraction constant's unskip decision must be made explicitly against WS2-alone vs WS2+WS3-cumulative (comment added at the constant; see the scaling plan's interactions §2); the first real `asv run` (pre-chain baseline) is explicitly scheduled for THIS MR's merge on the canonical host, per the README's capture-at-merge commands.

Two adversary process lessons for the harness's own expertise file, noted for the maintainer to carry into `agent-harness` when convenient (that submodule has uncommitted state; not edited here): (a) skip reasons are prose, not enforcement — demand a mechanical arming trigger; (b) check which side of a same-run timing ratio pays first-call import/JIT cost.

## Plan amendments

Append-only. Concise per mental-model rule 17.

### 2026-05-30 — Two cross-workstream interaction requirements (from 2026-05-29 maintainer discussion)

- **In-scope tweak (Workstream C): small-scale streaming-vs-single-shot guard.** Added a done-when so the regression band covers a named small-graph scenario where single-shot should stay selected, catching an accidental "always stream" change in the threshold policy WS2 and WS3 of the scaling plan share.
- **In-scope tweak (Workstream C): rolling-baseline cumulative benchmark.** Added a done-when so benchmark scenarios run at small/medium/large scales against a rolling baseline updated as each scaling workstream merges, reporting per-workstream marginal delta and cumulative joint effect. Reuses Workstream A's equivalence check unchanged; no new equivalence concern.
- Added a "Cross-workstream interaction support" section summarizing both, with a pointer to the full interaction catalog in `wiki/wiki/plans/scaling-large-networks.md` ("Cross-workstream performance interactions", added in parallel). No new workstream, no entry-point or attribute-read change (no feasibility audit needed).

### 2026-06-05 — Artifact-retention requirement (capture-at-merge for perishable wins)

- **In-scope tweak (Workstream C): durable per-scenario artifact capture at each merge.** New done-when: the harness durably stores (committed, e.g. extending the in-repo baseline JSON from the existing done-when at line 89) the **per-scenario** results (small / medium / large; peak RSS **and** timing) at each merge, rich enough to regenerate publication-quality plots for the Workstream-6 blog later, not merely a pass/fail regression verdict. This extends the existing rolling-baseline store (done-when at line 93 / "Cross-workstream interaction support" §1) from "rolling value sufficient for the no-regression gate" to "retained per-scenario history sufficient to re-plot the chain's progression."
- **Rationale to record (load-bearing, not insurance): not all wins are reconstructable at the end.** *Retained-alternative* changes keep both paths in the code (single-shot datashader and two-stage `construct_curves` both stay as defaults / equivalence baselines per the scaling plan), so their before/after is always re-runnable on demand. *Replace-in-place* changes delete the old code on merge, e.g. scaling-plan WS1 (full-length boolean-mask membership storage replaced by integer indices) and future Bezier-call reductions, so their "before" is perishable and survives **only** as what the harness captured at merge. The capture is therefore the sole evidence for the blog's replace-in-place speedup/memory claims, making it load-bearing for Workstream 6, not a nice-to-have.
- **Confirm at the harness's own pre-implementation alignment (grill) pass.** Flag this requirement explicitly to be confirmed / agreed with the maintainer at the harness's grill pass, **before** downstream workstreams build, so the capture design (schema, what fields, where committed) is validated before the chain makes replace-in-place "before" states impossible to re-benchmark. This is the next real checkpoint; nothing dispatches until the harness is built and grilled.
- No new entry point, no new attribute read (no feasibility audit needed); this is a reporting / artifact-store requirement on Workstream C, reusing Workstream A's equivalence check unchanged.

### 2026-06-11 — ASV hybrid restructure (maintainer review session, settled decision)

- **Trigger:** 2026-06-11 maintainer review; final call, not a proposal. Plan stays gated behind in-flight work; amendments capture decisions while fresh.
- **In-scope tweak (Workstream C): pytest gates become relative, same-run ratio assertions.** No absolute timings vs. a committed baseline JSON: e.g. streamed-path peak RSS ≤ a stated fraction of single-shot peak RSS at the large scenario; small-graph streamed-vs-single-shot timing within a stated band. Absolute wall-time baselines are unstable on shared GitLab CI runners; ratios are self-normalizing and more reward-hack-resistant. Absolute numbers are never CI pass/fail criteria.
- **In-scope tweak (Workstream C): ASV (airspeed velocity) owns history/reporting.** Rolling baseline, retained per-scenario history (small/medium/large), marginal-vs-cumulative delta reporting across the workstream chain, and the capture-at-merge artifact store from the 2026-06-05 amendment. ASV `time_` benchmarks replace the planned `time_median_of_n` for history; `peakmem_` benchmarks cover memory history. ASV's committed results JSON is the durable per-scenario store and the static data source for the scaling plan's Workstream 6 blog figures (plain JSON, plottable without ASV's HTML dashboards). Config/benchmarks home (e.g. `benchmarks/` + `asv.conf.json`) is an implementation detail, outside the coverage surface.
- **In-scope tweak (Workstream B): shrinks to one psutil-based subprocess peak-RSS helper** used by the pytest ratio gates. The tracemalloc-rejection rationale stands and stays documented. The rest of B's planned helpers are superseded by ASV built-ins.
- **Workstream A unchanged:** equivalence gate stays pytest; correctness checking of curve arrays/rasters is not ASV's job.
- **Recorded in Default justifications:** ASV-as-CI-gate rejected (`asv continuous` builds and benchmarks two commits per MR; too heavy for per-MR gating); fully-hand-rolled rolling-baseline store rejected (reinvents ASV's history tracking).
- **Perishability relaxation (softens, does not remove, the 2026-06-05 rationale):** ASV can check out and benchmark historical commits, so replace-in-place "before" states (e.g. scaling WS1's storage change) remain re-measurable after merge, provided benchmark code (which lives outside the package tree) guards against API drift across commits. Capture-at-merge via committed ASV results stays the primary record.
- Dev/CI tooling only; no new library entry point or attribute read (no feasibility audit needed).

### 2026-06-11 — Serial performance stage (maintainer review session, settled decision)

- **In-scope tweak (Workstream C):** `@pytest.mark.performance` tests are deselected from the normal `-n 7` parallel run and executed in a separate serial pytest invocation (Makefile target + CI stage + pytest.ini marker/deselection config, named in Workstream C Files). Rationale: 7 xdist workers competing for cores scrubs timing measurements.

### 2026-06-11 — Coverage scope ruling (maintainer decision)

- **In-scope tweak (Workstreams A and B):** the 100% coverage gate applies to `src/hiveplotlib` only (the code users hit in the deployed package). `runners/performance/`, ASV benchmark code, and harness tooling are exempt; the "100% coverage on `equivalence.py` / new helpers" done-when phrasing is relaxed to "exercised by tests." No config change needed: `pytest.ini` already sets `--cov=src/hiveplotlib`.
- **Pragma policy (recorded for downstream scaling workstreams):** GPU-gated and RAM-gated tests skip in CI and would leave their `src/` branches uncovered, so such branches (e.g. cuDF dispatch, 10M-scale paths) carry explicit `# pragma: no cover` (or equivalent) with a comment naming the gating test, so downstream workstreams don't each rediscover the coverage failure.

### 2026-06-11 — Aggregation-combination correction (cross-plan consistency)

- **In-scope tweak (Workstream C language only):** wherever this plan refers to combining per-chunk raster aggregates, align with the scaling plan's parallel amendment: `ds.any` combines via elementwise OR/max (NOT addition); combination happens on raw per-chunk aggregates, with `tf.spread` and the density-correction divide applied exactly once at the end. Audit of this plan's body found no language asserting additive combination; a guard bullet was added to Workstream C so future equivalence/regression wording can't contradict. Full correction lives in `wiki/wiki/plans/scaling-large-networks.md` (amended in parallel; not edited here).

### 2026-06-11 — Capture schema, retroactive runs, benchmark host (maintainer review session, settled decisions)

- **Trigger:** continuation of the same 2026-06-11 maintainer review; three final dispositions, settled. Plan stays gated and not executing.
- **In-scope tweak (Workstream C): capture schema = ASV's native results format; no bespoke schema design.** The capture-at-merge store is whatever `asv run` emits, committed as-is. The 2026-06-05 grill checkpoint ("schema, what fields, where committed") therefore shrinks to a five-minute confirmation: benchmark scenario naming covers small/medium/large scales with both `time_` and `peakmem_` variants, sufficient to regenerate the Workstream-6 blog figures from the committed JSON alone.
- **In-scope tweak (recorded rationale): retroactive re-benchmarking is recovery-only, never the foundation.** Maintainer asked whether capture could be dropped in favor of ASV's old-commit re-runs; no, for two reasons: (a) **benchmark-code API drift** — the chain deliberately changes the surface benchmarks touch (`stream_chunk_threshold` doesn't exist pre-WS2; the fused path doesn't exist pre-WS3), so old-commit runs need per-benchmark guards, each a chance to silently measure the wrong thing; (b) **environment non-reproducibility** — ASV rebuilds hiveplotlib at the old commit under re-run-time deps, so retroactive numbers are not the historical numbers, and an old commit may not run at all under future datashader/numba pins. Rule when retroactive runs ARE used: both ends of a comparison run in the same ASV session (same machine, same deps). Capture-at-merge remains the primary record, and is nearly free since committing ASV results is already the rolling-baseline mechanism. Refines (tightens) the prior entry's "perishability relaxation" bullet.
- **In-scope tweak (Workstream C): canonical benchmark host is the maintainer's local machine (WSL box).** Capture-at-merge ASV runs happen locally at each workstream merge and the results JSON is committed; CI never writes to the ASV history. CI's only performance role is the relative same-run pytest ratio gates. ASV's machine-tagging makes this first-class. Caveat: a mid-chain machine swap forks the history and muddies cumulative-vs-original comparisons, so aim to keep one host for the chain's duration.
- Dev/CI tooling only; no new library entry point or attribute read (no feasibility audit needed).

### 2026-06-26 — Grill capture (Wave 1) recorded as a new `## Alignment (grill)` section

- The plan predates the `## Alignment (grill)` gate slot; created it (before Workstreams) and recorded the maintainer shared-understanding pass, Wave 1: confirmed scenario sizes (tiny/small/medium/large with concrete node/edge counts, 10M+ manual-only), the tiny time-only floor and its rationale, the `getrusage`-primary / psutil-fallback peak-RSS call, and Workstream A's self-proof shape. The two emergent decisions the grill settled are recorded as the separate in-scope-tweak amendments below.
- No new workstream, no entry-point or attribute-read change (no feasibility audit needed).

### 2026-06-26 — Emergent: benchmark scenario set becomes tiny / small / medium / large (grill-settled)

- **In-scope tweak (Workstream C + Cross-workstream interaction support §1–§2):** the benchmark scenario set is now **tiny / small / medium / large** with concrete sizes (tiny ≈ 34 nodes / 78 edges karate-club scale; small ≈ 2,000 / 5,000; medium ≈ 25,000 / 250,000; large ≈ 100,000 / 2,000,000). 10M+ is a manual stress run only, not a standing capture-at-merge scenario. **Supersedes the "small / medium / large" scale list** used in the 2026-05-30, 2026-06-05, and 2026-06-11 amendments and their done-whens; those historical entries are left intact per append-only, and this entry is the current truth.
- **Tiny is `time_`-only** (no `peakmem_`): peak RSS at karate-club scale is interpreter/import-dominated, so a `peakmem_` ratio there is noise. Small / medium / large keep both `time_` and `peakmem_`.
- **The streaming-vs-single-shot guard is anchored at the tiny floor** (or explicitly includes it), since a fixed-overhead "always stream" regression first shows at karate-club scale, where small=2,000 would miss it. Updated the WS-C done-when, the WS-C pytest-ratio-gate example, and interaction-support §2 accordingly (re-homed the guard from "small" to "tiny floor"; time-only at that scale).
- No new workstream, no entry-point or attribute-read change (no feasibility audit needed); benchmark-scenario / reporting requirement only.

### 2026-06-26 — Emergent: Workstream B peak-RSS primary path is stdlib `getrusage`, psutil optional (grill-settled)

- **In-scope tweak (Workstream B):** `measure_peak_rss(...)` runs the workload in a spawned child and returns peak RSS in bytes via **stdlib `resource.getrusage(RUSAGE_CHILDREN).ru_maxrss` as the primary path** (exact kernel high-water mark, no polling/sampling miss, no new dependency; Linux/WSL is both canonical host and CI). psutil polling and `/usr/bin/time -v` "Maximum resident set size" are the documented **cross-platform fallback only**. Updated the WS-B done-when, the WS-B intro line, the "Peak RSS …" Default justification, and the `measure_peak_rss` Naming audit line.
- **psutil is not a required core dependency.** If used at all it is an optional fallback, handled so the suite never errors on its absence (the primary `getrusage` path is stdlib). Removed the implication (in the prior phrasing and the 2026-06-11 ASV amendment's "one psutil-based subprocess peak-RSS helper") that psutil is the primary mechanism or a needed dep; this entry supersedes that framing.
- **The `tracemalloc`-rejection rationale stays documented verbatim in intent** (under-counts numba/numpy C allocations); unchanged.
- Workstream A needs no change from this grill (its shape was confirmed as-is in the Alignment section).
- Dev/CI tooling only; no new library entry point or attribute read (no feasibility audit needed).

### 2026-06-26 — Measurement-scope limitation: `measure_peak_rss` is single-process (maintainer discussion; canonical record)

- **Trigger:** maintainer discussion (2026-06-26) clarifying what `measure_peak_rss` (shipped in Workstream B) does and does not cover. This plan is the **primary/canonical record** of the limitation; a pointer is being added to scaling-plan Workstream 5 in parallel.
- **In-scope tweak (Workstream B note, no behavior change):** `measure_peak_rss` is **single-process by design**. That is **correct and complete for the in-RAM, numba-threaded work (scaling-plan WS1–3)** but **does NOT reach the multi-process or GPU work (scaling-plan WS5)**. Recorded as a known limitation under Workstream B (and cross-referenced from Out of scope) so nobody assumes the helper covers the Dask/GPU paths.
  - **Fine for WS1–3:** numba `parallel=True` / `prange` use a threading backend (TBB / OpenMP / workqueue); threads share one address space and one RSS, so all parallel-thread allocations live in the measured child and are captured by its `ru_maxrss`, with concurrent allocations pushing the high-water mark up together (worst-case parallel footprint captured by construction). No special multithreaded handling needed.
  - **Does NOT reach (WS5):** process-based parallelism (Dask) spreads allocation across worker *processes* the parent's `ru_maxrss` can't see (`RUSAGE_CHILDREN` being the cumulative-max trap WS-B deliberately avoided) — needs process-tree RSS aggregation; GPU memory (cuDF) allocates VRAM not RSS — needs a distinct GPU-memory metric (cupy mempool stats / `nvidia-smi`).
- **Disposition:** the WS5 memory-measurement extension (process-tree RSS aggregation for multi-process Dask; a GPU-VRAM metric for cuDF) is **WS5's responsibility, not provided by WS-B**, and aligns with this plan's existing "Out of scope: heavy GPU/cuDF/Dask benchmarking deps" boundary.
- No new workstream, no entry-point or attribute-read change, no behavior change to shipped WS-B code (no feasibility audit needed); documentation/limitation record only.

### 2026-06-26 — Tier-2 process-tree peak-RSS helper moves INTO Workstream B (maintainer discussion)

- **Trigger:** maintainer discussion (2026-06-26). The multi-process Dask-path memory metric, previously deferred to scaling WS5 by the earlier-today measurement-scope-limitation entry, is **buildable and testable now without Dask** (validated against a synthetic stdlib `multiprocessing` workload), so it moves into this harness MR.
- **In-scope tweak (Workstream B): add a process-tree peak-RSS helper.** `measure_peak_rss_tree` (or a `tree=True` mode on `measure_peak_rss`) runs the workload in a child and **samples** the summed RSS of that child plus all descendant processes at a fixed interval (~10–50 ms), tracking the max, returning bytes. Uses **psutil** for descendant discovery (`children(recursive=True)`) + per-process RSS sampling.
- **psutil returns as a dependency for the tree path only.** The single-process exact path (tier 1, shipped) stays psutil-free (stdlib `resource.getrusage(RUSAGE_SELF)`); only the tree path needs psutil. This does not contradict the earlier "psutil not required" decision (that decision was about the single-process path, which stays stdlib-only). Recorded as an explicit split — tier-1 single-process = stdlib exact; tier-2 tree = psutil sampled — so it doesn't read as a relitigation of the 2026-06-26 grill's `getrusage`-primary call.
- **Two honest caveats** documented in the helper docstring and the Workstream B note: (a) the tree path is **sampled, not exact**, so a sub-interval transient peak can be missed (acceptable: Dask workloads run on second-to-minute timescales with allocation plateaus); (b) summing RSS across processes **overcounts shared library pages**, so it is a **conservative upper bound** (PSS would be more accurate, Linux-only and pricier, a possible future refinement).
- **Tested now** against a synthetic multiprocessing workload (top-level importable function spawning N children each allocating a known array concurrently): assert the tree peak is meaningfully above a single-process baseline and roughly tracks the concurrent allocation. Exercised-by-tests (coverage gate is `src/hiveplotlib` only).
- **Effect on the measurement-scope limitation:** the harness now covers single-process exact (tier 1) AND multi-process sampled (tier 2); the only remaining uncovered regime is GPU VRAM. Updated the Workstream B measurement-scope note and the Out-of-scope GPU/Dask bullet accordingly.
- Dev/CI tooling only; no new **library** entry point or attribute read (the new helper is a `runners/performance/` harness function, not user-facing API; no feasibility audit needed). psutil is added as a (tree-path-only) harness dependency.

### 2026-06-26 — GPU-VRAM note refined with local capability probe; stays WS5 (maintainer discussion)

- **Trigger:** maintainer discussion (2026-06-26) following a local GPU capability probe. Refines (does not move) the GPU-VRAM bullet of the measurement-scope limitation. No change to this MR's scope.
- **In-scope tweak (Workstream B note language + Out-of-scope bullet):** the GPU regime is **testable locally and is NOT permanently hardware-blocked** — the canonical host has a usable GPU (NVIDIA RTX 5070, 12 GB; `nvidia-smi` reads VRAM), so the earlier framing overstated the blocker.
- **Why it still lands at WS5 (dependency-and-consumer reason, not hardware):** (a) there is no cuDF / datashader-CUDA code path to measure until WS5; (b) the env currently has no working GPU-allocation library to write a synthetic test against (`numba.cuda.is_available()` is False; cupy and pynvml absent); (c) the correct metric is **per-process cupy mempool peak (exact)**, not `nvidia-smi` whole-GPU usage (shared/noisy — the box already sits ~5 GB used by other apps). cupy is the WS5/RAPIDS stack, so its install, the synthetic test, and the real consumer all arrive together at WS5.
- **Disposition:** WS5 builds and tests the GPU-VRAM metric on the local RTX 5070 as a **hard done-when gate** — no GPU scaling claim without a measured per-process VRAM peak via cupy mempool stats. Not stubbed, not assumed. (This refines the GPU bullet in the measurement-scope note above; the cupy-mempool-peak metric is the canonical choice, replacing the earlier "cupy mempool stats / `nvidia-smi`" either/or framing.)
- No new workstream, no library entry-point or attribute-read change (no feasibility audit needed); WS5-scoped note refinement only. The synthetic-test-and-metric work itself is recorded as a hard gate for scaling-plan Workstream 5, flagged in that plan in parallel.

### 2026-06-27 — Documentation reconciliation: shipped WS-B mechanism is child-reported `RUSAGE_SELF`; bookkeeping refresh (maintainer-approved)

- **Trigger:** maintainer-approved documentation-reconciliation pass (2026-06-27). The plan contradicted the shipped WS-B code: the "Peak RSS" Default justification, the WS-B done-when, the Wave-1 grill-capture bullet, and the 2026-06-26 "getrusage primary" amendment all said the primary path is **parent-measured `getrusage(RUSAGE_CHILDREN).ru_maxrss` around a spawned child**, while the shipped code (2026-06-26 WS-B Implementation log entry) has the **child measure and report its OWN `getrusage(RUSAGE_SELF).ru_maxrss`**, launched via a two-level subprocess (near-empty launcher spawns the measured interpreter). The plan's own measurement-scope note already called `RUSAGE_CHILDREN` "the cumulative-max trap WS-B deliberately avoided," so the plan disagreed with itself.
- **In-scope tweak (correction of record; explicitly supersedes the mechanism wording of the 2026-06-26 "Workstream B peak-RSS primary path is stdlib `getrusage`, psutil optional" amendment, which stays untouched per append-only).** The grill confirmed the **INTENT** (stdlib `getrusage`, exact kernel high-water mark, no psutil on the single-process path); implementation corrected the **MECHANISM** (child-reported `RUSAGE_SELF`, two-level subprocess), avoiding two traps: (a) `RUSAGE_CHILDREN` is a cumulative max across ALL reaped children of the parent's lifetime, so sequential measurements would be poisoned by the largest prior one; (b) on Linux a child spawned directly from a heavy parent inherits its `ru_maxrss` floor (~200 MB under pytest), which the near-empty launcher resets. Updated the Default justification and the WS-B done-when (including the shipped signature: `measure_peak_rss(fn)` takes a top-level importable zero-arg function, not `callable_or_argv`); added a bracketed correction note to the grill-capture bullet so every section now reads the true mechanism.
- **Bookkeeping refresh (living sections corrected to match the Implementation log):** WS-A Status set to complete (2026-06-26); WS-B Status set to complete including tier 2 (2026-06-26 / 2026-06-27); WS-C stays not started. Holdouts corrected: `time_call` remains intact, but the module's shipped additions are `measure_peak_rss`, `measure_peak_rss_tree`, `_sample_tree_rss`, `HAS_PSUTIL`, plus the incidental import-block fix — not "only `measure_peak_rss`."
- **Cross-plan:** `wiki/wiki/plans/scaling-large-networks.md` WS7's dependency block still cited the dropped `time_median_of_n` helper; corrected in parallel (see that plan's 2026-06-27 bookkeeping amendment).
- No behavior change, no new entry point or attribute read (no feasibility audit needed); documentation reconciliation only.

### 2026-07-02 — Adversary post-impl review: three must-fix hardenings + worth-discussing dispositions (fixed in this MR)

- **Trigger:** adversary post-impl mode, run cold against the full shipped WS-A/B/C set (2026-07-02). Three must-fix findings, all confirmed empirically; a code-engineer is landing all three fixes in the same MR before maintainer review. Full record in the "Adversary post-impl review (2026-07-02)" section above the amendments.
- **In-scope tweak (Workstream C, must-fix): dormant tiny-floor timing gate de-biased.** The dormant gate compared a cold first call against a warm one (~66x measured bias: the first in-process rasterize pays import+JIT), so at WS2-unskip it would have tolerated an ~80x real regression inside the 1.25x band. Fix: unconditional warmup of both paths + best-of-N timing in the dormant body.
- **In-scope tweak (Workstream C, must-fix): mechanical arming trigger for the dormant gates.** Nothing forced the three dormant streamed-vs-single-shot gates to unskip at scaling WS2; the skip reasons were prose. Fix: a canary test in the always-running default suite fails the moment `stream_chunk_threshold` appears in the datashader entry point's signature, instructing the implementer to unskip the dormant gates and delete the canary. The scaling plan's WS2 done-when is amended in parallel to make (unskip + canary deletion + explicit RSS-constant decision) part of WS2's completion.
- **In-scope tweak (Workstream A, must-fix): vacuous-pass hole closed.** Empty-vs-empty curve mappings (e.g. wrong/stale `tag` on both sides) and (0,0) raster pairs passed silently; the equivalence wall could "pass" while comparing nothing. Fix: comparing-nothing now raises.
- **Worth-discussing dispositions:** seed/size triplication across fixture modules enforced by a default-suite consistency test; psutil added to the `testing` extra + `unmarked` pytestmark so measurement tests actually run in CI (previously silently skipped); the 0.5 dormant RSS-fraction constant carries a comment requiring its unskip decision be made explicitly against WS2-alone vs WS2+WS3-cumulative (never quiet retuning); the first real `asv run` (pre-chain baseline) is scheduled for THIS MR's merge on the canonical host per the README's capture-at-merge commands.
- **Harness-process lessons (for `agent-harness` expertise, when convenient — that submodule has uncommitted state, not edited now):** skip reasons are prose, not enforcement — demand a mechanical arming trigger; check which side of a same-run timing ratio pays first-call import/JIT cost.
- Dev/CI tooling only; no new library entry point or attribute read (no feasibility audit needed).

### 2026-07-03 — Tier-2 process-tree helper demoted to provisional; disposition trigger at scaling chain close (maintainer decision)

- **Trigger:** maintainer review of tier-2 (`measure_peak_rss_tree`), 2026-07-03; decision is keep-but-demote. No code change now; status/disposition record plus a durable principle.
- **In-scope tweak (Workstream B tier-2 block + measurement-scope note): status change to provisional, not presumed-permanent.** Tier-2 is a *candidate* instrument for scaling WS5's Dask-path memory measurement, not the chosen one. Supersedes the "covers scaling WS5's Dask path" framing here and the scaling plan's "WS5 inherits tier 2, does not build its own" framing (2026-06-26 entries left intact per append-only; the scaling plan's WS5 done-when is amended in parallel).
- **Instrument-selection framing (replaces inheritance).** Scaling WS5's measurement-methodology done-when becomes an explicit instrument SELECTION among three candidates, with the deciding fact recorded: Dask's default local scheduler for dataframe work is **threaded** (one process), in which case tier-1 single-process exact RSS suffices and neither tier-2 nor Dask-native diagnostics are needed. Candidates: (a) **tier-1** if WS5 lands on the threaded scheduler; (b) **Dask-native diagnostics** (MemorySampler / per-worker metrics) if on the distributed scheduler — preferred where applicable per the ecosystem-standard-tooling principle below; (c) **tier-2** only where an external whole-tree observer is needed: the multiprocessing scheduler (which has no Dask diagnostics), or external verification of framework self-reporting (which matters most for the future autonomous-loop use, where the metric must resist gaming).
- **Maintainer rationale (durable principle):** low-level bespoke code whose correctness is contingent on the implementing session's understanding rather than the maintainer's fluency is a maintenance liability; ecosystem-standard instruments with community guarantees and uniformity with how the rest of the world profiles (e.g. Dask's own profiler for Dask code) are preferred even at some capability cost. Tier-2's one retained unique justification is the external-observer/ungameability property.
- **Hard pruning trigger (the compromise):** if, as the scaling workstreams actually execute, the profiling practice in use does not employ tier-2 and its presence can no longer be justified, it MUST be removed — the `measure_peak_rss_tree` function, `_sample_tree_rss`, its two tests, the tree workload, and psutil from the `testing` extra if nothing else needs it — BEFORE the scaling super-plan closes. Removal or retention is an explicit recorded decision at the scaling plan's chain-close checkpoint (recorded there alongside ADR promotion), not a silent lingering.
- No code change now, no new entry point or attribute read (no feasibility audit needed); the tier-2 helper and its tests stay shipped as-is pending the WS5 selection and the chain-close disposition.

### 2026-07-03 — Harness-validation tests get a dedicated `perf_harness` marker, out of the shipped-code CI signal (maintainer decision)

- **Trigger:** maintainer decision (2026-07-03); a code-engineer is landing the change in the same MR in parallel. This entry records the decision; the WS-C done-when, Files, README bullets and the Naming audit are edited to match.
- **Driving invariant (record this as the CI semantics):** a test failure in the Python-matrix or backend-addon CI jobs means shipped library code is broken and implies a bugfix release. Harness-validation tests — equivalence soundness, measurement-instrument validation, constants consistency, the WS2 arming canary — validate `runners/performance/` and benchmark tooling that never ships to users; their failures are predominantly harness-side. In particular, the equivalence identity tests compare a render to itself in one environment, so they are structurally incapable of catching cross-version drift; their unique detection value is the comparison logic itself. Leaving them under the `datashader`/`unmarked` markers diluted the release signal.
- **In-scope tweak (Workstream C): new `perf_harness` marker on the four harness test modules.** Equivalence tests lose the `datashader` marker (gaining a skipif-on-missing-datashader guard) *[skipif guards superseded same-day: removed by the 2026-07-03 skipif-removal ruling below — a missing dep in the perf lane is a broken `.[perf,testing]` install and must ImportError loudly, not skip]*; measurement + consistency tests lose `unmarked`; the canary loses `datashader`. The default run deselects `perf_harness` alongside `performance`; the serial stage and the per-MR `performance_gates` CI job select `-m "performance or perf_harness"`. Resulting semantics: matrix/addon jobs = shipped-code-only signal (the maintainer's inference rule holds exactly); perf_harness lane red = fix the tooling, no release. **Canary enforcement moves from `make test` to the per-MR `performance_gates` job** — it still blocks every MR, including scaling WS2's, so the mechanical arming trigger keeps its teeth. Supersedes the 2026-07-02 adversary-fix dispositions that put the measurement tests under `unmarked` and the canary in the always-running default suite (those entries stay intact per append-only; bracketed correction notes added at the 2026-07-02 review section).
- **Marker-name rationale (Naming audit updated):** bare `harness` rejected as ambiguous (test harness / agent harness / performance harness); `perf_harness` matches this plan's and the README's vocabulary.
- **In-scope tweak (Workstream C, capture-at-merge practice): run an ASV capture at dependency-bump merges too** (datashader/numba/numpy pins), not only at workstream merges, so history steps attribute to the bump rather than the next unrelated code merge. Rationale: the gates are structurally drift-immune (all assertions are same-run, same-environment comparisons), so version bumps manifest only as ASV history steps; capturing at the bump keeps those steps attributable. Added to the WS-C ASV/capture-at-merge done-when and the README bullet.
- Dev/CI tooling only; no new library entry point or attribute read (no feasibility audit needed).

### 2026-07-03 — Perf-lane dependency skipif guards removed; a missing dep in that lane must ImportError loudly (maintainer ruling)

- **Trigger:** maintainer ruling (2026-07-03), same day as and partially superseding the marker-split amendment above; a code-engineer is landing the change in the same MR in parallel (it owns the test files, pyproject, CI, README — no code edited by this amendment).
- **In-scope tweak (Workstream C): the dependency skipif guards added with the `perf_harness` marker split are REMOVED** — the datashader skipifs on the equivalence tests, the canary, and the RSS sanity gates, and the psutil skipifs on the measurement tests. Rationale: the perf lane has exactly one documented sufficient install (`.[perf,testing]`), and the `perf` extra now pulls `hiveplotlib[datashader]` self-referentially (below), so an environment missing datashader or psutil in that lane is a half-completed install, not a configuration choice; the desired signal is a loud ImportError, not a graceful skip. Guarding against a broken install of a single-purpose dev lane is excessive paranoia. Consequence: the silent-skip-in-CI masking risk (the 2026-07-02 adversary's worth-discussing item) is eliminated structurally rather than mitigated.
- **Scope boundary (record precisely):** graceful-skip remains the standing convention for the DEFAULT suite's optional-backend markers (users legitimately install backend subsets); this ruling applies to the `perf_harness`/`performance` lane only. `measure_peak_rss_tree`'s internal `RuntimeError` naming the `perf` extra stays — that is the helper's runtime contract, not a test guard.
- **In-scope tweak (pyproject): the `perf` extra gains a self-referential `hiveplotlib[datashader]` dependency**, so the datashader extra's colormap/companion deps ride along and the two extras cannot drift.
- **Conditional (CI): the `performance_gates` job moves to `.[perf,testing]`** if the in-flight engineer's sufficiency verification succeeds; the Implementation log will carry the outcome.
- Dev/CI tooling only; no new library entry point or attribute read (no feasibility audit needed).
