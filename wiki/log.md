---
title: Wiki Log
type: log
updated: 2026-08-01
---

# Hive Plot Research Wiki — Log

## [2026-04-06] setup | Wiki Initialized
Wiki structure created. Directories: `raw/`, `wiki/sources/`, `wiki/entities/`, `wiki/concepts/`, `wiki/analyses/`. Schema defined in `CLAUDE.md`. Index and log files created. Ready for first source ingest.

## [2026-04-06] ingest | hiveplotlib Python repo
Ingested the hiveplotlib Python repository as a foundational source. Created source summary (`sources/hiveplotlib-python.md`), entity page (`entities/hiveplotlib.md` — designated as main hub), and contributed to 7 concept pages. The library has 6 visualization backends, 46 example notebooks, 422 tests at 100% coverage, and key classes: BaseHivePlot, HivePlot, HivePlotMatrix, NodeCollection, Edges, Axis, P2CP.

## [2026-04-06] ingest | hiveplotlib-javascript repo
Ingested the hiveplotlib-javascript repository. Created source summary (`sources/hiveplotlib-javascript.md`) and entity page (`entities/hiveplotlib-javascript.md`). Single-file D3.js library (~650 lines) that renders hiveplotlib JSON exports as browser SVGs. Maps matplotlib kwargs to SVG/D3 attributes. Published as `@hiveplotlib/d3` on npm.

## [2026-04-06] ingest | Krzywinski et al. 2012
Ingested the foundational hive plot paper. Created source summary (`sources/krzywinski-2012.md`) and entity page (`entities/martin-krzywinski.md`). Paper introduces hive plots as rational alternative to force-directed layouts, with case studies on E. coli regulatory network and cancer gene-disease network. ~257 citations. Created concept pages: hive-plot, node-assignment, edge-rendering, force-directed-layout, hive-plot-matrix, p2cp, bezier-curves.

Pages created: 13. Pages updated: index.md, overview.md, log.md.

## [2026-04-06] ingest | Perez et al. 2021 — HyPE (Hive Panel Explorer)
Ingested via web research. Created source summary (`sources/perez-2021-hype.md`). First interactive formalization of the Hive Panel concept. Python 2.7 + D3.js, demonstrated on forest soil microbiome network (1,880 OTUs, 13,605 edges). Krzywinski is co-author. Tool no longer maintained. Updated hive-plot-matrix.md and martin-krzywinski.md with cross-references.

## [2026-04-06] ingest | Bostock 2012 — D3 Hive Plots
Ingested via web research. Created source summary (`sources/bostock-2012-d3-hive-plots.md`) and entity page (`entities/mike-bostock.md`). Influential D3.js v2 hive plot implementation visualizing Flare toolkit dependencies. Key insight: "force layouts make poor use of the most effective visual channel (position)." Architecturally monolithic vs. hiveplotlib-javascript's rendering-only approach.

## [2026-04-06] ingest | Nöllenburg & Wallinger 2023 — Computing Hive Plots
Ingested via web research. Created source summary (`sources/nollenburg-2023-computing-hive-plots.md`). First formal algorithmic framework for hive plot construction — proves all three subproblems (vertex partitioning, axis ordering, vertex ordering) are NP-complete. Updated hive-plot.md and hive-plot-matrix.md.

## [2026-04-06] ingest | Krzywinski et al. 2017 — Differential Hive Plots
Ingested via web research. Created source summary (`sources/krzywinski-2017-differential.md`) and concept page (`concepts/differential-hive-plot.md`). Visual diff between two networks. Published in Leonardo (MIT Press art/science journal). Not yet implemented in hiveplotlib. Updated martin-krzywinski.md.

## [2026-04-06] ingest | Domain Application Survey
Created three application domain concept pages from web research:
- `concepts/applications-bioinformatics.md` — Strongest domain (HyPE, HiveGraph, pleiotropy)
- `concepts/applications-cybersecurity.md` — Most innovative (DDoS classification via hive plot images as ML features)
- `concepts/applications-software-engineering.md` — Linux kernel, Flare toolkit, code dependencies
Updated hive-plot.md with application domain links.

## [2026-04-06] analysis | Karate Club Walkthrough
Created `analyses/karate-club-walkthrough.md` — step-by-step walkthrough of the karate_club.ipynb example. Covers dataset, partitioning strategy (faction membership), sorting (degree), edge styling (color by intra/inter-faction), and key insights (faction separation visible, leaders prominent, cross-faction connections NOT driven by sociability). Also compared with election_96 and bitcoin_user_ratings examples.

## [2026-04-06] lint | First Health Check
Performed first wiki lint pass. Results below.

**Pages created this session:** 23 content pages + index/overview/log
**Pages updated this session:** hive-plot.md, hive-plot-matrix.md, martin-krzywinski.md

## [2026-04-13] ingest + analysis | GNN Heterogeneity Research Direction
Extended wiki with GNN evaluation research direction. Created concept pages for graph neural networks, GNN evaluation metrics, and structural heterogeneity. Created research proposal analysis page exploring use of HivePlotMatrix to expose classification heterogeneity hidden by aggregate GNN metrics. Added source stubs for Ma et al. 2021 (subgroup fairness) and Kipf & Welling 2017 (foundational GCN). Updated 4 existing pages with cross-references. Pages created: 6. Pages updated: 7 (hive-plot-matrix.md, hiveplotlib.md, hive-plot.md, applications-cybersecurity.md, index.md, overview.md, log.md).

## [2026-04-13] query + ingest | Deep Dive on Ma et al. 2021 and GNN Fairness Literature
Full reading of Ma, Deng & Mei 2021 PDF + citation landscape research. Key findings:

**Ma et al. corrections and details:** Authors are Jiaqi Ma, Junwei Deng, Qiaozhu Mei (UMich), not Jing Ma et al. as previously recorded. Paper provides PAC-Bayesian generalization bound (Theorem 3) showing training-set distance (ε_m) is the key predictor of subgroup accuracy. Node centrality (degree, closeness, betweenness, PageRank) shows no clear monotonic trend in their 5-bin bar charts — but this does not rule out non-linear patterns that continuous hive plot axes could capture.

**Citation landscape (2022–2025):** Surveyed citing papers including Subramonian et al. (NeurIPS 2024, 38-paper degree bias survey), GraphPatcher (NeurIPS 2023), DegFairGNN (AAAI 2023), FairACE (2025). All metrics/training-focused, no visualization frameworks. GNNFairViz (IEEE TVCG 2025) is the closest visual analytics competitor but targets demographic-attribute fairness, not structural subgroup diagnosis.

**Novelty confirmed on three axes:** (1) No existing work uses network visualization for structural subgroup performance diagnosis. (2) Edge-level heterogeneity is entirely unexplored in the GNN fairness literature — nobody has examined whether misclassification errors cluster along specific edge types. (3) Multi-dimensional decomposition sweep has no precedent.

**Analysis page reframed:** Added training-set distance and geodesic distance as sweep variables (per Ma et al.). Elevated edge-level heterogeneity as a distinct and novel contribution. Added Related Work section positioning vs. Ma et al., Subramonian et al., and GNNFairViz. Kept degree/centrality as important sweep candidates (non-linear patterns may exist beyond what bar charts capture).

Pages created: 2 (subramonian-2024-degree-bias.md, gnnfairviz-2025.md). Pages rewritten: 1 (ma-2021-subgroup-fairness.md — from stub to full source page). Pages updated: 3 (gnn-heterogeneity-hive-plots.md, index.md, overview.md).

## [2026-04-13] ingest | Subramonian, Kang & Sun 2024 (Degree Bias)
Full web-research ingest. Authors: Arjun Subramonian, Jian Kang, Yizhou Sun (UCLA). Surveys 38 papers on GNN degree bias, catalogs 10+ competing hypotheses, finds many are contradictory (notably H5 vs. H10 on representation variance). Provides first rigorous probabilistic bounds via collision probability, prediction homogeneity, and training dynamics. Key insight: RW and SYM graph filters create degree bias through fundamentally different geometric mechanisms. Heterophilic graphs show weaker degree bias. Visualization: standard scatter/PCA plots only, no network visualization. New sweep variable candidates for HivePlotMatrix: inverse collision probability, prediction homogeneity. Rewrote stub to full source page. Updated concept pages: structural-heterogeneity.md, gnn-evaluation.md, graph-neural-networks.md.

## [2026-04-13] ingest | GNNFairViz (Ye et al. 2025)
Full web-research ingest. Authors: Xinwu Ye et al. (Fudan + Cagliari). Multi-view interactive visual analytics for GNN fairness. Core contribution: bias taxonomy separating model bias from data bias (attribute + structural) via counterfactual what-if comparisons. Key finding: "Overwhelming Effect" — minority node representations dominated by majority neighborhoods through message passing. Uses embedding projections and connectivity summaries, NOT hive plots or rational layouts. Focused on demographic-attribute fairness (gender, race), NOT structural subgroup fairness. Code public on GitHub and PyPI. Confirmed as closest visual analytics competitor but operating in fundamentally different space from HivePlotMatrix proposal. Rewrote stub to full source page. Updated concept pages: gnn-evaluation.md, graph-neural-networks.md.

## [2026-04-13] analysis | Cora Prototype Implementation Plan
Created `analyses/cora-prototype-plan.md` — concrete 5-phase implementation plan for building the GNN heterogeneity HivePlotMatrix prototype on Cora. References actual hiveplotlib API: `HivePlotMatrix.from_variable_sweep()`, `networkx_to_nodes_edges()`, `NodeCollection.create_partition_variable()`, tagged `Edges` for edge-level analysis. Includes: environment setup, GNN training (GCN/GAT/GraphSAGE via PyTorch Geometric), structural property computation (degree, centrality, community, local homophily, training-set distance per Ma et al.), edge-level metadata (error score, cross-group edges, degree ratio), partition sweep, model comparison, and edge-focused visualization. Plan is self-contained for execution in a fresh session.

## [2026-05-17] structure | Added plans/ directory
Added `wiki/plans/` to host working plans alongside ADRs. Motivation: multi-day / multi-week structural plans benefit from version control, and plans + ADRs in the same repo make ADR promotion an intra-repo distillation. Seeded with two in-flight plans: `i-want-to-plan-optimized-hoare.md` and `networkx-metric-expansion-and-backend-refactor.md`. Added `plans/README.md` flagging the directory as scratch work in progress, not curated wiki content. Updated `CLAUDE.md` directory structure.

## [2026-05-31] sync | hiveplotlib v0.28 NetworkX streamlining (scheduled weekly update)
Synced the wiki to a week of hiveplotlib development (commits 2026-05-25 → 2026-05-30) building toward v0.28.0 (`0.28.0a0`), themed "streamlined NetworkX usage." Key shipped behavior: NetworkX is now a first-class input *and* output — `HivePlot(graph=...)` and `HivePlotMatrix.from_partition/from_variable_sweep(graph=...)` ingest a graph directly (with `graph_directed`/`graph_multigraph` defaulting off the graph's own type), `HivePlot.to_networkx()` / `converters.nodes_edges_to_networkx()` export back to any of the four graph classes, and a new `graph_features` package wraps 35 node + 8 edge NetworkX metrics indexed by string name, computable straight onto axes via `node_graph_metrics` / `edge_graph_metrics` at construction. The wrappers are namespaced under `graph_features/networkx/` to leave room for a future `igraph` backend (per the roadmap). Also: public `HivePlotMatrix.backend` property; deprecated `hive_plot_n_axes()` removed; several fixes (integer-partition `KeyError`, `Edges.copy`/`reset_edges` `relevant_edges` sync, `from_tags` tag validation). Three new gallery notebooks (Computing Graph Metrics, Exporting to NetworkX, Exporting to JSON) bring the example count to 49.

Pages created: 1 (`concepts/graph-features.md`). Pages updated: 4 (`entities/hiveplotlib.md`, `concepts/hive-plot-matrix.md`, `index.md`, `overview.md`).

Web scan for new external hive plot work (past week): nothing new surfaced. The most recent notable tool remains [[perez-2021-hype|HyPE]] (2020/2021); no 2026 hive plot papers or tools found.

## [2026-06-06] sync + lint | v0.28 graph-metric type handling refinements (scheduled weekly update)
Synced the wiki to a week of hiveplotlib development (commits 2026-05-30 → 2026-05-31 plus in-flight working-tree changes on the `46-more-streamlined-networkx-usage-and-support` branch). No new public API since last week's `0.28.0a0` sweep; this week refines how the graph-metrics feature picks the internal graph's type. Two refinements worth recording:

- **Directedness inferred from the requested metric set.** Building a `HivePlot` / `HivePlotMatrix` from `nodes` / `edges` with `graph_directed` unset now infers directedness from the metrics requested (`triangles` builds an undirected internal graph, `in_degree` a directed one), falling back to `True` when no requested metric cares. A self-contradictory request (one metric needs directed, another undirected) raises the up-front conflict `ValueError` rather than silently choosing. Building from a `graph` keeps that graph's own type and never re-infers. New internal helpers `_infer_graph_directed` / `_as_metric_name_list`; `_graph_directed` is now `Optional[bool]` (`None` = unpinned, re-inferred per call). `compute_graph_metrics()` still validates only and never infers.
- **Parallel-edge-collapse warning (landing on the branch).** A new `warn_on_parallel_edge_collapse` parameter (default `True`) plus a `_warn_if_parallel_edges_collapse` seam emits a `UserWarning` when a simple-graph build merges same-direction duplicate `(from, to)` rows last-write-wins (so a `weight`-using metric sees only one row). Reciprocal `(a, b)` / `(b, a)` merges under an undirected build do not warn, since that merge is the metric's definitional behavior. Expanded `converters.nodes_edges_to_networkx` and `compute_graph_metrics` docstrings cover the weight-loss caveat.

Also this week (not wiki-bearing): test-suite speedups (smaller holoviews / hive-plot test graphs, a `test-fast` no-coverage target, `--no-cov` where possible), centralized graph-type checking, and doc-revision passes.

Pages updated: 2 (`concepts/graph-features.md` — added a "Graph-type handling" section; `entities/hiveplotlib.md` — graph-metrics bullet now notes directedness inference and the collapse warning).

Lint pass: no contradictions or stale claims found. The earlier `graph-features.md` line "graph_directed ... otherwise it defaults to True" was the behavior superseded this week; corrected in the new "Graph-type handling" section (the unconditional `True` default now applies only when no requested metric implies directedness). No new orphan pages; `graph-features` remains well cross-linked. `index.md` graph-features entry still accurate (no new pages, no count changes).

Web scan for new external hive plot work (past week): nothing new surfaced. arXiv and general search return only the known prior art ([[nollenburg-2023-computing-hive-plots|Computing Hive Plots]], the P2CP-revisited paper, [[perez-2021-hype|HyPE]]); no 2026 hive plot papers or tools found.

## [2026-06-11] sync | graph_metric_backend shipped (backend dispatch for graph metrics)
The graph-metric-backend-dispatch plan fully shipped into v0.28.0 (all five workstreams; final QA: 1317 tests, 100% coverage, docs build clean). A `graph_metric_backend` parameter on `compute_graph_metrics()` / `HivePlot` / all five `HivePlotMatrix` surfaces routes metric computation through networkx's backend dispatch system (nx-parallel tested in CI; nx-cugraph known-good, GPU-only). Semantics: strict up-front validation against the runtime registry (`InvalidGraphMetricBackendError`; degree-family per-metric entries rejected with an actionable message), lenient `NotImplementedError` fallback with INFO logging (the codebase's first stdlib logging), per-metric reserved `"backend"` key with explicit-`None` opt-out, and three-level precedence (per-metric > per-call > stored construction intent, mirroring `graph_directed`). New gallery notebook: Graph Metric Backends. Feeds the combined v0.28 close-out ADR; no standalone record.

Pages updated: 2 (`entities/hiveplotlib.md` — v0.28 NetworkX-integration bullet + notebook count; `concepts/graph-features.md` — new "Backend dispatch (v0.28)" section including the three-senses-of-backend naming-audit triangle). Plan stays in `plans/` for now (two sibling v0.28 plans still in flight; archiving deferred to the combined ADR promotion).

## [2026-06-12] analysis | JOSS submission — Workstream B evidence filed
Filed Workstream B evidence for the JOSS submission plan (`plans/joss-submission.md`) from completed web research (all accessed 2026-06-12). Created three analysis pages feeding the paper: `analyses/nxviz-comparison.md` (State of the field — nxviz revived June 2026 via v0.8.0 plotly backend + v0.9.0 chord diagrams; deprecated OO `HivePlot`, functional 3-group-capped API; fair per-dimension deltas vs hiveplotlib, including the now-stale "matplotlib-only" claim — nxviz has 2 backends as of June 2026); `analyses/hiveplotlib-research-impact.md` (Research impact — thin-but-real: two 2025 bio papers flagged UNVERIFIED/paywalled for maintainer confirmation, IEEE/TDS mentions, PMC7887807 recorded as a false positive, PyPI ~1,743/month, open gaps noted); `analyses/joss-ai-disclosure-precedents.md` (mandatory AI-disclosure wording — policy verbatim, accepted-paper examples grouped minimal/scoped/detailed, recommendation toward the detailed end with final wording gated on Gary). Updated `concepts/hive-plot.md` (competing-implementations table gains a maintenance-status column; nxviz row added). Author-metadata checklist (G3) presented to Gary in the run report, not filed (needs his answers). Pages created: 3. Pages updated: 3 (`concepts/hive-plot.md`, `index.md`, `log.md`).

## [2026-06-13] sync + lint | weekly verification pass (no new public API)
Scheduled weekly run. No new hiveplotlib commits landed this week (HEAD is still 2026-05-31, branch `46-more-streamlined-networkx-usage-and-support`); the substantive v0.28 work this cycle was the `graph_metric_backend` dispatch feature (already filed 2026-06-11) and the JOSS Workstream B research (already filed 2026-06-12). The tool changes are therefore already captured: the `entities/hiveplotlib.md` hub, `concepts/graph-features.md`, and `index.md` all reflect backend dispatch, directedness inference, and the parallel-edge-collapse warning. This run verified that and ran a lint sweep rather than re-documenting shipped work.

Lint fixes: corrected the example-notebook count from 49 to 50 (the Graph Metric Backends gallery notebook pushed the count up; `examples/*.ipynb` now lists 50) on the two living pages that still carried 49 (`overview.md`, `index.md`). The hub page already read 50.

Lint observations (not fixed, recorded for a future pass): `sources/hiveplotlib-python.md` remains a dated 2026-04-06 snapshot (version `0.27.0a1`, 46 notebooks, 422 test functions); it is a point-in-time source summary, not a living page, so the current numbers live on `entities/hiveplotlib.md`. That same snapshot lists the edge-kwarg hierarchy in a different order than the canonical one in `concepts/edge-rendering.md` and the repo (`all` then `clockwise` then `counterclockwise` then `repeat` then `non_repeat`); worth reconciling when that page is next refreshed. No contradictions, stale claims, or orphan pages found among the living pages; the three new JOSS analyses are registered in `index.md` and cross-linked.

Web scan for new external hive plot work (past week): nothing new surfaced. arXiv and general search return only the known prior art ([[nollenburg-2023-computing-hive-plots|Computing Hive Plots]] (arXiv 2309.02273), the [[p2cp|P2CP-revisited]] paper (arXiv 2109.10193), [[perez-2021-hype|HyPE]], and hiveplotlib's own docs). The June-2026 nxviz revival is already filed in `analyses/nxviz-comparison.md`. No 2026 hive plot papers or new tools found.

Pages updated: 2 (`overview.md`, `index.md` — example count 49 to 50, `updated` dates bumped). No pages created.

## [2026-06-18] adr | ADR 0001 filed — NetworkX integration (v0.28, GitLab #46)
2026-06-18: ADR 0001 filed — the combined v0.28 NetworkX close-out, distilling the four working plans (`i-want-to-plan-optimized-hoare`, `networkx-metric-expansion-and-backend-refactor`, `graph-metric-backend-dispatch`, `graph-metric-conflict-validation`) into one record: unified `graph=` input (the `from_networkx` classmethod declined), the `graph_features` package (35 node + 8 edge metrics), `compute_graph_metrics`, `to_networkx` + JSON export, the `graph_metric_backend` dispatch system, and the `@requires_graph_type` conflict-validation layer; Deferred / Declined section captures the `from_variable_sweep` validator gap, the four future-igraph questions (GPL posture gating), and the declined `GraphMetricsSpec` consolidation with its revival trigger. Pages created: 1 (`adr/0001-networkx-integration.md`). Pages updated: 3 (`index.md` — new ADR section; `entities/hiveplotlib.md` — v0.28 marked shipped + ADR cross-reference; `log.md`).

## [2026-06-19] research | Hive plots for knowledge graphs
Open-ended, maintainer-directed exploration of whether hive plots and [[hiveplotlib]] can visualize knowledge graphs. Grounded in a fresh source read (`node.py`, `edges.py`, `hiveplot.py`, `converters.py`) plus web research (Heterogeneous Information Network / metapath literature, Hetionet, the KG-visualization survey literature, RDF vs property graphs). Core synthesis: a KG is too heterogeneous to be one hive plot, but a hive plot is a strong renderer for a scoped, [[metapath]]-shaped slice; the right unit is "one hive plot per question," which fixes the axes (a metapath) and the edges (chosen predicates). hiveplotlib already provides the primitives (entity type as partition, typed edge tags, `HivePlotMatrix.from_tags()` panels, repeat axes for subject/object duality, directed and multigraph edges), so the gaps are ergonomic rather than fundamental: ingestion converters for RDF (`rdflib`) and property graphs (Neo4j), a metapath constructor, schema/aggregation helpers, predicate-faceting ergonomics, and a role-partition convenience. Filed a seven-pattern taxonomy and a Hetionet worked example. Pages created: 3 (`concepts/knowledge-graph.md`, `concepts/metapath.md`, `analyses/hive-plots-for-knowledge-graphs.md`). Pages updated: 2 (`index.md`, `log.md`). Suggested next sources to ingest: an HIN/metapath survey (e.g. Sun & Han) and the Hetionet / Himmelstein drug-repurposing paper.

## [2026-06-20] sync + lint | weekly verification pass (curated llms.txt shipped; v0.28 on master)
Scheduled weekly run. The big change this cycle was already documented as it landed: branch `46-more-streamlined-networkx-usage-and-support` merged to `master` and v0.28.0 released (2026-06-18), distilled in [[0001-networkx-integration|ADR 0001]] (filed 2026-06-18) and the knowledge-graph research (2026-06-19). The one shipped-but-undocumented change this week was the curated `llms.txt`.

- **Curated `llms.txt` shipped (commit `69ed098`, 2026-06-19).** hiveplotlib now serves a hand-curated `llms.txt` (llmstxt.org shape: H1, blockquote summary, `## Section`s of `[page](url): description` bullets) at the docs site root on Read the Docs via a one-line `html_extra_path` addition. The index ranks the conceptual entry points first (`introduction_to_hive_plots`, `quick_hive_plots`, `introduction_to_p2cps`), then an Installation section, then the API reference, with the ~40 gallery notebooks under `## Optional`, so an agent pointed at the docs finds the load-bearing pages before the gallery breadth. Docs-infrastructure only: no Python API, no library behavior change, no new dependency. `llms-full.txt` (full-text concatenation) was scoped out during the grill pass, with a recorded revival trigger (revive if agents need full-text ingestion and the figure-noise problem becomes cheap). The working plan (`plans/llm-friendly-docs.md`) is now fully shipped (both workstreams + QA done) and is eligible for archiving to `plans/archived/` on maintainer confirmation; not moved here (the lifecycle reserves the move for maintainer sign-off).

The other `master` commits this week are not wiki-bearing: `delete setup.py` (cruft removal), `drop gitlab pages` (CI infra; RTD unaffected), and the merge/release commits themselves.

Pages updated: 2 (`entities/hiveplotlib.md` — added an agent-first-docs bullet for the served `llms.txt`; `overview.md` — corrected two "in-progress v0.28" claims to "shipped" now that v0.28 is released).

Lint pass: one stale claim fixed. `overview.md` still framed the v0.28 graph-feature API as "in-progress" in two places (the software-features bullet and the GNN-heterogeneity section); v0.28 released 2026-06-18, so both now read "shipped." No other living page carried an "in-progress / alpha / upcoming" v0.28 claim (full grep clean). Notebook count (50) and metric count (35 node + 8 edge) consistent across hub, index, and overview. No contradictions or orphan pages found among living pages.

Lint observations (recorded, not fixed): (1) `entities/hiveplotlib.md` still reads "Repository: GitLab (private)"; after the v0.28 group migration to `gitlab.com/hiveplotlib`, the repo URL changed and the visibility may have flipped (JOSS submission typically wants a public repo), but visibility was not verifiable from the working tree this run, so the claim is left for maintainer confirmation. (2) `sources/hiveplotlib-python.md` remains a dated 2026-04-06 snapshot (`0.27.0a1`, 46 notebooks, edge-kwarg hierarchy listed in a non-canonical order); it is a point-in-time source summary, not a living page, so the current numbers live on `entities/hiveplotlib.md` (carried over from the 2026-06-13 lint, still true).

Web scan for new external hive plot work (past week): nothing new surfaced. arXiv and general search return only the known prior art ([[krzywinski-2012|Krzywinski 2012]], [[nollenburg-2023-computing-hive-plots|Computing Hive Plots]] (arXiv 2309.02273), the [[p2cp|P2CP-revisited]] paper, [[perez-2021-hype|HyPE]], CommViz semantic hive plots, and hiveplotlib's own docs). No 2026 hive plot papers or new tools found this week. The June-2026 nxviz revival is already filed in `analyses/nxviz-comparison.md`.

## [2026-06-25] analysis | Neural-network training dynamics via P2CP (exploration)
Filed an exploration note for the throwaway `hiveplotlib-nn-viz` prototype (GitHub `gjkoplik/hiveplotlib-nn-viz`; its `PLAN.md` is the canonical summary). The prototype watches a tiny MLP (`784 -> 64 -> 32 -> 10`) learn MNIST as an animated movie over 51 mlflow-logged checkpoints, using hiveplotlib's [[p2cp|P2CP]] and radial layouts. Two figures: (A) per-class output-probability P2CP small multiples via `p2cp_n_axes()` on the softmax (each class blooms from a tight central ring into a petal on its own axis, confusions as secondary lobes), and (B) cross-layer co-activation as a closed three-axis polar triangle (h1/h2/output, all three pairwise relationships) vs a straight-parallel-coordinates comparison that needs a repeated axis to close the loop. Recorded for the reusable hiveplotlib takeaways, not as a shipped feature: usage notes (manual plots via `BaseHivePlot`; `datashade_edges_mpl` is edges-only, compose `axes_viz`; spread the aggregate then shade, `tf.shade(tf.spread(agg))`), the finding that an MLP's discriminative structure reads in output space rather than its distributed hidden code (so Figure A is crisp, Figure B subtler, and B's edges key off class-selectivity), and the prior-art / novelty framing (Figure A complements and cites the Grand Tour; Figure B's neuron-to-neuron co-activation in radial/PC form is defensibly novel). Open question: whether it graduates to a writeup. Pages created: 1 (`analyses/nn-training-dynamics-p2cp-exploration.md`). Pages updated: 4 (`index.md` — new analysis entry + date; `entities/hiveplotlib.md` — exploration pointer under Research Directions + See Also; `concepts/p2cp.md` — new Applications cross-link; `log.md`).

## [2026-06-25] structure | Examples & Applications catalog
Wired up back-links for the `analyses/examples-and-applications.md` catalog (a maps-of-content hub created earlier this session that gathers hiveplotlib's worked examples and application explorations, from shipped notebooks through throwaway prototypes). The hub already linked out to its four cataloged pages and was already referenced from `index.md` and the NN-viz exploration; this pass adds the reciprocal links so each cataloged page points back. Added `[[examples-and-applications]]` to the `## See Also` of the three remaining rows (`analyses/hive-plots-for-knowledge-graphs.md`, `analyses/gnn-heterogeneity-hive-plots.md`, `analyses/karate-club-walkthrough.md`) and a section-level pointer to the catalog near the Research Directions list on the main hub. No change to the catalog's placement (stays a MOC in `analyses/`), scope (all four current rows), or contents. Pages updated: 4 (`analyses/hive-plots-for-knowledge-graphs.md`, `analyses/gnn-heterogeneity-hive-plots.md`, `analyses/karate-club-walkthrough.md`, `entities/hiveplotlib.md`; `updated` dates bumped on the three analysis pages).

## [2026-06-27] sync + lint | weekly verification pass (holoviews >=1.23 backend revision on master; perf harness in flight)
Scheduled weekly run. Synced the wiki to a week of tool changes (master since 2026-06-20), ran a lint sweep, and did a web scan. No commits made; all changes left in the working tree.

One wiki-bearing change shipped to `master` this cycle: the **holoviews edge-plotting revision** (GitLab #30, merged `e9440de`, 2026-06-26). The `hiveplotlib[holoviews]` extra now requires `holoviews>=1.23` (was `>=1.15`); that release ships the upstream fix for [holoviews #6469](https://github.com/holoviz/holoviews/issues/6469), so the library dropped its internal single-color-edge workaround (it had been forcing `cmap` to the intended color on the holoviews-bokeh back end so the `Contours()` call wouldn't pick a vdim for color). Single-color edge behavior is unchanged; only the version floor moved and the hack is gone. The same commit re-ran every example notebook (output churn only) and carried dev-tooling changes that are not wiki-bearing: coverage now uses the Python 3.12+ `sys.monitoring` core (`core = "sysmon"`) instead of `sys.settrace`, unit-test console output moved from verbose to progress dots with `-ra`, and the `test-fast` Makefile target was removed. The other master commit this week (`09fa7a0`, a CHANGELOG note for the already-shipped `llms.txt`) is not wiki-bearing.

In flight (not shipped, not curated onto living pages): the **performance-regression / equivalence harness** (GitLab #52, current branch, working-tree only). `plans/performance-regression-harness.md` was updated this week from a 2026-06-26 grill pass: benchmark scenario sizes pinned (tiny ~34 nodes / 78 edges karate-club scale, small ~2,000 / 5,000, medium ~25,000 / 250,000, large ~100,000 / 2,000,000; 10M+ is a manual stress run, not a standing capture-at-merge scenario), a tiny time-only floor scenario added to catch fixed-overhead "always stream" regressions, and peak-RSS measurement settled on stdlib `resource.getrusage(RUSAGE_CHILDREN).ru_maxrss` around a spawned child as the primary path (psutil / `time -v` demoted to cross-platform fallback, no new core dependency). Plan stays in `plans/`; nothing graduates to a living page until the gate ships.

Pages updated: 1 (`entities/hiveplotlib.md` — Visualization Backends gains the holoviews>=1.23 note; Repository line de-staled from "GitLab (private)" to the `gitlab.com/hiveplotlib` group with visibility left unverified; `updated` bumped).

Lint pass: the long-carried edge-kwarg-order observation is **resolved as a false alarm**. The 2026-06-13 lint flagged that `sources/hiveplotlib-python.md` orders the edge-kwarg hierarchy differently from "the canonical one in `concepts/edge-rendering.md` and the repo," asserting the repo order was `all -> clockwise -> counterclockwise -> repeat -> non_repeat`. Checked against source this run: `EDGE_KWARG_HIERARCHY` (`hiveplot.py:71-77`) and the `set_edge_kwarg_order` docstring default (`:2052`) are both `all -> repeat -> non_repeat -> clockwise -> counterclockwise`, which is exactly what `concepts/edge-rendering.md` already lists. So the living concept page is correct and needs no change; the differing order is in `CLAUDE.md`'s description (repo file, out of scope here) and the dated `sources/hiveplotlib-python.md` snapshot. No living-page fix made. No contradictions or orphan pages found among living pages. Pre-existing minor count nuance left as-is: the hub counts six backends (holoviews-bokeh and holoviews-matplotlib separately) while `analyses/nxviz-comparison.md` counts five backend families (holoviews as one) for the per-dimension comparison; both are internally consistent for their purpose.

Lint observation carried forward (not fixed): `sources/hiveplotlib-python.md` remains a dated 2026-04-06 snapshot (`0.27.0a1`, 46 notebooks); it is a point-in-time source summary, not a living page, so current numbers live on `entities/hiveplotlib.md`.

Web scan for new external hive plot work (past week): nothing new surfaced. Searches return only the known prior art ([[krzywinski-2012|Krzywinski 2012]], [[nollenburg-2023-computing-hive-plots|Computing Hive Plots]] (arXiv 2309.02273), the [[p2cp|P2CP-revisited]] paper, [[perez-2021-hype|HyPE / Hive Panel Explorer]]) and hiveplotlib's own docs / the TDS intro article. No 2026 hive plot papers or new tools this week.

## [2026-07-03] adr + close-out | ADR 0002 filed — Performance regression + equivalence harness (GitLab #52); plan archived
2026-07-03: ADR 0002 filed — full close-out of the performance-regression + equivalence harness (GitLab #52; dev/CI infrastructure, no user-facing API change, not tied to a release), distilling the shipped gate for the scaling milestone: pytest relative same-run ratio gates + ASV capture-at-merge history (hybrid; ASV-as-CI-gate and a hand-rolled baseline store rejected), the dtype-aware equivalence wall with identity-passes / perturbation-fails soundness and comparing-nothing guards, two-tier peak-RSS measurement (child-reported `getrusage(RUSAGE_SELF)` via a two-level subprocess as primary, `tracemalloc` rejected; tier-2 process-tree helper provisional with a hard pruning checkpoint at the scaling chain's close), pinned tiny/small/medium/large scenarios (tiny a time-only floor), canary-armed dormant streamed-vs-single-shot gates, the `perf_harness` marker lane and its CI-semantics invariant (matrix red = release, perf lane red = tooling), the self-referential `perf` extra's loud-failure install contract, and capture-at-merge on the designated machine (`ringtail`) including dependency-bump captures. The working plan moved to `plans/archived/performance-regression-harness.md` (maintainer pre-confirmed), and the active scaling plan's "Prior ADRs / design docs" section was refreshed to cite ADR 0001 and the directly load-bearing ADR 0002. Pages created: 1 (`adr/0002-performance-regression-harness.md`). Pages updated: 5 (`entities/hiveplotlib.md` — harness subsection + scaling development priority + ADR cross-reference; `concepts/bezier-curves.md` — See Also cross-link to the equivalence wall; `plans/scaling-large-networks.md` — Prior ADRs section refresh only; `index.md` — new ADR entry; `log.md`). Pages moved: 1 (`plans/performance-regression-harness.md` → `plans/archived/`).

## [2026-07-04] sync + lint | weekly verification pass (scaling WS1-WS3 in flight on branch 53; master unchanged)
Scheduled weekly run. Synced the wiki to a week of tool changes, ran a lint sweep, and did a web scan. No commits made; all changes left in the working tree.

The week's substantive work is the **scaling milestone starting to land**: the first three of the five-workstream scaling super-plan (`plans/scaling-large-networks.md`) now sit as one commit each on branch `53-scale-hiveplotlib-to-larger-networks`, on top of the merged performance harness. This is the "internal chunking of an in-RAM-but-huge edge frame" trio (needs no narwhals); it directly implements the membership-storage → chunked-rasterization → fused-build chain the [[hiveplotlib|hub]]'s Development Priorities already names. All three carry v0.29.0 (unreleased) CHANGELOG entries but are **not merged to master and not curated onto living pages** (same in-flight discipline the perf harness followed before it shipped). master's HEAD is `dff7ead` ("first performance benchmark"), unchanged for wiki purposes since [[0002-performance-regression-harness|ADR 0002]] was filed 2026-07-03.

- **WS1 — membership storage as scale-adaptive integer indices** (`608575e`). `Edges.relevant_edges` now stores each axis pair's edge membership as an integer array of edge-dataframe row positions rather than a full-length boolean mask (dtype chosen by scale). Anyone reading `relevant_edges` directly must now apply the stored positions positionally (`df.iloc[positions]`). All built-in visualization backends and `HivePlot.to_json` produce identical output as before; the change is structural, not a RAM win in the common case. This is the storage precursor the plan flagged as gating the later chunked paths.
- **WS2 — reduction-aware chunked rasterization** (`413b743`). `datashade_edges_mpl()`, `datashade_nodes_mpl()`, and `datashade_hive_plot_mpl()` gain a `stream_chunk_threshold` parameter that rasterizes large plots chunk by chunk (automatic at 1,000,000+ edges or nodes, forceable either way), bounding peak *transient* memory by the largest chunk rather than the full edge set while matching single-shot output. Moment-based reductions (`ds.var()`, etc.) always rasterize single-shot. Single-shot stays the default (the memory win only matters above a wall most users never hit).
- **WS3 — fused curve construction into streamed rasterization** (`7c0c610`, plus a 2026-07-04 touch-up). The datashader entry points can now rasterize a hive plot straight from its stored edge ids: edge subsets whose curves were never constructed (`add_edge_ids()` without `construct_curves()`) are built during rasterization and immediately discarded, never persisted. With streaming this bounds peak *resident* curve memory by the largest (axis pair, tag) chunk rather than the full edge set. This is the real resident-memory unlock. All other backends still require `construct_curves()`, and those backends (`matplotlib`, `bokeh`, `plotly`, `holoviews`) now **warn** when a stored edge subset holds edge ids but no constructed curves (previously such subsets were silently skipped, often yielding an edge-less figure); the warning names `construct_curves` as the fix and datashader as the rasterize-from-ids alternative. Also fixed under this commit: the multi-tag `ValueError` in `add_edge_curves_between_axes` / `add_edge_kwargs` now lists both directions' tags instead of printing the same direction twice.

Also in flight (not counted, not curated): WS4 (narwhals input boundary) has begun; a new `examples/creating_hive_plots_from_polars.ipynb` is untracked in the working tree, and `src/hiveplotlib/frames.py` (narwhals dispatch) is new but uncommitted. These land on living pages when WS4 ships to master.

Lint pass: clean. Verified the example-notebook count against `git ls-tree master`: 50 tracked notebooks, which is exactly what the three living pages carrying the count (`entities/hiveplotlib.md`, `overview.md`, `index.md`) already say. The working tree shows 51 `.ipynb` files, but the 51st is the untracked in-flight WS4 polars notebook, so the shipped count is unchanged and no fix is warranted. Metric counts (35 node + 8 edge) and the six-backend figure are consistent across hub, index, and overview. No contradictions, stale claims, or orphan pages found among living pages. The hub's forward-looking "scaling super-plan is the next milestone, now unblocked" framing remains accurate (the milestone is underway on branch, not yet merged), so it is left as written.

Lint observations carried forward (not fixed): (1) `entities/hiveplotlib.md` still reads "visibility not re-verified from the working tree" for the `gitlab.com/hiveplotlib` repo; not verifiable from the tree this run, left for maintainer confirmation (carried from 2026-06-27). (2) `sources/hiveplotlib-python.md` remains a dated 2026-04-06 snapshot (`0.27.0a1`, 46 notebooks); it is a point-in-time source summary, not a living page, so the current numbers live on `entities/hiveplotlib.md`.

Web scan for new external hive plot work (past week): nothing new surfaced. Searches return only the known prior art ([[krzywinski-2012|Krzywinski 2012]] / hiveplot.com, [[nollenburg-2023-computing-hive-plots|Computing Hive Plots]] (arXiv 2309.02273), [[perez-2021-hype|HyPE / Hive Panel Explorer]], and assorted older Python ports) plus hiveplotlib's own docs. No 2026 hive plot papers or new tools this week; the June-2026 nxviz revival is already filed in `analyses/nxviz-comparison.md`.

Pages updated: 1 (`log.md`, including the `updated` frontmatter bump to 2026-07-04). No pages created; no living pages edited (this week's work is in-flight on branch `53`).

## [2026-07-04] analysis | Soccer passing networks as hive plots (exploration)
Filed an exploration note for the throwaway `hiveplotlib-futbol` prototype (GitHub `gjkoplik/hiveplotlib-futbol`; its `PLAN.md` and `notes/` are the canonical detail). The prototype renders soccer passing networks as fixed-layout [[hive-plot|hive plots]] on StatsBomb open data: the pass-endpoint model puts two nodes per pass (origin, destination) on their pitch-third axis, sorted by lateral distance from center, so two teams read apples-to-apples, the documented fix for the average-position hairball that makes standard pass networks incomparable. Recorded a nine-figure catalog (flagship two-team comparison, formation "lineups", shot build-up, under-pressure split, total-xT-weighted, mean-xT/danger-per-pass, spatial flow-motifs, turnovers, Barcelona motifs across 45 Guardiola-era La Liga matches), the hard-won design calls (lateral distance-from-center over left/right and over a disingenuous height sort; axis ranges pinned to absolute pitch bounds at construction, not inferred from data), the datashader count-vs-column-reduction semantics, reusable `statsbombpy` gotchas (events return type-grouped, must sort by `index`; NaN conventions; possession-based turnovers and chains), and the two novelty angles (fixed-layout comparability for passing networks; spatial flow-motifs the Gyarmati literature never shows). Nothing shipped into the library; graduation candidates (shot build-up, the two xT lenses, Barcelona motifs) named. Left forward links to unwritten `[[expected-threat]]` and `[[flow-motifs]]` concept pages. Pages created: 1 (`analyses/soccer-passing-hive-plots.md`). Pages updated: 4 (`analyses/examples-and-applications.md` — new catalog row; `entities/hiveplotlib.md` — exploration pointer under Research Directions + See Also; `index.md` — new analysis entry; `overview.md` — one-sentence application-domain note; `log.md`).

## [2026-07-04] analysis | Bioinformatics hive plot examples (exploration)
Filed an exploration note for the public `hiveplotlib-bioinformatics-examples` repo (GitHub `gjkoplik/hiveplotlib-bioinformatics-examples`; its `README.md`, `DATASETS.md`, and `notes/` are the canonical detail), real biological-network hive plots in hiveplotlib's strongest adoption domain ([[applications-bioinformatics]]). The exploration hunted for a "hot damn" hero figure and built two datasets to publishable quality: the **C. elegans connectome** (sensory/interneuron/motor axes; four figures culminating in a two-panel [[hive-plot-matrix|HivePlotMatrix]] `unify_axes=True` male-vs-hermaphrodite comparison with shared-metric-degree node positions) and the **E. coli RegulonDB** signed regulatory network (Krzywinski's role-partition construction, activation/repression/dual edges, a regulatory-core view where sign reads per TF). Honest disposition recorded: both are **credible real-data demonstrations but neither is the hero** (real networks control for nothing, so the connectome comparison collapses to a density difference and the GRN rewards study more than it grabs; datashader was rejected as inapplicable at ~1k edges). The durable strategic conclusion is the **Datasaurus reframe**: the instant-comparison hero is the engineered matched-statistics work ([[same-stats-different-graphs]]), which the real-data examples corroborate rather than replace. Captured a "What was tried and dropped" section (single-network headline, flow coloring, hairball-vs-hiveplot, overlaid differential, datashader rescue, GRN full-hierarchy sign) and a "Reusable hiveplotlib patterns" section (`HivePlot(graph=...)` + `node_graph_metrics`, multi-tag `Edges` semantic coloring, `unify_axes` with shared-metric positions, `repeat_axes`, edge-tag [[differential-hive-plot]] emulation). Also recorded the RegulonDB **licensing wrinkle** (academic/non-commercial only, fragile third-party mirror URL, resolve before public ship). Nothing shipped into the library. Pages created: 1 (`analyses/hiveplotlib-bioinformatics-examples.md`). Pages updated: 4 (`concepts/applications-bioinformatics.md` — Worked Examples section + See Also, noting the neuroscience gap is now filled; `entities/hiveplotlib.md` — exploration pointer under Research Directions + See Also; `analyses/examples-and-applications.md` — new catalog row; `index.md` — new analysis entry; `log.md`).

## [2026-07-04] concept/entity | Soccer-exploration supporting pages (four created; anchor cross-links resolved)
Gave every substantive idea the [[soccer-passing-hive-plots]] anchor references its own citable page. Created three concept pages and one entity page, each grounded in maintainer-supplied domain substance plus the anchor page (external papers cited inline; no `sources/` pages created since none were ingested):
- `concepts/expected-threat.md` — Expected Threat (xT), Karun Singh's 2018 grid-based possession-value Markov model (blog verified 200; 12×8 open grid the prototype used); the StatsBomb OBV commercial sibling; the total-xT and mean-xT hive-plot mappings as the fix for "all passes look equal."
- `concepts/flow-motifs.md` — Gyarmati / Kwak / Rodriguez 2014 (arXiv 1409.0308, title/authors/date verified) five-class 3-pass motif fingerprint (ABAB/ABAC/ABCA/ABCB/ABCD); the tiki-taka outlier finding; the aspatial-literature gap that spatial motifs on a fixed layout fill (Barcelona 36.3% vs opponents 29.7% reuse share). Player-level Peña/Navarro extension cited cautiously as related.
- `entities/statsbomb.md` — StatsBomb Open Data (statsbombpy), the 120×80 event model, non-commercial-with-attribution licensing, 2026 open-data coverage (no Premier League, no live), and the reusable `statsbombpy` gotchas (type-grouped events, sort by `index`; NaN conventions; possession columns; carry endpoints).

## [2026-07-04] analysis | NN co-activation + lock-in rebuild (bounded research-track producer path)
Rebuilt `analyses/nn-training-dynamics-p2cp-exploration.md` in place (slug kept, 5 inbound wikilinks + the examples-catalog row keep resolving) from the bounded, adversary-validated research run (`plans/nn-hiveplot-research-run.md`) that re-grounded the `hiveplotlib-nn-viz` prototype's prior-art position. New H1/title reflects the headline shift to cross-layer co-activation + lock-in across MNIST and Fashion-MNIST. Landed the run's mixed yield (3 validated + 1 validated-inconclusive) into the producer-path slots: **#1** softmax-P2CP is validated *positioning* (a generalizable P2CP complement to the Grand Tour, encoding explicitly prior art, RadViz-on-simplex named as an unswept adjacency); **#2** cross-layer co-activation validated as a *narrow three-property intersection* (co-activation edges + polar/PC rendering + per-image-over-training), each near-miss (Horta 2021, NeuroBreak, TF Playground, VINCI 2010) carrying a faithful quote pinning the one axis it fails, phrased as search-scope with the NOT-swept venues named, never a first-ness grade; **#3** output-vs-hidden distributed-code as a validated *data observation*; **#4** lock-in as **validated-inconclusive**, the visual-novelty note carrying all three caveats in the same breath (named only by analogy at layer/weight level per Achille et al.; actively contested by ITOP; committed/transient split unsettleable as real-vs-artifact in literature mode, deferred to a data-validity run). Stated the honest marginal yield up front (panel *changed* the encoding-novelty demotion + the #2 sharpening + #4's counter-current; *inherited* #1/#3; two staleness fixes were zero-agent edits). Three staleness fixes made: added Fashion-MNIST (peak test acc ~86.4%, grounded from the run's mlflow `test_acc` metric; MNIST ~96.4%) and the lock-in persistence figure (Figure C); corrected the Figure B usage note from `BaseHivePlot` to the generic `HivePlotMatrix` (built-in edge colorbar + cross-cell density unification, grounded against `src/nnviz/plot_dense.py`). Compact sources slot with per-citation trust notes (peer-reviewed vs preprint vs paraphrase). Pages created: 0. Pages updated: 2 (`analyses/nn-training-dynamics-p2cp-exploration.md` — full rebuild; `index.md` — one-line entry refreshed for the new headline). Landed under maintainer approval; not committed.
- `concepts/fixed-layout-comparability.md` — the general hive-plot principle (structural axes make two networks share a frame, which force-directed layouts cannot), the soccer example, and the pin-the-axis-ranges corollary. Cross-linked to `differential-hive-plot` and `hive-plot-matrix` **without duplicating** them: DHP is framed as the single-plot diff form of the same goal, HivePlotMatrix as the multi-panel structure it operationalizes, `structural-heterogeneity` as what a fixed structural frame exposes. Sourced to `krzywinski-2012` (the existing source page) for the hive-plot origin.

Anchor cross-links resolved on `analyses/soccer-passing-hive-plots.md`: dropped the "(page not yet written)" parentheticals on `[[expected-threat]]` / `[[flow-motifs]]`; wikilinked the first "Data and methodology" mention to `[[statsbomb]]`; wikilinked "fixes the layout by structure" in "The bet" to `[[fixed-layout-comparability]]`.

Pages created: 4 (`concepts/expected-threat.md`, `concepts/flow-motifs.md`, `concepts/fixed-layout-comparability.md`, `entities/statsbomb.md`). Pages updated: 3 (`analyses/soccer-passing-hive-plots.md` — anchor cross-links; `index.md` — four new entries under Concepts/Entities; `log.md`). Not committed (wiki is its own repo).

## [2026-07-04] research | Same Stats, Different Graphs grounding & novelty (literature-mode run)
Bounded literature-mode research run against the "Same Stats, Different Graphs" demo in `hiveplotlib-datasaurus` (working plan `plans/same-stats-different-graphs.md`): is the demo well-grounded and honestly framed? Shallow panel of four disjoint lenses (prior-art, counter-evidence, network-science grounding, visualization methods), four independent verify vouchers (claim-maker never the voucher, default-refuted), and one cold convergence-gate adversary. Terminal outcome: validated finding. Established: the network-science foundations are correct (Newman's assortativity sign pattern, the coefficient definition, double-edge-swap degree preservation, configuration-model null); the demo is a novel synthesis, not a reinvention (the cited Chen et al. 2018 holds aggregate statistics as ranges and draws with a spring layout, never hive plots, verified by direct text extraction; holding the full degree sequence identical is a stronger construction); the "rational layout" and hairball framing are Krzywinski's own. The one honest caveat, which the run owns rather than commits: the discriminating power is the degree-rank ordering, not the hive glyph (the hive plot is handed the degree metadata the force-directed foil never gets, and a reordered adjacency matrix would also expose the structure), so the defensible claim is "a degree-rank ordering makes the differences pop, and the hive plot applies one cleanly and reproducibly at scale." Not validated (deferred data-validity mode): the actual renders and measured assortativity values were not reproduced. Also surfaced and fixed: the `ANCHORS.md` "sexual-contact networks" disassortative anchor is imprecise (disassortative by gender, scale-free by degree, no row in Newman's degree table), corrected in the datasaurus repo. Consumption: 9 agents (4 lenses + 4 vouchers + 1 adversary), ~385k subagent tokens, cap 20. Pages created: 1 (`analyses/same-stats-different-graphs-grounding.md`). Pages updated: 2 (`index.md` — new analysis entry; `log.md`). Left in the working tree, not committed. Follow-up noted: the `[[same-stats-different-graphs]]` forward-link in `analyses/hiveplotlib-bioinformatics-examples.md` currently resolves to the plan; consider pointing it at this analysis. A data-validity run is the natural next step.

## [2026-07-04] research | Spectral hive plots: novelty and use-case grounding (deep-research run)
Deep-research run (5 angles, 21 sources fetched, 25 claims adversarially verified, 0 refuted) grounding the new [[spectral-hive-plots]] research direction, whose prototype lives in the separate `hiveplotlib-spectral` repo (spectral clustering drives a hive plot: the partition/cuts assign axes, a higher eigenvector beyond the cut sorts within each axis, the affinity graph supplies edges). Triggered by the maintainer's request to document the exploration's ideas, counterfactuals, relevance, and use cases now that a research capability is wired in. Verdict: a **novel recombination of established parts**, not new in any single ingredient and not already assembled anywhere. Every ingredient is prior art: hive-plot axis assignment is never spectral ([[krzywinski-2012]], [[perez-2021-hype]], [[nollenburg-2023-computing-hive-plots]]); [[koren-2005-graph-drawing|spectral graph drawing]] (Hall 1970, Koren 2005) uses Laplacian eigenvectors as continuous Cartesian coordinates, not radial cluster axes; eigenvector ordering is classic [[atkins-1998-spectral-seriation|Fiedler seriation]]. The one non-obvious move, reading within-cluster structure off a higher eigenvector, is already standard in [[nedialkova-2014-diffusion-map-md|diffusion-map molecular dynamics]] (v1 folded/unfolded, v2 and v9 resolve substructure inside individual states), which both de-risks the idea and locates the novelty squarely in the recombination; it also corroborates the prototype's random-walk-over-symmetric normalization choice, since those diffusion/kinetic-map coordinates are the rw family. Closest spatial analog: [[inoue-2010-diffusion-ppi|diffusion spectral clustering of PPI networks]] (radial scatter, not a hive, no higher-eigenvector within-axis order). Use cases ranked chemistry-first: MD conformational analysis (diffusion maps / Markov state models) is the most technically credible because its eigenvectors are already computed and carry kinetic meaning; [[aron-2020-gnps|GNPS]] MS/MS molecular networks and [[scalfani-2022-chemical-space-networks|chemical space networks]] are the clearest tooling gap (force-directed only, one molecular family visible at a time), though the "would add value" claim is a design inference and GNPS2 may have narrowed the gap; PPI partitioning is a supporting analog. What would kill it: a single paper doing the full three-part recombination (none found, so the claim rests on absence of evidence), or evidence that a t-SNE/UMAP scatter of the eigenvectors reads cut structure and overlap strictly better than an axis layout (secondary question, unresolved by the verified set). The run also connected the prototype's empirical degeneracy finding (the symmetric toy's threefold degeneracy that made the per-axis-sort experiment tempting) to the literature's recognized fragility of eigenvector swapping under eigenvalue near-degeneracy. Consumption: 103 agents, ~3.18M subagent tokens. Pages created: 7 (`analyses/spectral-hive-plots.md` and six `sources/` stubs: koren-2005-graph-drawing, atkins-1998-spectral-seriation, nedialkova-2014-diffusion-map-md, aron-2020-gnps, scalfani-2022-chemical-space-networks, inoue-2010-diffusion-ppi). Pages updated: 3 (`index.md` new source and analysis entries; `overview.md` new Spectral Hive Plots research direction; `log.md`). Left in the working tree, not committed (wiki is its own repo). Follow-ups: the [[spectral-clustering]] concept page and a `node-assignment` cross-link (spectral partition as a "which axis / where on axis" rule) are the deferred scaffolding, currently dangling forward-links; the next empirical test is a real chemistry network, with MD/MSM eigenvectors the highest-credibility entry point since they are already computed.

## [2026-07-04] concept | Spectral-clustering concept page + node-assignment integration (scaffolding)
Completed the deferred scaffolding for the [[spectral-hive-plots]] direction. Created `concepts/spectral-clustering.md` (the pipeline; the three Laplacian normalizations and why rw carries cut meaning while sym only adds a sqrt-degree factor; the trivial / cut / higher-harmonic eigenvector bookkeeping; local scaling for multi-scale density; the near-degeneracy eigenvector-swap fragility; and the relatives that use the same eigenvectors differently, namely spectral graph drawing, seriation, and diffusion maps), which resolves the [[spectral-clustering]] forward-links the analysis page and six source stubs left dangling. Integrated spectral assignment into `concepts/node-assignment.md`: a new "Spectral (data-driven)" axis-assignment subsection (the partition's cuts become the axes, learned from connectivity rather than a node attribute) and a "Spectral" positioning entry (a higher eigenvector beyond the cut as the within-axis order, which is spectral seriation), plus See Also links to [[spectral-clustering]] and [[spectral-hive-plots]]. This is the durable-knowledge complement to the research analysis: the analysis page carries the verdict and citations, these concept pages carry the reusable method. Pages created: 1 (`concepts/spectral-clustering.md`). Pages updated: 3 (`concepts/node-assignment.md` spectral subsection + positioning entry + See Also; `index.md` new concept entry; `log.md`). Not committed (wiki is its own repo).

## [2026-07-04] research | Bioinformatics prior-art reconciliation (producer-path fold)
2026-07-04: folded a validated bounded prior-art panel (six lenses + three verification vouchers, all primary-source-verified) into the backbone `analyses/hiveplotlib-bioinformatics-examples.md`, validating the headline (neither built example is the hero) and *correcting* the backbone where the panel found it wrong. Both of the two planning-mode adversary `must-fix` items were accepted: absence claims were held to a `validated inconclusive` ceiling with named search surfaces (the two headline absences were instead *killed* by located positive refuters, so none laundered into a positive), and the disconfirmation lens was chartered to hunt the *mundane* refuters, which is what surfaced the corrections. Corrections folded: (1) both stories are **re-tellings** of published figures, not firsts (the connectome hairball-vs-hiveplot is [[tabacof-2013-connectome-hive-plots|Tabacof/OpenWorm 2013]], with its own verbatim first-application claim; the sex dimorphism re-encodes [[cook-2019-both-sexes-connectome|Cook 2019]]'s summed color-coded Cartesian figure; the E. coli role-partition GRN is [[krzywinski-2012|Krzywinski 2012]]'s own worked example), so the delta is execution/medium, not primacy; (2) the regulator/manager/workhorse role vocabulary is [[yu-gerstein-2006-regulatory-hierarchy|Yu & Gerstein 2006]]'s, imported by Krzywinski, not his invention, and the repo's "dual" role label is wrong (middle role is "managers"; "dual" collides with RegulonDB's separate activator/repressor/dual *sign* class); (3) RegulonDB's terms are a restrictive academic/noncommercial EULA forbidding redistribution/derivatives, **not** CC-BY, so the repo cannot bundle it, the current student-mirror fetch is itself an EULA violation, and there is no clean permissive swap (Abasy Atlas is CC-BY-NC but RegulonDB-derived, so still EULA-encumbered); (4) the "position metadata isn't cleanly sourceable" dead end is refuted, so a hermaphrodite AP-position structural figure is **buildable** (WormAtlas NeuronType + Skuhersky 2022), with only male-specific tail-neuron positions still a gap; (5) the Datasaurus reframe is grounded, [[kobourov-2018-same-stats-different-graphs|Kobourov 2018]] did the matched-stats device on networks first with a spring reveal, so hiveplotlib's honest delta is the *deterministic* (data-is-a-function, not a random seed) hive-plot medium. Softenings folded: "single plot collapses to density" retreated to "an undifferentiated single plot without a load-bearing axis choice" (celebrated single-panel hive plots read as more than density), and "biology hands you the axes for free" retreated to "a defensible partition" (most C. elegans neurons are polymodal). FOLD-GUARD applied: topically-adjacent sources (NodeTrix density-comparison problem) kept as related reading, not verdict grounding; the refuted Abasy third-party-rights sub-claim dropped. New sources cited as dangling forward-marker wikilinks (no `sources/` stubs fabricated, per the producer-path no-fabrication rule): [[tabacof-2013-connectome-hive-plots]], [[cook-2019-both-sexes-connectome]], [[yu-gerstein-2006-regulatory-hierarchy]], [[kobourov-2018-same-stats-different-graphs]]. Pages updated: 3 (`analyses/hiveplotlib-bioinformatics-examples.md` — full prior-art fold; `plans/bioinformatics-hive-plot-exploration-research.md` — Run outcome note; `index.md` — reconciled entry, source count 1 to 7). Surfaced for maintainer approval, not committed (wiki is its own repo).

## [2026-07-04] data-validity | Same Stats, Different Graphs demo confirmed end to end
Ran the deferred data-validity mode on the [[same-stats-different-graphs-grounding]] finding: executed the generator (`example_same_stats_graphs`, seed=42) in the `hiveplotlib-datasaurus` repo and inspected the five story figures. Every claim the literature run had quarantined now holds. Invariants (all PASS): all six variants share n=1000, m=3068, density=0.00614, the identical sorted degree sequence and degree histogram, and the identical degree at every individual node (so the degree-rank axis positions are pixel-identical), with identical baked degree_rank/degree_group/community attributes and determinism under the seed. Measured structure matches the notebook: assortativity +0.86 (assortative), -0.50 (disassortative), -0.26 (stripes), near 0 (random, community); the community variant has 93% within-community edges vs about 33% elsewhere; core_periphery measures -0.50, essentially identical to disassortative, confirming why it was cut. Renders behave as claimed: spring collapses three of four ladder variants to blobs and unrolls only assortative into a comet; re-ordering the same circle by degree already separates the variants (the ordering, not the glyph, does the work); the hive plots give each variant a distinct signature on the shared scaffold; the community variant hides under the degree partition and pops under the community partition. Updated `analyses/same-stats-different-graphs-grounding.md` (data-validity section added, the "what was not validated" section retired). Also drafted the research's one notebook recommendation into `hiveplotlib-datasaurus/build_notebook.py` (Beat 3: a reordered adjacency matrix would also expose the structure, so the win is the degree-rank ordering applied cleanly at scale; the `.ipynb` needs a rebuild to reflect it). Pages updated: 2 (`analyses/same-stats-different-graphs-grounding.md`, `log.md`). Not committed (wiki is its own repo); the datasaurus-repo edits are likewise uncommitted.

## [2026-07-06] analysis | GNN hive plots: broadened research directions
Reopened the GNN scope after the [[gnn-heterogeneity-findings]] dead end (a refuter or owner for every candidate discovery). The maintainer asked to think broader than the first pass (node-classification error diagnosis on citation graphs, one static encoding) and prioritized a **strong visual artifact** over novelty, accepting the tradeoff explicitly ("visually appealing, but probably nothing research-profound"). Filed a directions menu ranked for artifact strength, organized around four levers the first pass held fixed: the task, what gets plotted (outcomes vs. mechanism), the datasets, and the encoding grammar. Six directions: (1) **over-smoothing as a per-layer HivePlotMatrix** (flagship, top pick, lowest lift; watch class separation collapse with depth, a known phenomenon shown so the conclusion lands instantly); (2) **per-epoch training movies** (the GNN analog of the [[nn-training-dynamics-p2cp-exploration|nn-viz]] MLP work, less contrived because a real graph is watched splitting out; subsumes DIRECTIONS.md's learning-order axis); (3) **link prediction, edge as the prediction** (fixes the medium-mismatch where node classification treats edges as derived; hallucinated vs missed links as tags); (4) **differential hive plot** across architecture/seed/layer (data already in outputs/; shared failure cancels, only the delta shows); (5) **Hetionet KG completion** (joins the [[hive-plots-for-knowledge-graphs]] thread; the [[knowledge-graph-hetionet-spike]] already vendored a CC0 CbGaD slice); (6) **mechanism views** (over-squashing / Ricci curvature, influence/receptive field, embedding-vs-structure mismatch; higher risk). Each direction carries a "prior art to check" line so a downstream run keeps the findings page's counterfactual discipline. Datasets section: flagged the Chameleon/Squirrel degree-inversion in DIRECTIONS.md as fragile (Platonov et al. 2023 ICLR train/test leakage), recommended the modern heterophily benchmarks (roman-empire, amazon-ratings, minesweeper, tolokers, questions), OGB for scale + link-pred, TGB for temporal. Added a **dispatch decomposition** (three independent clusters: pure-ML, KG/biomedical, research-track) so the work can be tasked separately; the KG cluster is split out on its own merits (most distinct technically, has local groundwork) and tasked with a plain benign-purpose framing (network visualization of a public CC0 knowledge graph). Pages created: 2 (`analyses/gnn-research-directions.md`, `concepts/gnn-over-smoothing.md`). Pages updated: 3 (`index.md` — new concept + analysis entries; `overview.md` — Research Directions paragraph after the findings dead end; `log.md`). Not committed (wiki is its own repo; maintainer reviews and commits). A schematic mockup of the over-smoothing per-layer collapse was rendered for the maintainer in-chat.

## [2026-07-06] analysis | GNN directions: repo-state correction
Same-day follow-up to [[gnn-research-directions]] after inspecting the sibling repos. Corrected the page's implicit "build from scratch" framing: `training-gnn-hiveplot` already implements directions 2 (per-epoch node-task training movie: prepare/train/enrich/render/movie pipeline, correct-vs-incorrect HivePlotMatrix panels) and 3 (link task: a TP/FN/FP/TN hive grid with fixed node positions and per-cell edge subsets, animated over training via an inner-product decoder). So those are polish-and-extract, not green-field. Added an "Already exists" bullet to directions 2 and 3 and an "Existing homes" table to the dispatch decomposition mapping every direction to a repo: `training-gnn-hiveplot` (2, 3, and the reusable frame/movie infra for 1), `gnn-hiveplots` (4 differential, 6 mechanism, as post-processing/extension of existing outputs), and a **new bio-isolated repo** for 5 (Hetionet), with the loader graduating into `hiveplotlib` per the [[knowledge-graph-hetionet-spike|spike]]. Net: only direction 5 clearly warrants a new repo; direction 1 (over-smoothing) is the one genuinely new build. Pages updated: 1 (`analyses/gnn-research-directions.md`). Not committed.

## [2026-07-06] analysis | GNN directions: Hetionet reuse + repo target
Follow-up on direction 5 after inspecting the `kg-hetionet-spike` worktree. Confirmed the visualization half already exists there (uncommitted): the `hetionet_cbgad_data()` reader, the vendored CbGaD slice, the `make_hetionet_dataset.py` runner, and `examples/hetionet_kg_views.ipynb` (builds the metapath hive plot: Compound/Gene/Disease axes on `kind`, edge filtering to a metapath's relations, per-relation edge tags via `update_edge_viz_kwargs`, within-metapath degree sort). So only the GNN link-prediction layer is new. Corrected two things in the brief: the target repo is the maintainer's new standalone **`hiveplotlib-hetionet`** (dual-homed, so the reusable loader/rendering can later port into `hiveplotlib`; the maintainer owns porting, and the agent must NOT couple to or modify the actively-changing hiveplotlib worktree, which is mid scaling revision); and the link-prediction target must be an edge type actually present in the slice (`associates` Gene–Disease or `binds` Compound–Gene), since the slice has no direct Compound–treats–Disease edges (the drug-repurposing CtD story needs a re-subset via the runner). Updated direction 5's "Already exists" bullet and the dispatch "Existing homes" table row. Pages updated: 1 (`analyses/gnn-research-directions.md`). Not committed.

## [2026-07-06] build | edge_coverage introspection + Hive Plot Gotchas docs page (GitLab #55)
Wired the shipped edge-coverage-and-gotchas plan (`plans/edge-coverage-and-gotchas-docs.md`, both workstreams committed on branch `55-edge-coverage-and-gotchas`, MR !37; qa pass on both) into the living wiki. WS-A (`e6b6691`) adds a read-only `BaseHivePlot.edge_coverage` property returning an `EdgeCoverage(drawn, total, fraction)` NamedTuple (re-exported from the top level), a percent-only `(N% drawn)` clause in `HivePlot.__repr__` (inward-rounded boundaries), and a single `Edges._drawn_count()` seam so the in-flight scaling membership-storage swap (MR !36) is a one-spot change; deliberately no undrawn-edge warning (the repr clause is the point-of-use signal), no `HivePlotMatrix` aggregate (matrix-level coverage is a conceptual non-goal, not a deferral). WS-B (`1f230be`) adds `examples/hive_plot_gotchas.ipynb`, a short pitfalls-index tutorial covering six gotchas (intra-axis edges, >3-axes adjacency, overplotting, parallel-edge collapse, edge-kwarg-hierarchy overrides, `graph_directed` inference) that cross-references `edge_coverage`. Verified against the branch-55 worktree before curating (not plan prose): the property, NamedTuple, repr clause, top-level export, `_drawn_count` seam, and notebook are all real. Work is unreleased (targets v0.29.0) and lives on branch, so it is recorded on the hub as a post-0.28 `master` capability, not folded into the shipped v0.28 material or the master-tracked 50-notebook count. Pages updated: 1 (`entities/hiveplotlib.md`: `edge_coverage` Key Feature bullet, gotchas-page note, date bump). `index.md` reviewed and left unchanged: no wiki page was created or renamed (the property and notebook are library artifacts), and the index's hub entry is a page-level one-liner, not a capability list. Not committed (wiki is its own repo; maintainer reviews and commits). ADR-promotion eligibility and plan archival surfaced to the maintainer in the run report.

## [2026-07-06] update | Docs cheat sheet, README framing, saving-for-publication notebook shipped to branch (post-task)
The `plans/docs-cheat-sheet-and-readme.md` plan's three workstreams shipped to branch `56-docs-cheat-sheet-and-readme` (draft MR !41 to master, "Closes #56"; qa pass on all three): WS-A a new task-oriented "Cheat Sheet" quick-reference docs page (`docs/source/cheat_sheet.md`) under a new "Quick Reference" toctree, with every fenced snippet run in CI by a new hand-rolled docs-snippet test harness (`tests/docs_cheat_sheet_test.py`; main-stream shared state, per-tab replay — "testability == copy-paste-ability"); WS-B a few sentences of "when and why hive plots" framing at the top of both README copies (the JOSS statement-of-need argument); WS-C a new "Saving Hive Plots for Publication" gallery notebook (`examples/saving_plots_for_publication.ipynb`) consolidating vector/DPI export and the datashader DPI/pixel-spread coupling. Updated `entities/hiveplotlib.md` (Key Features: the three new docs capabilities). Plan proposed for archival (all workstreams shipped) and flagged ADR-eligible (the docs-snippet-test convention) pending Gary's green-light. Not committed (wiki is its own repo); surfaced for maintainer review.

## [2026-07-06] update | HivePlot.unify_axes mirror shipped to branch (post-task)
The `plans/hiveplot-unify-axes.md` plan's four workstreams shipped to branch `51-add-unify-axes-to-hiveplot` (MR !40, pending maintainer review+merge): a `unify_axes` constructor parameter and post-hoc `HivePlot.unify_axes()` method mirroring [[hive-plot-matrix|HivePlotMatrix]], backed by a shared `_unify_axes.py` resolver moved out of `hiveplot_matrix.py`. Updated `entities/hiveplotlib.md` with the capability note (Key Features). Plan stays active (MR unmerged); no ADR promotion (applies ADR 0001's existing symmetry precedent, no new decision). Not committed (wiki is its own repo).

## [2026-07-06] build | Hetionet GNN link-prediction error view (direction 5) in hiveplotlib-hetionet
Executed direction 5 of [[gnn-research-directions]] in the new standalone `hiveplotlib-hetionet` repo. Vendored the CC0 CbGaD slice and its reader, lifted the metapath rendering out of the [[knowledge-graph-hetionet-spike|spike]] notebook into a portable module targeting released hiveplotlib 0.27.0 (precomputed degree sort; the notebook's `node_graph_metrics=` / `graph_multigraph=` do not exist on 0.27.0), and built the new GNN link-prediction layer: a 2-layer GraphSAGE/GCN over a learnable embedding table with an inner-product decoder, a type-constrained Disease-Gene held-out split, and threshold-tuned TP/FN/FP tagging (test AUC ~0.83 SAGE, ~0.80 GCN; trained on CPU for determinism). Rendered the outcomes as small multiples on unified degree-sorted metapath axes after the single-overlay hero was found to occlude the active axis pair. Honest KG findings appended to [[hive-plots-for-knowledge-graphs]] (new gap #7 plus an empirical-follow-up section) and to direction 5 of [[gnn-research-directions]]: the `associates` prediction is bipartite, so the third (Compound) axis is inert (hive plots want three or more meaningful axes; the compelling three-axis version is CtD drug repurposing, which needs a re-subset); degree-sorted axes make degree bias in the errors legible (false positives and false negatives pile on hub genes, connecting to [[subramonian-2024-degree-bias]]); the highest-leverage package asks are edge-styling-by-column and a fixed-layout / swappable-edge-layers object, alongside the [[hiveplot-unify-axes]] affordance; per-edge directedness (the one fundamental flaw) recurred, since the message-passing graph had to be symmetrized. The prior-art check reconfirmed [[jin-2022-gnnlens|GNNLens]] owns visual GNN-error diagnosis, so the claim is a positional KG-error view, not a method. Pages updated: 2 (`analyses/hive-plots-for-knowledge-graphs.md`, `analyses/gnn-research-directions.md`). Repo code lives in `hiveplotlib-hetionet` (not the wiki). Not committed (wiki is its own repo); surfaced for maintainer review.

## [2026-07-06] research | GNN hive-plot work: empirical findings + adversarial prior-art check
Filed the results of executing the [[cora-prototype-plan]] (extended to CiteSeer) and subjected every candidate finding to an adversarial literature counterfactual (a fan-out of parallel literature agents covering six domains: GNN calibration, residual correlation on graphs, slice/subgroup discovery, GNN visual analytics, homophilous-failure mechanisms, and benchmark label quality). The counterfactual pass **demoted or refuted every headline**, which is the point: (1) the homophily predictor is a **confirmation** of known results (Zhu 2020, [[ma-2022-homophily-necessity|Ma 2022]], Loveland 2023, Mao 2023); (2) the proposal's flagship **edge-level "error contagion" is refuted** (2.22x both-wrong enrichment collapses to ~1.16x under a community-x-homophily permutation null, and the premise is textbook via [[huang-2021-correct-and-smooth|Correct & Smooth]] and [[jia-benson-2020-residual-correlation|Jia & Benson]]); (3) the ex-"keeper" **calibration-by-distance is prior art** ([[hsu-2022-gnn-calibration|Hsu et al. 2022]] §4.3 names distance-to-training as a GNN calibration factor on the same model and datasets), leaving only a narrow error-conditional refinement (near-training errors are genuinely overconfident: 57% of Cora adjacent errors and 85% of CiteSeer adjacent errors exceed 0.5 confidence, above the reliability diagonal); (4) the **residual screen** (covariate baseline for per-node error -> edge-level independence null -> flag unexplained joint failures) is **novel only as a composition** ([[congalton-1988-error-autocorrelation|Congalton 1988]] join-count is the sharpest antecedent; [[jin-2022-gnnlens|GNNLens]] is the nearest visual-analytics prior art; slice discovery is a mature field), and its intra-class failure pockets (Cora Probabilistic_Methods; CiteSeer IR/DB/ML) have a mechanism already owned by [[ma-2022-homophily-necessity|Ma 2022]] and an **unruled-out label-noise alternative** (GraphCleaner; the pockets could be coherent mislabels invisible to disagreement-based detectors). Honest meta-conclusion recorded: the statistics did the finding, the hive plot contributed positional layout, and GNNLens already occupies the visual-GNN-error-diagnosis space. Pages created: 7 (`analyses/gnn-heterogeneity-findings.md` and six sources: `hsu-2022-gnn-calibration`, `huang-2021-correct-and-smooth`, `jia-benson-2020-residual-correlation`, `congalton-1988-error-autocorrelation`, `ma-2022-homophily-necessity`, `jin-2022-gnnlens`). Pages updated: 5 (`analyses/gnn-heterogeneity-hive-plots.md` — scorecard replaced with post-counterfactual verdicts + superseded banner; `analyses/cora-prototype-plan.md` — shipped banner; `concepts/gnn-evaluation.md` — calibration-heterogeneity subsection, GNNLens added, opportunity section tempered; `overview.md` — GNN section rewritten to the honest outcome + count table corrected to real values 27/5/21/14; `index.md` — six new sources, new findings analysis, superseded/shipped annotations, date bump). Prototype code lives in the separate `gnn-hiveplots` repo (not the wiki). Not committed (wiki is its own repo); surfaced for maintainer review.

## [2026-07-07] update | Node/edge subset + highlight shipped to branch (post-task)
The `plans/node-edge-subset-highlight.md` plan's ten workstreams (A–J) shipped to branch `59-subset-highlight` (off scaling branch `53`, MR !43 `Closes #59`, targets v0.29.0, unreleased; qa pass on all). Two complementary drill-down/emphasis mechanisms: `HivePlot.subset(nodes=, edges=, hops=, closure=)` returns an independent frozen-geometry (placements + curves + pinned axis ranges) `HivePlot` of the *induced* subgraph (raises `SubsetBeforeBuildError` pre-build); and a named-highlight registry (`add_highlight`/`update_highlight`/`remove_highlight` + `highlights` property) that persists accent state on the plot, drawn as an overlay ABOVE the [[edge-rendering|edge-kwarg hierarchy]] (default `closure="incident"`, Okabe-Ito accent palette, no auto-dimming; `Highlight`/`HIGHLIGHT_ACCENT_PALETTE` top-level re-export mirroring EdgeCoverage). Selection vocabulary: node IDs, boolean masks, narwhals-expression shorthand, axis pairs; `hops` gives undirected ego expansion. Overlay renders on matplotlib/bokeh/holoviews/plotly; datashader RAISES `HighlightsUnsupportedByDatashaderError` and points at the subset-and-datashade recipe. Registry lands on `HivePlot` (not `BaseHivePlot`), so P2CP is untouched. `HivePlotMatrix` broadcasts both across panels. Faithful-lazy posture: subset/highlight, the condition shorthand, AND `hops>0` all run on Dask edge data without materializing the frame (bounded endpoint-ID scans), reproducing the in-memory subgraph byte-for-byte, matching [[0002-performance-regression-harness|the scaling milestone's]] faithful-not-approximate bar. Deletes the four rejected `data_subset`/`data_highlight` TODOs. Two new gallery notebooks (Subsetting Hive Plots, Highlighting Hive Plots). Verified against the plan's Implementation log (WS A–J all complete, final full suite green at 100% coverage) before curating. Pages updated: 1 (`entities/hiveplotlib.md` — subset/highlight Key Feature block, `HivePlot` Main-Classes note, date bump). `index.md` reviewed and left unchanged (no wiki page created or renamed; the methods are library artifacts, and the hub entry is a page-level one-liner). This is a MAJOR multi-workstream plan with all workstreams complete: ADR promotion (next number 0003, alongside sibling ADRs 0001 NetworkX-integration and 0002 perf-harness) and the plan's move to `plans/archived/` are flagged ELIGIBLE, awaiting the maintainer's green-light (not performed here). Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-07-11] update | Scaling-to-larger-networks milestone shipped to branch (weekly wiki update)
Weekly sync of the wiki to a week of tool changes. The main change: the five-workstream scaling super-plan shipped end to end to branch `53-scale-hiveplotlib-to-larger-networks` (MR !36, `Closes #53`, targets v0.29.0, unreleased; commits `608575e`..`3dcc0bc`, 2026-07-03 through 2026-07-09; stress-tested against a 62M-edge OGB network). The five pieces, each cleared through the [[0002-performance-regression-harness|equivalence wall]] before claiming its win: (WS1) `Edges.relevant_edges` stores each axis pair's membership as an integer array of edge-dataframe row positions rather than a full-length boolean mask (read positionally via `df.iloc[positions]`; identical backend output; the seam `edge_coverage` was written behind); (WS2) a `stream_chunk_threshold` parameter on the three `datashade_*_mpl` entry points, auto-chunking at 1M+ edges/nodes, identical figure, peak memory bounded by the largest chunk, non-chunkable reductions (`ds.var`/`ds.std`) exempt; (WS3) rasterize straight from stored edge ids, building curves during rasterization and discarding them (never persisted), with the other four backends now warning instead of silently drawing an edge-less figure when a subset holds ids but no curves; (WS4) the **narwhals input boundary** — `NodeCollection`/`Edges` accept any eager narwhals-supported dataframe (`polars`, `pyarrow`, `modin`, `cuDF`, plus `pandas`), round-trip `data` in the user's own library, and gain `frame_library` + `to_pandas()`; `narwhals` is now a core dependency (pure-Python single wheel; making it optional would fork the code path); (WS5) lazy `dask.dataframe` edge frames stay lazy end to end (datashader's own Dask pipeline partition by partition) and `cuDF` frames rasterize on the GPU via datashader's CUDA path, Dask shipping with `hiveplotlib[datashader]`, plus two CI-only thread-safety fixes (serial Bézier kernel and no concurrent arrow string `Index` construction on the Dask route). Three new gallery notebooks: Creating Hive Plots from Polars / from Dask / from cuDF. Pages updated: 1 (`entities/hiveplotlib.md` — new "Scaling to larger networks" section grounding the previously-dangling narwhals-input-boundary reference in the subset/highlight block, Development Priorities first bullet flipped from "next milestone" to "shipped to branch `53`, awaiting review/merge", Status note that v0.29.0 is accumulating unreleased, date bump). `index.md` reviewed and left unchanged (no wiki page created or renamed; these are library artifacts, and ADR 0002 already indexes the milestone's gate). Also ran a wiki lint pass (see below) and a web scan for recent hive plot news (nothing worth filing). ADR promotion for the scaling milestone (candidate number 0003, alongside 0001 NetworkX-integration and 0002 perf-harness) is a maintainer call once MR !36 merges (not performed here). Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-07-18] update | Review-queue week: no tool changes, wiki reconciled to actual repo state
Weekly wiki sync. **The tool did not change this week.** No commits landed in `hiveplotlib` after 2026-07-09, no merge request changed state after 2026-07-10, and no issue was opened or updated after 2026-07-07; `master` is still at the 2026-07-03 performance harness. Last week's entry already captured the scaling milestone through commit `3dcc0bc`, so this pass reconciled the wiki against repo state rather than against new work, and recorded three things the wiki had been missing.

First, the **review queue** is now the project's actual binding constraint, so it gets its own section on `entities/hiveplotlib.md`: eight open Draft MRs (!36 scaling, !43 subset/highlight, !40 `unify_axes`, !37 `edge_coverage`/gotchas, !41 cheat sheet/README, !38 deprecation policy, !42 overlap-warning fix, !39 graph-metrics tutorials), all targeting v0.29.0, none merged. Recorded the **MR !44 consolidation** (closed 2026-07-07, its SBM gallery folded into !39, which now closes both #49 and #61; branch `61` deletable, issue #61 open until !39 merges) and a **verified merge-order risk**: all seven live branches append to the same unreleased `CHANGELOG.rst` section so every branch after the first conflicts there, and `53`/`51` additionally share `hiveplot.py`, `hiveplot_matrix.py`, `tests/hiveplot_test.py`, and `docs/source/_llms/llms.txt`.

Second, filled **three feature gaps** the entity page had never covered, read from the branches' own changelog entries rather than from plans: named importable warning categories plus the one-minor-version deprecation policy (!38; back-end warnings still subclass `UserWarning` so existing filters match, but a downstream running warnings-as-errors can now silence one category), the inverted overlap-warning gate fix (!42; it now respects `warn_on_overlapping_kwargs` and stops false-alarming on clockwise/counterclockwise edges sharing a `color`), and the six-notebook graph-metrics tutorial series (!39). Added the two unscheduled roadmap items the page also lacked: the `anywidget` kernel-free-docs-embed widget (#58, WS-0 spike passed) and the PEP 604 union-syntax modernization (#54).

Third, the **web scan surfaced one real finding**, a discoverability defect rather than a publication: the pre-migration docs domain `geomdata.gitlab.io/hiveplotlib` still ranks first in search for the library's name, and it now answers with a 302 to a GitLab auth page, so the top organic result is an auth-walled dead end while the following results are stale v0.25 through v0.27 pages. Canonical `hiveplotlib.readthedocs.io/stable/` verified correct at v0.28. This was then run down in conversation with the maintainer and **consciously declined**; see the Status section of `entities/hiveplotlib.md` for the diagnosis, the verified-clean forward-looking metadata, the accepted cost, the two deferred sub-levers, and the JOSS revival trigger. Worth noting for future runs: the old domain sat stale for two-plus years while still ranking first, so its placement is an artifact of accumulated inbound links rather than freshness, and it will not be displaced by improving the new docs. No new hive plot literature or tooling in the last week; search still returns [[krzywinski-2012]] and the 2021 Hive Panel Explorer as the field's landmarks.

**Lint pass: clean.** 98 pages scanned, zero broken wikilinks across curated content (all apparent hits were false positives: table-escaped `\|` aliases, and Python double-bracket list literals inside plan code blocks), zero orphans, no duplicate titles, no `updated`-before-`created` dates. Two by-design gaps left alone: plan files carry no frontmatter (the plan template does not define any, and plans are declared scratch), and `index.md`/`log.md` omit `created`. Fixed `log.md`'s missing trailing newline.

Pages updated: 1 (`entities/hiveplotlib.md`: new "Review queue" section, Status rewritten to "accumulating but nothing landed" plus the docs-discoverability caveat, three new Key Features entries, two new Development Priorities, date bump). `index.md` reviewed and left unchanged (no page created or renamed). ADR promotion for the scaling milestone (candidate 0003) and for subset/highlight both remain flagged ELIGIBLE and blocked on merge, unchanged from last week. Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-07-22] update | PEP 735 dependency-groups migration shipped to working tree (post-task)
The `plans/pep735-dependency-groups.md` plan's five workstreams (A-E) are complete: dev-only dependencies (`dev`, `docs`, `testing`, `perf`, `ruff`, `ty`) moved from `[project.optional-dependencies]` to a PEP 735 `[dependency-groups]` table, with every consumer swept to `--group` install forms (install.sh, Makefile, GitLab CI including the checkout-then-group weekly released-code jobs, Read the Docs `build.jobs.install`, CONTRIBUTING, runners tooling, CHANGELOG). The six user-facing backend extras remain the published extras surface; an independent wheel-metadata witness confirms no dev names in `Provides-Extra`. Five adversary post-impl passes (zero must-fix) and qa close passed (1591 tests, 100% coverage, zero-warning docs build). All of it is uncommitted working-tree state on branch `53-scale-hiveplotlib-to-larger-networks`, awaiting maintainer review; it ships with that branch's merge + immediate release, so the plan stays ACTIVE (not archived). Pages updated: 1 (`entities/hiveplotlib.md` — Status gains a Packaging bullet, date bump). qa flagged the plan ADR-promotion-ELIGIBLE (durable decisions: the user-extras-vs-dev-groups boundary, `hiveplotlib[<extra>]` self-reference composition, the weekly-job checkout-then-group design, and the uv-audit groups-coverage verification), with the maintainer's green-light pending. Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-07-25] update | Second frozen week: no tool changes, review queue re-verified
Weekly wiki sync. **The tool did not change this week.** Verified against the repo and GitLab: no commit after 2026-07-09 (`master` still at the 2026-07-03 performance harness, HEAD `3dcc0bc`), no merge request merged or reopened (all eight Draft MRs !36-!43 last touched 2026-07-10), and no issue opened or updated after 2026-07-07. The only working-tree movement, the PEP 735 dependency-groups migration, was already captured in the 2026-07-22 post-task entry; a re-read of the uncommitted diffs (`pyproject.toml`, `.gitlab-ci.yml`, `.readthedocs.yaml`, `install.sh`, `Makefile`, `runners/performance/*`, `tests/performance_measurement_test.py`) confirmed they are all `[extra]`-to-`--group` consumer sweeps of that same migration, nothing new. This is the second consecutive frozen week; the binding constraint remains merge, not authorship.

Reconciled the wiki to the re-verified state rather than to new work: updated the `entities/hiveplotlib.md` review-queue section heading to "as of 2026-07-25", rewrote its intro to record the two-plus-week freeze with the concrete no-merge / no-commit / no-issue evidence and a pointer to the PEP 735 Packaging note, and annotated the merge-order-risk line as unchanged at the 2026-07-25 re-check. No feature content changed (the page was already current through the 2026-07-22 PEP 735 entry).

**Lint pass: clean.** 101 pages scanned (up from 98, the delta is the untracked `plans/pep735-dependency-groups.md` and `plans/sbm-partition-gallery-example.md` plan files plus one archived plan). Zero duplicate titles, zero `updated`-before-`created` dates, zero missing trailing newlines. 16 apparent broken wikilinks, all confirmed false positives of the two known categories: table-cell aliases whose escaped `\|` leaves a trailing backslash on the captured target (every such target verified to exist as a file, and the unescaped copies of the same links elsewhere pass), and two two-element Python list literals (double-square-bracket `[[...]]` nesting) quoted in prose in an archived plan. Same by-design gaps left alone as prior weeks (plan files carry no frontmatter; `index.md`/`log.md` omit `created`).

**Web scan: nothing new.** A search for recent hive plot work surfaced only long-standing landmarks, all already in the wiki: [[krzywinski-2012]] (the Briefings in Bioinformatics origin), Bostock's D3 hive plots, the 2023 combinatorial-framework paper ([[nollenburg-2023-computing-hive-plots]]), and the 2021 Hive Panel Explorer ([[perez-2021-hype]]). No hive plot literature or tooling published in the last week. Worth noting: the search results themselves reproduce the [[hiveplotlib|`geomdata.gitlab.io` discoverability defect]] filed on 2026-07-18 (stale v0.26/v0.27 doc pages still ranking), which stays consciously declined with the JOSS-landing revival trigger.

Pages updated: 1 (`entities/hiveplotlib.md`: review-queue date and freeze evidence, merge-order re-check note, date bump). `index.md` reviewed and left unchanged (no page created or renamed). ADR promotions for the scaling milestone (candidate 0003) and for subset/highlight both remain flagged ELIGIBLE and blocked on merge, unchanged. Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-08-01] update | Third frozen week for shipped behavior; new tooling backlog (ruff 0.16 #62, PEP 604 #54 re-scope)
Weekly wiki sync. **No shipped tool change again this week.** Verified against the repo and GitLab: no commit after 2026-07-09 (`master` still at the 2026-07-03 performance harness `dff7ead`, branch `53` HEAD `3dcc0bc`), and no merge request merged or reopened (all eight Drafts !36-!43 last touched 2026-07-10). That is the third consecutive frozen week measured by merge, which stays the binding constraint over authorship.

What moved, and the reason this run is not a copy of 2026-07-25: two tooling issues changed on 2026-07-27, both assigned to gjkoplik, neither a library change. **#62 was filed** ("Adopt ruff 0.16's expanded default rule set"): ruff 0.16.0 swapped its small default selection for 415 rules, which land under the project's `extend-select`, so 52 previously-clean lines now fail with no config change; ruff is pinned to `0.15.22` in the `ruff` dependency group and in both the `lint_check` and `format_check` CI jobs, and the issue is to unpin and clear the 52 (top offenders: `PLC0206` dict iteration without `.items()` 22, `BLE001` bare `except Exception` 16, spread across `runners/` 15 and `src/` 12). **#54 was re-scoped** ("Modernize type hints to PEP 604 union syntax"): updated and cross-linked to #62, carving the one `UP035` `typing.Tuple` flag over to #62 and keeping #54 the mechanical `Union`/`Optional` sweep. Both are backlog and tooling planning, so the shipped-behavior freeze holds.

Reconciled the wiki to the re-verified state: bumped `entities/hiveplotlib.md`'s review-queue heading to "as of 2026-08-01", extended the freeze note with the 2026-08-01 no-merge / no-commit evidence and the two-issue movement, annotated the merge-order-risk line as unchanged at the 2026-08-01 re-check, and added a #62 bullet under Development Priorities next to the existing #54 entry.

**Lint pass: clean.** 101 pages scanned. Zero duplicate titles, zero updated-before-created dates, zero missing trailing newlines. 20 apparent unresolved wikilink targets, all pre-existing and non-regressions: the two known false-positive classes from prior weeks (table-cell targets carrying a trailing backslash from an escaped `\|`, and double-square-bracket Python list literals quoted in prose such as `[["origin_country", "origin_continent"]]`), plus 17 display-name links (`[[Martin Krzywinski]]` x14, `[[Mike Bostock]]` x3) that resolve by title to the existing `entities/martin-krzywinski.md` and `entities/mike-bostock.md` pages. Same by-design gaps left alone as prior weeks (plan files carry no frontmatter; `index.md`/`log.md` omit `created`).

**Web scan: nothing new.** A search for recent hive plot work surfaced only long-standing landmarks already in the wiki: [[krzywinski-2012]] (the Briefings in Bioinformatics origin), Bostock's D3 hive plots, the 2023 combinatorial-framework paper ([[nollenburg-2023-computing-hive-plots]]), the 2021 Hive Panel Explorer ([[perez-2021-hype]]), and hiveplot.com. No hive plot literature or tooling published in the last week.

Pages updated: 1 (`entities/hiveplotlib.md`: review-queue date and freeze evidence, new #62 priority bullet, merge-order re-check note, date bump). `index.md` reviewed and left unchanged (no page created or renamed). ADR promotions for the scaling milestone (candidate 0003) and for subset/highlight both remain flagged ELIGIBLE and blocked on merge, unchanged. Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-08-08] update | Authorship resumed on branch `53` (PEP 735 and PEP 604 committed, polars promoted into CI); merge freeze at four weeks
Weekly wiki sync. **The tool changed this week, for the first time since 2026-07-09.** Two commits landed on branch `53` on 2026-08-02 (HEAD `3dcc0bc` to `1700c5f`, MR !36 updated the same day; the other seven Drafts are all still last-touched 2026-07-10). Merge is still the binding constraint: nothing merged, `master` unmoved since the 2026-07-03 performance harness (`dff7ead`), which makes four consecutive weeks measured by merge. Neither commit is a new library feature; both are sweeps that had been sitting in the working tree or the backlog, and both now ride along with !36 rather than getting their own merge windows.

`ee6ac5a` ("type hints and other misc cleanup", 56 files) committed three things. First, the **PEP 735 dependency-groups migration** that had been uncommitted working-tree state since the 2026-07-22 entry: the six dev-only groups moved into `[dependency-groups]`, every consumer swept to `--group` forms, and Read the Docs restructured onto `build.jobs.install` (the `python.install` block with its `extra_requirements` list is gone, since a dev group cannot be requested that way). Second, the **PEP 604 union-syntax modernization** (#54), the one piece with a user-visible effect: all public type hints now use `X | None` / `X | Y` plus builtin and `collections.abc` generics, with a CHANGELOG entry under Changed noting that rendered signatures in the API docs and IDE tooltips change while runtime behavior does not. The repo convention flipped in the same commit, `CLAUDE.md`'s trip-wire going from "Use `Union[X, Y]` not `X | Y`" to the exact inverse. Third, the **ruff pin moved into `pyproject.toml`** (`ruff==0.15.22` in the `ruff` group) to match the pin the `lint_check` and `format_check` CI jobs already carried, each side now commented to point at the other; #62 (unpin and clear the 52 newly-failing lines under ruff 0.16) stays open backlog.

`1700c5f` promoted **polars to a CI-exercised backend** rather than one only covered inside the full dev install. `pyarrow` joined the `polars` extra (polars defers to it for `to_pandas()`), `polars` joined the `base_installs` add-on matrix so the extra installs and tests in isolation, and roughly 335 lines of polars-parametrized cases moved out of `edges_test.py` / `node_test.py` into `polars_test.py`. `frames_test.py` dropped its module-level `pytestmark` so no test carries both `unmarked` and `polars`, which is what kept polars cases out of a base install's `-m unmarked` run. This is the same marker-discipline failure mode that has bitten the project before, fixed at the source rather than at the marker.

One further change sits **uncommitted in the working tree (2026-08-07)** and is wording, not behavior. The equivalence harness's reference side was renamed "proven" to "baseline" throughout `runners/performance/` and its tests, including the public keyword `assert_raster_equivalent(proven_kwargs=)`, now `baseline_kwargs=`; several shipped docstrings and comments also lost their process-flavored framing ("as before", "byte-for-byte unchanged", "the pre-existing pandas code paths"). Alongside it, the two prose sites stating resident curve memory as arithmetic derived from the default (`hiveplot.py`'s `construct_curves` note, `viz/datashader.py`'s streaming-policy comment) were rewritten from "roughly 800 MB per 1,000,000 edges at the default `num_steps=100`" to the symbolic `num_edges * (num_steps + 1) * 8` bytes. Those are exactly the two sites the curve-resolution plan's replace-and-sweep names, so that part of the sweep is pre-paid; the plan itself still has no dispatched code workstream.

Also recorded: the **lower-default-curve-resolution plan** (`plans/lower-default-curve-resolution.md`, opened 2026-08-01, amended twice on 2026-08-02, status *exploring*, no code changed). The default `num_steps=100` is measured at roughly 4-6x more than the default 1500px canvas can resolve; the plan recommends 50 on headroom grounds rather than appearance (not one pixel differs from n=100 by more than 7/255 at true standalone resolution, sub-quarter-pixel at a 4000px export, and margin for the canvas-size and `control_rho_scale` multipliers that 25 and 16 lack), and holds the number open for the grill. The stated asymmetry with the v0.27.0 `dpi` precedent it follows is that a user never *sees* `num_steps` change, so a publication export can go subtly polygonal with no signal. One deferred design question, narrowed by the maintainer and deliberately not a workstream: whether the fused rasterize-from-ids path should carry its own lower default, gated on an unmeasured fused-path memory sweep and on whether path-dependent fidelity is acceptable. `plans/archived/adaptive-curve-sampling.md` holds the measurement that produced the finding and the rejection of adaptive per-curve sampling.

**Lint pass: clean.** 103 pages scanned (up from 101; the delta is the two new plan files committed on 2026-08-02 plus one newly archived plan). Zero duplicate titles, zero updated-before-created dates, zero missing trailing newlines. Six unresolved wikilink targets, all the known Python-list-literal false positive (double-square-bracket list literals quoted in prose, e.g. `[["origin_country", "origin_continent"]]`), all in plan files and one log quotation. The display-name link class flagged in prior weeks (`[[Martin Krzywinski]]`, `[[Mike Bostock]]`) resolves correctly by title and is no longer counted. Same by-design gaps left alone as prior weeks (plan files carry no frontmatter; `index.md` / `log.md` omit `created`).

**Web scan: nothing new.** Searches for recent hive plot work returned only long-standing landmarks already in the wiki: [[krzywinski-2012]], Bostock's D3 hive plots, [[nollenburg-2023-computing-hive-plots|the 2023 combinatorial framework]], [[perez-2021-hype|Hive Panel Explorer]], hiveplot.com, and `hive_networkx`. One near-miss chased down and dismissed: CRAN's HiveR manual carries a 2026-05-07 build date, but the package itself is still 0.4.0 published 2024-07-18, so it is a rebuilt PDF, not a release. Search results continue to reproduce the [[hiveplotlib|`geomdata.gitlab.io` discoverability defect]] filed 2026-07-18 (which stays consciously declined with the JOSS-landing revival trigger), and now also show a stale cached title for the canonical `readthedocs.io/stable/` result reading "hiveplotlib 0.26.2 documentation"; the live page was verified correct at v0.28 on 2026-07-18, so this is search-index staleness rather than a new defect.

Pages updated: 1 (`entities/hiveplotlib.md`: Packaging note flipped from uncommitted to committed, review-queue section rewritten to "as of 2026-08-08" with the authorship-resumed evidence, harness vocabulary synced "proven" to "baseline" with a note on why ADR 0002 is deliberately left alone, polars CI follow-on added to the narwhals bullet, #54 and #62 priority bullets updated, new curve-resolution priority bullet, merge-order re-check note, date bump). `index.md` reviewed and left unchanged (no wiki page created or renamed; it does not index plans). ADR promotions for the scaling milestone (candidate 0003) and for subset/highlight both remain flagged ELIGIBLE and blocked on merge, unchanged. Not committed (wiki is its own repo; maintainer reviews and commits).
## [2026-08-15] update | Workstream 8 added to the scaling milestone (render-time curve geometry); merge freeze at five weeks
Weekly wiki sync. **The biggest single week of library change since the scaling chain closed.** Four commits landed on branch `53` between 2026-08-09 and 2026-08-13 (`1700c5f` to `5da553e`), and a complete, validated, uncommitted Workstream 8 sits on top of them. Merge remains the binding constraint and the freeze deepened: `master` has not moved since the 2026-07-03 performance harness (`dff7ead`), five consecutive weeks measured by merge, and the other seven Draft MRs are all still last-touched 2026-07-09 (`61` on 2026-07-07). Branch `53` is the only thing moving in the project. One detail worth flagging: `5da553e` is local-only, with `origin/53` still at `3f0a7a8`, so the last commit and the entire WS8 working tree are unpushed.

**The four commits are cleanup, not features.** `8fd4908` (2026-08-09, 10 files) dropped self-referential language from shipped code and prose, the comments and docstrings that narrated the project's own history ("as before", "the pre-existing pandas code paths") rather than describing the code; it is the committed form of the sweep that was uncommitted at last week's entry, and it enforces the repo's own no-process-references-in-shipped-code convention. It also carried the equivalence harness's "proven" to "baseline" rename, so that vocabulary note on `entities/hiveplotlib.md` flips from working-tree to committed. `3f0a7a8` (2026-08-12) is post-WS2 comment cleanup in `viz/datashader.py` and `tests/performance_regression_test.py`, net 18 lines removed. `dc0e39d` (2026-08-12) and `5da553e` (2026-08-13) are one-liners (a `CLAUDE.md` adjustment and a warning-language clarification in `viz/input_checks.py`).

**Workstream 8 is the substance: render-time curve geometry on the datashader edge entry points**, 1,231 added and 44 removed lines, uncommitted, across `CHANGELOG.rst`, `exceptions/hive_plot.py`, `hiveplot.py`, `viz/datashader.py`, and three test files. It does two things. First, it **fixes a real defect**: `construct_curves()`, `connect_axes()`, and `add_edge_curves_between_axes()` all hardcoded `num_steps=100` and ignored the instance's `num_steps_per_edge`, so through v0.28 curves built through those three came back at 100 no matter what the constructor was told; all three now default to `None` and resolve against the instance attribute, with a `Changed` CHANGELOG entry. Second, it **adds the lever**: `datashade_edges_mpl()` and `datashade_hive_plot_mpl()` take `num_steps` and `construct_curves_kwargs` (`short_arc`, `control_rho_scale`, `control_angle_shift`), building every rendered chunk at the requested geometry for that call alone, persisting nothing and reading past any stored curves rather than rewriting them. Off-list keys raise the new `InvalidConstructCurvesKwargError` at the entry point before any figure, canvas, or Dask computation, so a bad key does not surface as a partition-sizing error from inside the lazy route. The motivation is WS3-shaped: the fused never-persist route made curve geometry a render-time input, but the only way to lower the sampling rate was `construct_curves()`, which persists every curve and hands back exactly the resident cost the fused route exists to remove. Both docstrings carry the honest carve-out that the render-time lever recovers resident memory only where curves are never persisted, and *raises* peak memory on a curve-persisted instance. A lazy (Dask) subset never persists curves at all, so curve geometry passed to `connect_axes()` for one now warns and names the levers that do apply; the trigger is value-based rather than presence-based, which is what keeps `connect_adjacent_axes`'s own forwards from firing it and taking the Dask suite down under `filterwarnings = error`. Validation: 48 new cases across 20 test functions, full suite **1639 passed, 14 skipped** (cuDF, GPU-gated) at 100% coverage, `make format` / `make ty` / `make docs` clean.

**One must-fix is open** (2026-08-14, adversary post-implementation, verdict `propose` from both post-impl critics). Both entry-point docstrings misstate where a `construct_curves_kwargs`-only rebuild gets its sampling rate: they say every geometry field the call does not restate takes the *construction* default, `num_steps` included, when in fact `num_steps` comes from the instance's `num_steps_per_edge`. On a `HivePlot(num_steps_per_edge=16)` whose curves were persisted at 250, passing `construct_curves_kwargs={"short_arc": False}` alone rebuilds at 16, not 100. Two consequences ride along: the "Default `None` renders persisted curves as stored" sentence is false whenever the sibling dict is non-empty, and on P2CP input the rebuild reads the internal `BaseHivePlot`'s attribute, so a P2CP built at `build_edges(num_steps=16)` and rendered with `construct_curves_kwargs` alone rasterizes at 100 while the docstring claims P2CP works the same way. The adversary traced the false clause to **its own** earlier planning-mode wording, adopted verbatim by the implementer, which is the failure mode worth remembering: a reviewer's suggested phrasing carries reviewer authority into the code and then cannot be caught by that same reviewer's next pass. The existing test cannot see any of it, because its fixtures build instances whose declared rate *is* the library default.

**Also recorded: a parked design question, deliberately not yet a workstream.** WS3's never-persist route is unreachable from the high-level `HivePlot` API for in-memory input, because `HivePlot.__init__` ends with an unconditional `connect_adjacent_axes()` and the constructor carries no parameter to defer it. A pandas user at 10M edges materializes about 8.08 GB of curve coordinates inside `__init__` before they can call a datashader entry point at all, and WS8's render-time lever cannot help, because the persist already happened. Dask input reaches the route automatically; pandas and polars users reach it only through manual `BaseHivePlot` assembly. The maintainer-endorsed direction is a constructor-level `persist_curves: bool = True` flag, binding-constrained to compose with the per-call overrides under the library's existing `None`-defers-to-instance convention, and it still owes a naming audit, API usage examples, and an api-critic planning pass. Two riders travel with it: WS3's headline 5.06 GB to 1.05 GB figure was measured on the manual assembly, so the unwritten WS6 blog notebook and WS7 decision table must say how a reader actually reaches it; and `to_json` raises a bare `KeyError: 'curves'` on an eager ids-only subset (pre-existing, made reachable to more people by WS3/WS5, with the lazy half already raising legibly). **Sibling effect:** WS8 moved the [[hiveplotlib|lower-default-curve-resolution plan]]'s ground favorably. It created `_DEFAULT_NUM_STEPS_PER_EDGE` as the single named home for the number, pinned `HivePlot.__init__`'s signature default equal to it in a test so the two cannot drift, made a user's declared rate actually honored, and shipped the render-time escape hatch. That plan's remaining question is now just the number.

**Lint pass: clean.** 103 pages scanned, unchanged from last week. Zero duplicate titles, zero updated-before-created dates, zero missing trailing newlines, zero orphan pages. Seventeen unresolved wikilink instances, all the known double-square-bracket false positive in quoted code prose, all in plan files and one log quotation; this week's count is higher than last week's six only because the checker's link regex was widened, not because anything regressed. The widening did surface and then clear one candidate class: table-escaped alias links on `analyses/examples-and-applications.md`, written with a backslash before the pipe so it does not split the markdown table cell. That is the correct Obsidian form and resolves normally; the earlier flag was a checker bug, now fixed. Same by-design gaps left alone as prior weeks (plan files carry no frontmatter; `index.md` / `log.md` / the two plan READMEs omit `created`).

**Web scan: nothing new.** Searches for recent hive plot work returned only long-standing landmarks already in the wiki: [[krzywinski-2012]], Bostock's D3 hive plots, [[nollenburg-2023-computing-hive-plots|the 2023 combinatorial framework]], [[perez-2021-hype|Hive Panel Explorer]], hiveplot.com, and the Medium introduction post. No hive plot literature, tooling, or release in the last week. The two known search-index defects both persist unchanged: the [[hiveplotlib|`geomdata.gitlab.io` discoverability defect]] filed 2026-07-18 (consciously declined, JOSS-landing revival trigger) still ranks above the canonical docs, and the `readthedocs.io/stable/` result still carries a stale cached "0.26.2 documentation" title against a live page verified correct at v0.28.

Pages updated: 1 (`entities/hiveplotlib.md`: new "Added after the five (Workstream 8)" subsection under the scaling milestone, vocabulary note flipped from working-tree to committed `8fd4908`, review-queue section rewritten to "as of 2026-08-15" with the four commits and the unpushed-HEAD note, merge-order risk re-checked with the one-directional-growth observation, scaling priority bullet updated, two new priority bullets (the open WS8 must-fix and the parked `persist_curves` question), curve-resolution bullet updated for WS8's effect, date bump). `index.md` reviewed and left unchanged (no wiki page created or renamed; it does not index plans). ADR promotions for the scaling milestone (candidate 0003) and for subset/highlight both remain flagged ELIGIBLE and blocked on merge, unchanged. Not committed (wiki is its own repo; maintainer reviews and commits).

## [2026-08-22] update | Workstreams 8, 9, and 10 land on branch `53`; the scaling milestone's numbers re-measured on the final tree; merge freeze at six weeks
Weekly wiki sync. **The biggest authorship week of the sprint, and the first in a while with nothing left dangling.** Four commits landed on branch `53` between 2026-08-18 and 2026-08-20 (`5da553e` to `3b8d0d6`), 3,870 insertions across 24 files, and unlike last week everything is committed *and* pushed: local and `origin/53` are both at `3b8d0d6`, the working tree is clean apart from the two submodule pointers, and MR !36 shows a 2026-08-20 update. Merge is still the binding constraint and the freeze deepened again: `master` has not moved since the 2026-07-03 performance harness (`dff7ead`), **six consecutive weeks** measured by merge, and the other seven Draft MRs are all still last-touched 2026-07-09 (`61` on 2026-07-07). Branch `53` remains the only thing moving in the project.

**Workstream 8 committed, and closing its must-fix produced a behavior change.** `2ad1916` (2026-08-18) commits the render-time curve geometry work after a post-implementation critic fold-in, a notebook fold-in, and a docstring restructure. The must-fix this log recorded open last week (both datashader entry-point docstrings claiming a `construct_curves_kwargs`-only rebuild takes the *construction* default for `num_steps`) is resolved, and resolving it changed code rather than only prose: `P2CP.build_edges(num_steps=...)` now assigns that rate to the underlying `BaseHivePlot`'s `num_steps_per_edge`, exactly as `HivePlot.__init__` does with its own parameter, so a P2CP built at 16 and rendered with `construct_curves_kwargs` alone rebuilds at 16 rather than at 100. The docstrings now state the two fallbacks separately (the three geometry keys fall back to construction defaults, `num_steps` falls back to the instance attribute), and the curve-geometry model that had been restated across six parameter descriptions was consolidated into one `.. note::` per function. Notably, the adversary had traced that false clause to its own earlier planning-mode wording, adopted verbatim by the implementer.

**Workstream 9 is the substance: `persist_curves`, which makes the never-persist route reachable from the high-level API.** `8966c4d` (2026-08-20, 18 files, +1,876) turns last week's parked emergent finding into a flag. `HivePlot.__init__` ends in an unconditional `connect_adjacent_axes()` with no way to defer it, so WS3's fused route, whose entire value is not persisting curve geometry, was reachable for in-memory input only through manual `BaseHivePlot` assembly. `persist_curves` is now a `HivePlot` constructor parameter, a parameter on all three `HivePlotMatrix` builders, and a `BaseHivePlot` attribute every rebuild honors; `BaseHivePlot.set_persist_curves(persist_curves, drop_existing_curves=False)` sets it after construction and can free curves an instance already holds while keeping its ids and kwargs, with a `HivePlotMatrix` mirror that fans out over every populated cell. The per-call `connect_axes` / `connect_adjacent_axes` overrides in the first draft were dropped on 2026-08-18: one attribute read, nothing to resolve. **Three defects surfaced and were fixed on the branch:** `to_json` raised a bare `KeyError: 'curves'` on an ids-only subset and now raises `UnconstructedCurvesError` naming `construct_curves()` (GitLab #63); `update_edges` treated an ids-only subset as absent, warning that it found no edges while silently dropping the requested keyword arguments, which was live for *every* Dask-backed plot since those never persist curves at all (GitLab #65); and `update_edges` gained `warn_on_no_edges` to match `add_edge_kwargs`. `60dc7d1` and `3b8d0d6` are the follow-on documentation, matrix-render coverage, and a lockfile-refresh line in `install.sh`. Full suite at the tip: **1,739 passed / 14 skipped** (cuDF, GPU-gated) at 100% coverage, 4,703 statements, 0 missed.

**The memory claim is now measured on the `HivePlot` path rather than inferred from the hand assembly.** At 100K nodes and 2M edges on the canonical host `ringtail`, `HivePlot(persist_curves=False)` through the fused streamed route peaks at 1.082 GB against 1.079 GB for the hand-built ids-only assembly (ratio 1.003, pre-registered ceiling 1.25x) and 5.066 GB for the two-stage single shot. The 3.2 MB residual is the entire cost of everything the class adds at that node count. A direct probe of the on-instance representation measured float32 at 808.0 bytes per edge exactly, confirming the plan's own arithmetic rather than the 3.2 GB a float64 assumption had predicted.

**Workstream 10 is new and ships no code: a measurement-confirmation pass re-establishing every published number on the tree that will merge.** Added 2026-08-18 on the maintainer's ask, run 2026-08-21. The motivation is that WS2 and WS3 recorded their figures before WS8 shipped and before WS9 existed, so roughly half the sprint's claims were comparisons assembled across a moving tree. Protocol: one session, one host, six samples per number with the first discarded as warmup and still recorded, each peak-RSS sample its own freshly spawned process, ratios of medians, and a quiet-host rule. **Everything published confirms:** all eight gate constants pass inside 10%, all nine absolute figures inside 4%, and the four derived ogbn-products statistics reproduce bit-identically. One number moved and took the qualified verdict rather than a routed finding: the uncapped ogbn run's peak RSS medians 13.560 GiB against a recorded 11.9 (+13.9%), but its five-sample range spans 24.9%, wider than the band itself, so it reads as "outside the band, not attributable". **The blog post's thread-cap headline was corrected before publication:** with five samples on both sides, tripling the Dask thread cap from 8 to 24 workers costs 64% more peak memory (4.202 to 6.892 GiB) and returns no render speedup the samples can detect, where the earlier single-sample pair implied 46%. A method finding outweighs the protocol change that prompted it: the quiet-host rule took the same configuration's spread from 13.9% to 3.3%, so most of what was diagnosed first as scheduler nondeterminism and then as warmup was contention from the measuring session itself.

**The addendum is the one that matters for how the milestone gets described publicly.** Every WS10 figure compares render *routes on today's code*, which a reader would misread as a version claim. The maintainer asked whether the committed ASV outputs could serve the before/after instead, and they can: the pre-chain baseline captured 2026-07-03 on the same host, run against the branch tip with `--dry-run` so no history was written. **At two million edges the current code rasterizes in 70% of the time using 48% of the memory** (11,173 to 7,850 ms; 5,462 to 2,600 MB), with no user action and no parameter change. Construction also got materially faster at three of four scales, which no workstream claimed and the membership-storage redesign is the unverified candidate for. Two caveats are meant to travel with the numbers: rasterizing a 5,000-edge network costs 9 ms and 64 MB more than in July, unattributable from this data since the July environment resolved a different dependency stack (the maintainer's disposition: worth at most a sentence, stated in absolute terms); and `time_render_matplotlib` has no July counterpart, so **the default matplotlib backend has no before/after at all**.

**What remains on the milestone:** Workstreams 6 (the speedup blog notebook) and 7 (the backend performance decision table), both documentation-only and both un-gated now that WS10 has re-established their numbers. No critic finding is open. #63 and #65 stay open until !36 merges, since their fixes live on the branch. **#64 is the one genuinely open design question** and stays parked as a labeled discussion: `update_edges()` resets geometry parameters it was not asked to change, which has a defensible reading as intended behavior; WS9 corrected the method's inaccurate seeding comment and deliberately left the behavior alone.

**Merge-order risk re-checked, and it is compounding.** This week alone `53` added 412 lines to `hiveplot.py`, 72 to `hiveplot_matrix.py`, and 43 to `CHANGELOG.rst`, three of the four files it already shared with `51`. Every branch after the first will conflict on the shared unreleased v0.29.0 CHANGELOG section regardless; `53` merging first stays the natural order since `59` is stacked on it.

**Lint pass: two real fixes, otherwise clean.** 101 pages scanned (plans excluded from the frontmatter and orphan checks, since they use the harness plan template by design). The long-standing `[[Martin Krzywinski]]` and `[[Mike Bostock]]` links, 11 instances across 9 pages, never resolved: the files are kebab-case and Obsidian matches filenames, not titles. Both entity pages gained an `aliases:` frontmatter field, which fixes every instance at once and also clears the wiki's only orphan page (`entities/mike-bostock.md`, whose sole inbound links were of that form). Nothing else: zero missing frontmatter fields, zero other broken links, zero other orphans. The remaining seven unresolved link targets are all the known double-square-bracket false positive in quoted Python list literals inside fenced code blocks, which Obsidian does not linkify.

**Web scan: nothing new.** Searches for recent hive plot work returned only landmarks already in the wiki: [[krzywinski-2012]], Bostock's D3 hive plots, [[nollenburg-2023-computing-hive-plots|the 2023 combinatorial framework]], [[perez-2021-hype|Hive Panel Explorer]], CommViz's semantic hive plots, hiveplot.com, and the Medium introduction post. No hive plot literature, tooling, or release in the last week. Two standing observations re-confirmed rather than newly found: the docs-discoverability problem is unchanged (the retired `geomdata.gitlab.io` domain still outranks the canonical docs, and several stale version-pinned pages sit above `readthedocs.io/stable`, which itself correctly serves v0.28); and CommViz remains known prior art with no source page of its own, a standing gap rather than a new one.

Pages updated: 4 (`entities/hiveplotlib.md`: the scaling milestone's "Added after the five" subsection extended with the P2CP fix and rewritten as committed, new Workstream 9 and Workstream 10 subsections including the ASV version comparison, review-queue section rewritten to "as of 2026-08-22" with the six-week freeze and the three new issues, merge-order risk re-checked with this week's line counts, scaling priority bullet updated to name the remaining documentation workstreams, the WS8 must-fix priority removed as closed, the parked-design-question priority replaced with #64, and the curve-resolution priority given WS10's measured 808.0 bytes-per-edge figure; `entities/martin-krzywinski.md` and `entities/mike-bostock.md`: `aliases:` frontmatter, lint fix only, no content change; `log.md`). Pages created: 0. Nothing committed.
